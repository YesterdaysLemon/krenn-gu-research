# Component20 p+q wall boundary

This package owns the P4 diagonal-source-torus boundary theorem and its
existing primary verifier and audit.  The current result is `VERIFIED after
repair`; the earlier strengthened realization remains explicitly refuted.

The placement of the `B_full` and `B_drop` branches remains open.  The P5/H22
weighted wall is an external disputed-ownership package, not a member here.
Nothing in this colocation proves H31/H22 closure, component exhaustiveness,
or the global theorem.  Global Krenn--Gu remains **UNRESOLVED**.
