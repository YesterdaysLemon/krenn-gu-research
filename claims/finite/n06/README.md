# Six-vertex package

Start with [`SIX_VERTEX_CERTIFICATE.md`](SIX_VERTEX_CERTIFICATE.md). It states
the exact finite scope, certificate chain, dependencies, and replay
requirements. [`verify_six_vertex_final.py`](verify_six_vertex_final.py) is the
local replay carrier named by that record.

The theorem ledger deliberately retains `historical_certificate_chain`
provenance with no single mapped primary verifier or independent audit.
Co-location does not change that evidence model or extend the claim beyond
order six. The global Krenn-Gu conjecture remains **UNRESOLVED**.

Return to the [finite package index](../README.md).
