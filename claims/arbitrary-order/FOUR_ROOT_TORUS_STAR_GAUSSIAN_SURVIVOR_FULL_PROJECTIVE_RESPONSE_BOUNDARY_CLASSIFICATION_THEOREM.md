# Four-root torus-star Gaussian survivor: full projective response-boundary classification

## Status

**Exact fixed-Gaussian full-boundary classification (`GLD79`).**  Work over
`Q(i)` and then extend scalars to `C`.  Retain the complete canonical `GLD70`
fully-supported, nonisotropic rank-two torus-star interface, the exact
`GLD72` Gaussian survivor, its complete `35`-dimensional raw coefficient
fibre, and the legal `q_0` first response.

After base change to an algebraic closure, the projective boundary of the
homogenized `GLD74` necessary rank-one response incidence consists
scheme-theoretically of exactly the three reduced sign points classified by
`GLD77`.  There are no trivial, standard, mixed-isotypic, or other
proportionality-chart points.  This is the
exhaustive boundary classification needed by the local properness route.  It
does **not** itself exclude a moving survivor neighbourhood, classify another
survivor component or interface, prove source integration or a graph witness,
or resolve Krenn--Gu.  The global conjecture remains **UNRESOLVED**.

## 1. Homogeneous fixed-fibre incidence

Use the exact `GLD74` pivot solve at the Gaussian survivor

```text
alpha=alpha_0+K t,              t in A^35,              (1)
```

and its fixed `65`-dimensional mixed-response quotient.  Write the homogeneous
parts of the first three quotient columns as

```text
K_0 t, K_1 t, K_2 t in Q(i)^65.                       (2)
```

The projective boundary is the rank-at-most-one scheme

```text
B_0={ [t] in P(ker b) : rank[K_0t K_1t K_2t]<=1 }.    (3)
```

This definition does not choose a nonzero column and retains rank-zero
directions.  The usual projective cover has the first chart

```text
(aK_0-K_1)t=0,        (bK_0-K_2)t=0,                  (4)
```

and the two analogous charts with `K_1t` or `K_2t` chosen first.

The exact block minors in Section 3 show that `K_0` has rank `35`.  Hence
`K_0t=0` implies `t=0`, which is not projective.  Thus every point of (3)
lies in the chart (4), and the other two charts contain no additional point.
No projective coordinate is discarded by division.

## 2. Actual leaf symmetry and exhaustive isotypic support

The leaf-permutation group `S_3` preserves the complete fixed interface, the
nuisance map, the legal response maps, the fixed quotient, and the Gaussian
target.  This is checked again after the equal-leaf frame transformation used
by `GLD74`: for every leaf permutation the verifier checks covariance of the
transformed `81 x 79` nuisance map and all transformed `78 x 79` response
maps, invariance of the thirteen-dimensional quotient subspace, and the
induced identities

```text
Q(sigma) K_r = K_r rho_ker(sigma),       r=0,1,2.
```

Thus the three operators in (2) are intertwiners in the literal-Delta
coordinates actually used below.  Exact central idempotents give

```text
ker b = U_triv direct-sum U_sign direct-sum U_std,
dim(U_triv,U_sign,U_std)=(8,3,24),                     (5)

Q_65 = 20 trivial + 3 sign + 21 standard copies.      (6)
```

For fixed `(a,b)`, both equations in (4) preserve these isotypic summands.
If `t=t_triv+t_sign+t_std` solves (4), then every nonzero component solves
its corresponding block equation.  Conversely, a block solution is a full
solution.  Thus components from distinct isotypes cannot cancel, and the
projective support classification is exhaustive once the three pure blocks
are classified.

For the standard block, a transposition has a one-dimensional `+1`
eigenspace in the irreducible two-dimensional standard representation.
Schur's lemma therefore reduces the `24`-dimensional standard isotype to its
`12`-dimensional multiplicity matrix without losing a kernel condition.

## 3. Exact block certificates

### 3.1 Injectivity of `K_0`

In the fixed raw-fibre bases used by the verifier, the following maximal
minors of `K_0` are nonzero:

| block | quotient rows | determinant |
|---|---|---:|
| trivial | `(2,3,6,14,15,16,18,19)` | `-5328+5328i` |
| sign | `(0,19,44)` | `(-1+i)/6` |
| standard multiplicity | `(1,2,3,6,7,18,19,23,25,27,30,43)` | `21275136-7091712i` |

The standard multiplicity minor is injective on all twelve multiplicity
copies and therefore on the complete `24`-dimensional standard isotype.
Together the three minors prove `rank K_0=35`.

### 3.2 Trivial block is empty

Let

```text
A_triv(a)=(aK_0-K_1)|U_triv.                          (7)
```

The two `8 x 8` minors on rows

```text
(2,3,6,14,16,18,19,41),
(2,3,6,14,16,18,19,43)                               (8)
```

have monic gcd `a+1`.  Hence a trivial solution of (4) requires `a=-1`.
At that value (7) has rank seven and its kernel, normalized in the verifier's
fixed basis, is generated by

```text
(-2i/3, (1-i)/18, -2i, 2i, 0, 2-i, -3-i, 1).         (9)
```

Rows `2` and `14` of `(bK_0-K_2)t` are respectively

```text
(-7/3-5i/3)(b(3-8i)+3+2i),       (4+24i)(b-1).        (10)
```

Their monic gcd is one.  Therefore the trivial projective block is empty.

### 3.3 Standard block is empty

On the twelve-dimensional standard multiplicity space put

```text
M_std(a,b)=[aK_0-K_1 ; bK_0-K_2].                     (11)
```

The portable verifier evaluates three exact `12 x 12` minors of the first
pencil `aK_0-K_1`, on quotient rows

```text
(1,2,3,6,7,16,18,19,23,25,27,43),
(1,2,3,6,7,16,18,19,22,23,25,43),
(1,2,3,6,7,16,18,19,22,23,25,44).                    (12)
```

Their monic gcd is `(a+1)^4`.  Thus any common kernel must lie on `a=-1`.
There the first pencil has rank eight and a four-dimensional kernel.  After
restricting `bK_0-K_2` to that kernel, the `4 x 4` minors on quotient rows
`(1,2,3,6)` and `(1,2,3,7)` have monic gcd one in `Q(i)[b]`.
Consequently (11) has rank twelve for every `(a,b)`, and the standard block
is empty.  This is a finite determinantal cover, not sampling.

### 3.4 Sign block

`GLD77` identifies the sign block with

```text
W=[[u, iu+(1+i)v, -u],
   [v, (1-i)u-iv, -v],
   [w, -w, w]].                                       (13)
```

Its two-minor ideal is

```text
((u+v)(u-iv), uw, vw),                                (14)
```

whose projective scheme is the reduced union of three points.  Their
proportionality slopes are

```text
(-1,1), (1,-1), (-1,-1).                              (15)
```

## 4. Theorem and proof-topology consequence

### Theorem 4.1 (complete Gaussian projective boundary)

The scheme (3) is exactly the reduced three-point sign scheme (14).  In
particular it has no point with a nonzero trivial or standard component and
no point in either projective chart omitted from (4).

#### Proof

Injectivity of `K_0` puts every projective point in (4).  Equivariance and
the semisimple decomposition (5)--(6) make the kernel of (4) the direct sum
of its three isotypic kernels.  Sections 3.2 and 3.3 prove that the trivial
and standard kernels vanish for every slope pair.  Hence every projective
solution lies wholly in `U_sign`; Section 3.4 gives exactly the three reduced
points.  Conversely, `GLD77` directly verifies all three sign points.  This
also excludes mixed-isotypic solutions because any such solution would have
a nonzero component in one of the empty blocks.  Every displayed nonzero
minor and unit-gcd identity remains nonzero or a unit after field extension,
so the conclusion is geometric over the algebraic closure.  On a
neighborhood of each remaining sign point, full-rank trivial and standard
minors eliminate those coordinates scheme-theoretically; the sign ideal
(14) is reduced.  Thus no embedded or infinitesimal mixed-isotypic boundary
structure is left.  `square`

This closes the boundary obligation named by `GLD76`--`GLD78`.

**Successor update (`GLD80`, 2026-08-26).**  The intrinsic projective
rank-one incidence, saturated by the affine homogenizing coordinate, is
proper over an affine finite-type survivor neighborhood.  Algebraic DVR
selection and the three `GLD78` determinant units show that its strict
closure misses the Gaussian fibre.  Consequently an existential principal
survivor-open containing `F_0` excludes every raw preimage's first response.
The base polynomial is not explicitly computed; `GLD79` itself remains the
fixed-Gaussian boundary theorem.

## 5. Verification and hostile controls

Run:

```powershell
python claims/arbitrary-order/verify_four_root_torus_star_gaussian_survivor_full_projective_response_boundary_classification.py
python claims/arbitrary-order/verify_four_root_torus_star_survivor_response_sign_boundary_trichotomy.py
python -I claims/arbitrary-order/audit_four_root_torus_star_gaussian_survivor_full_coefficient_fibre_first_response_nonextension.py
```

The first verifier reconstructs the literal `GLD74` quotient, the actual
leaf action, all three central isotypic blocks, the injective `K_0` minors,
and the finite trivial/standard determinant certificates before replaying
the complete `GLD77` sign scheme.  The existing independent no-import audit
reconstructs the base quotient and sign scheme by a separate standard-library
route.  It does not replay the new standard-block determinants; that remaining
independent-evidence limitation is recorded rather than hidden.

The hostile controls are preserved:

- `GLD72` remains an exact concise GHZ tensor in the fixed nuisance space;
- the `GLD70` epsilon generator is not used as a GHZ-membership criterion;
- specialization is the literal Gaussian `65 x 3` quotient of `GLD74`;
- actual interface/source response covariance, not abstract tensor symmetry,
  supplies the direct isotypic decomposition;
- no chosen frame is asserted unique;
- all proportionality charts and rank drops are retained;
- the determinant/resultant identities are exact over `Q(i)`, not modular or
  numerical evidence;
- projective boundary points are not finite raw lifts, source graphs, graph
  witnesses, or counterexamples;
- no fixed-Gaussian statement is promoted to another survivor component,
  another interface, or the global conjecture.
