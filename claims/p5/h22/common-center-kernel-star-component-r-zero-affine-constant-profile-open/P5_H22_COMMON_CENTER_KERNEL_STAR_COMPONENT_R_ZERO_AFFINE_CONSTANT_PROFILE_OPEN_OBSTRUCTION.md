# Component twenty-three `r=0` constant-profile affine weighted-`H22` obstruction

## Status

**Exact characteristic-zero constant-profile-open theorem.**  Put

```text
f=t(t-1)(t+1)
```

and work on the normalized component-twenty-three divisor `r=0` over

```text
Q[t,1/f].
```

Every ordinary finite weight, all three finite endpoints
`lambda=0,1,-1`, and the projective weight are obstructed.  Thus the complete
weighted-`H22` fibre is empty at every point of this affine open inside the
all-pair locus.

This upgrades the separate divisor-generic theorem over `Q(t)`.  It does not
classify the chart boundary `t=0`, the special all-pair fibres `t=1,-1`, any
other component divisor, arbitrary source or ambient changes, or omitted
component charts.  The global Krenn--Gu conjecture remains **UNRESOLVED**.

## The exact constant-profile open

The normalized component basis is

```text
k=(1-rt)/(t-r),
alpha=(A, A+kD, A-C+B+rD, -(A+C)+B+tD),
beta =(B, B+C, C, C).
```

At `r=0`, one has `k=1/t`.  The six pair ranks over `Q(t)` are

```text
(3,3,3,4,4,4).
```

For the only variable rank-four pair, the gcd of its cleared maximal minors is

```text
8*t^2*(t-1)*(t+1).
```

Its rank drops from four to three at both `t=1` and `t=-1`.  The full six-rank
profile at either point is

```text
(3,3,3,4,3,4),
```

so both points remain all-pair and are not all-pair boundaries.  The value
`t=0` is the `k=1/t` chart boundary.  Therefore `f!=0` is exactly the
constant-`(3,3,3,4,4,4)` profile open used here, not the complete affine
all-pair locus.

Mark every row by replacing

```text
beta_i -> beta_i+h_i alpha_i.
```

Use all eight extension entries and combine the fourteen mixed finite-`D01`
rows with the fourteen mixed finite-`D23` rows into a `28 x 8` matrix `M`.

## Ordinary finite weights: one localized unit certificate

Assume

```text
f*lambda*(lambda-1)*(lambda+1) != 0.
```

Take the eight raw maximal minors of `M` on row sets

```text
0,1,3,7,8,9,11,12
0,1,3,4,7,8,9,11
1,3,4,5,7,8,9,11
1,3,4,7,8,9,11,12
1,3,4,7,8,9,11,13
1,3,4,7,8,9,11,14
1,3,4,7,8,9,11,16
1,3,4,7,8,9,11,17.
```

Their only coefficient denominators are powers of `t`.  After clearing those
unit denominators, let `J` be the ideal generated by the eight numerators.
Introduce `u` and compute over the exact rational polynomial ring.  Singular
returns

```text
J + <u*f*lambda*(lambda-1)*(lambda+1)-1> = <1>.     (1)
```

Consequently these eight minors have no common zero anywhere on the ordinary
constant-profile affine open.  The mixed matrix has rank eight there, so every
ordinary weight is obstructed.  This raw-minor certificate makes no divisions
by the branch-tree factors `t-2`, `t-4`, or `3t-4`; those were elimination
artifacts, not exceptional fibres within this open.

## Finite endpoints and projective weight

At `lambda=0`, the dense minor on rows `0,1,3,4,7,8,9,14` is

```text
-256*h3*H0/t,
H0=2*h3*t^2+2*h3*t-4*h3-t+2.                      (2)
```

It remains only to treat `h3=0` and `H0=0`.  The retained `h2=0` module is a
redundant independent check.  All module computations are performed over
`Q[t,h,1/f]`; the `H0=0` branch is computed directly in the quotient by
`<H0>`, without solving for `h3` or dividing by `t+2`.

The exact row-module results are

| weight or branch | mixed-row module | pure-diagonal membership |
| --- | --- | --- |
| `lambda=0, h2=0` | `R^8` | all four |
| `lambda=0, h3=0` | `R^8` | all four |
| `lambda=0, H0=0` | `R^8` | all four |
| `lambda=1` | `<e1,e2,e3,e4,e6,e7,e8>` | `A01,A23,B01` yes; `B23` no |
| `lambda=-1` | `R^8` | all four |
| projective weight | `R^8` | all four |

At `lambda=1`, every mixed-kernel vector has a zero pure diagonal because
three pure diagonals already lie in the displayed rank-seven module.  Hence no
genuine binary restriction survives.  The other five modules are full.  This
closes both weight charts uniformly for every `f!=0`.

## Boundaries and retained special all-pair fibres

The localized proof excludes exactly

```text
t=0,  t=1,  t=-1.
```

Here `t=0` is a normal-form chart boundary.  The points `t=1,-1` are special
all-pair fibres with profile `(3,3,3,4,3,4)` and remain **UNKNOWN**; the present
certificate must not be read as closing them.  In particular, `t=-2` is not
excluded: on that fibre equation `H0=0` reads `4=0`, so that residual branch is
empty.  Likewise `t=2`, `t=4`, and `t=4/3` lie inside the proved open.  No
finite-field calculation is used.

## Replay

```powershell
uv run --with sympy python claims/p5/h22/common-center-kernel-star-component-r-zero-affine-constant-profile-open/verify_p5_h22_common_center_kernel_star_component_r_zero_affine_constant_profile_open_obstruction.py
uv run --with sympy python claims/p5/h22/common-center-kernel-star-component-r-zero-affine-constant-profile-open/audit_p5_h22_common_center_kernel_star_component_r_zero_affine_constant_profile_open_obstruction.py
```

The primary uses the repository model but builds the localized ideals and
modules afresh.  The audit has no repository imports: it rebuilds the
component rows, permanent tensor, mixed matrices, raw minors, and all six
endpoint/projective modules independently over characteristic zero.
