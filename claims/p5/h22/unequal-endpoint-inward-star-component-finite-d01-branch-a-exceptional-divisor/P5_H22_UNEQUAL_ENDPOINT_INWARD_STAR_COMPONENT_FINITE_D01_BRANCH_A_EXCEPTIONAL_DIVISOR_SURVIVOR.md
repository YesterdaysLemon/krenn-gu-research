# Survivor on component twenty-five's finite-`D01` exceptional `A` divisor

## Status

**Exact characteristic-zero terminal-incidence theorem.**  The sole ordinary
exceptional-weight divisor

```text
d_A=(js-1)lambda-(js+1)=0                         (1)
```

on the finite-`D01`, `A=0` branch is generically **populated**, not empty.
Over the generic component field its terminal Segre/incidence ideal is a
proper ideal and equals a compact ideal giving a rational section.

This is a survivor of the binary fixed-vertex test, not a counterexample to
the global Krenn--Gu conjecture.  One-marked ternary compatibility has not yet
been imposed.  The `B=0` branch and all special component divisors remain
separate and **UNKNOWN**.  The global conjecture remains **UNRESOLVED**.

## Exact section

Put

```text
P=ej+k^2,       Q=e+j,       R=1+ejs^2,
F=PR-Q^2.
```

Use the residual parameterization

```text
z_2=(lambda-1)w,       z_4=-(lambda+1)w.
```

Over `C(e,j,s)`, let `D` be generated by `F`, `d_A`, the three forced
mode-zero mixed coordinates, `C_0000-1`, the denominator-free `A` equation,
the denominator-free linear `S_13` residual, and the terminal equations
`S_23,S_123` from the preceding certificates.  Exact block-order reduction
gives a proper standard basis of size nine.

Moreover, `D` has the same reduced ideal as the compact nine-generator ideal
`H` consisting of `F`, `d_A`, and

```text
16s(e-j)Q^2 k w +(js-1)^2 R =0,
4(e^2-k^2)P z_3 +(js-1)=0,
8kQ^2(e-j)z_6+j(js-1)R=0,                         (2)

z_5-z_6+kz_3=0,
kz_1-kQz_6+kPs(lambda-1)w+j(k^2-e^2)z_3=0,
(k^2-e^2)z_7-Pz_6+k^2Q(lambda-1)sw+ez_1=0,

2(lambda-1)kQz_0-2(lambda-1)P^2z_3
 +2(lambda-1)kQ^2(lambda+1)w+1=0.                (3)
```

The two independently constructed standard bases both have size nine.  Every
generator of `D` reduces to zero modulo `std(H)`, every generator of `H`
reduces to zero modulo `std(D)`, and `1` does not reduce to zero.  On the
generic chart, (1)--(3) give

```text
lambda=(js+1)/(js-1),
w=-(js-1)^2 R/[16s(e-j)Q^2k],
z_3=-(js-1)/[4(e^2-k^2)P],
z_6=-j(js-1)R/[8kQ^2(e-j)],                       (4)
```

with the other four extension coordinates uniquely recovered from (3).

## Exact rational witness

The interior component point

```text
(e,j,k,s,lambda)=(-5,2,3,-1,1/3)
```

has

```text
(z_0,z_1,z_2,z_3,z_4,z_5,z_6,z_7)
 =(13/448,-33/56,-1/56,-3/64,
   -1/28,79/448,1/28,5/32).                       (5)
```

Direct exact substitution verifies `F=d_A=0`, the full terminal system, and
all ten normalized two- and three-mode fixed-vertex Segre equations.  Thus
the proper ideal is not merely a formal projection artefact.

## Localization boundary and replay

The ideal equality is over the coefficient field `C(e,j,s)`.  It is therefore
a generic statement and does not classify the base special divisors introduced
by its coefficient denominators.  In particular, `js=1` makes `d_A=-2`, while
`s=0` forces the already separate endpoint `lambda=-1`.  The original chart
also keeps

```text
P R k Q (e-j)(e^2-k^2)(lambda^2-1)
```

nonzero.  No saturation-free assertion over those omitted base fibres is made.
No finite-field computation is used as proof.

Run:

```text
uv run --with sympy python \
  claims/p5/h22/unequal-endpoint-inward-star-component-finite-d01-branch-a-exceptional-divisor/verify_p5_h22_unequal_endpoint_inward_star_component_finite_d01_branch_a_exceptional_divisor.py

uv run --with sympy python \
  claims/p5/h22/unequal-endpoint-inward-star-component-finite-d01-branch-a-exceptional-divisor/audit_p5_h22_unequal_endpoint_inward_star_component_finite_d01_branch_a_exceptional_divisor.py
```

The primary verifier also checks (5) against all ten normalized Segre
equations.  The audit imports no project code, rebuilds every permanent by
subset dynamic programming, and independently verifies the two ideal
containments generator by generator.  Each Singular call is capped at 120
seconds.
