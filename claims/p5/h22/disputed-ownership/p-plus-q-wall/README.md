# Weighted-H22 `p+q=0` disputed-ownership package

Status: **NEUTRAL COLOCATION; SCIENTIFIC DISAGREEMENT UNRESOLVED**

The global Krenn--Gu conjecture remains **UNRESOLVED**.

This narrow package holds exactly the forty artifacts about the weighted-H22
`p+q=0` wall whose evidence and lifecycle records cannot honestly be assigned
to one of the more specific Component20, embedded-P3, projective-descendant,
or status-bearing packages.  Colocation records the disagreement; it does not
adjudicate ownership, reconcile statuses, or turn one artifact into evidence
for another.

The records intentionally retain several different axes and timeslices:

- the aggregate theorem says `VERIFIED` only for the complete nine-stratum
  diagonal-source-torus DVR wall;
- `P5_H22_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_BOUNDARY_PARTIAL.md` remains
  a `SUPERSEDED PARTIAL CHECKPOINT; whole-wall H22 remains UNKNOWN`, and the
  historical coverage audit retains `claim_label: UNKNOWN`;
- the infinity-endpoint independent report remains `REFUTED` only for its
  original one-neighbour/rank-exactness analysis, while later compatibility
  and integration records remain separately `VERIFIED` at their stated
  scopes; and
- discovery blocks marked `CANDIDATE` remain candidate-lineage records even
  where later prose records a scoped verification.

Here, "complete" means only that all forty members of this exact topical and
evidence forest are colocated.  It does not assert mathematical case-cover
completeness.  In particular, this package does not establish a
non-diagonal or arbitrary-`GL4` source closure, the full embedded-P3
projective H22 closure, component exhaustiveness, an arbitrary-order bridge,
a prize graph, or the global theorem.

Files outside this directory that are named or hashed by these records remain
typed dependencies and provenance inputs.  They are not silently owned by
this package.  The separate first/second-component provenance disagreement
also remains separate; no member of that conflict is moved here.

No mathematical status, scope, lifecycle label, evidence role, audit claim,
or historical text is changed by this package.
