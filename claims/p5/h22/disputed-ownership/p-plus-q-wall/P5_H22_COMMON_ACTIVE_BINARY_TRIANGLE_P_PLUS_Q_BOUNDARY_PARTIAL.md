# Partial weighted `H22` checkpoint on the component-twenty `p+q` boundary

## Status

**SUPERSEDED PARTIAL CHECKPOINT; whole-wall `H22` remains UNKNOWN.**  The
independent audit in this file verifies:

1. the entire `a=-1/2` replacement family has a homogeneous Hall-support
   obstruction in direction `D01`;
2. the generic genuine-binary marking projections displayed below are exact;
3. on both `B_full` and `B_drop`, every generic finite-`D01` extension over
   each of the two projected marking lines has a fixed rank-four one-marked
   minor.

Later exact no-import replays also close the generic `D01`-infinity branch on
both charts and every direct `a=0,-1` finite exceptional fibre:

- `P5_H22_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_GENERIC_D01_INFINITY_OBSTRUCTION.md`;
- `P5_H22_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_EXCEPTIONAL_FIBRES_OBSTRUCTION.md`.

The complete `B_drop` finite-`D23` classification retains genuine compatible
rank-at-most-three neighbours and is recorded only as a construction
candidate; the paired `D01`-infinity obstruction is essential.

All these results are conditional on the `p+q` boundary-chart classification in
`P4_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_BOUNDARY.md`.  Direct exceptional
finite fibres and generic displayed charts are now closed.  The projective
lower-pair strata on the omitted free-plane boundary of the embedded-`P_3`
component and the two component-fourteen infinity endpoints remain open, as
do non-diagonal source changes, arbitrary-order gluing, and the global
Krenn--Gu conjecture.

## Weighted directions and chart bases

For a row `z=(z0,z1,z2,z3)`, an extension entry `x`, and finite slope `r`, use

```text
D01^r(z,x)=(r z0+z1,z2,z3,x),
D23^r(z,x)=(z0,z1,r z2+z3,x).                       (1)
```

The corresponding infinity charts are

```text
D01^infinity(z,x)=(z0,z2,z3,x),
D23^infinity(z,x)=(z0,z1,z2,x).                     (2)
```

Put

```text
e=X0, L=A-B, M=A+B, s0=2a+1,
k0=s0 C-a(a+1)L.
```

The two finite-centre boundary orientations are

```text
B_full:
  alpha=(k0,e,e,M),
  beta =(e+lambda L,(a+1)L+C,aL+C,e),
  lambda*s0 != 0;

B_drop:
  alpha=(k0,e,e,M),
  beta =(L,(a+1)L+C,aL+C,e),
  s0 != 0.                                         (3)
```

Every affine marking is `beta_i(h)=beta_i+h_i alpha_i`.

## Exact Hall obstruction on the half-centre replacement

At `a=-1/2`, the valid replacement basis is

```text
alpha=(L,e,e,M),
beta =(C-k e,(1/2)L+C,-(1/2)L+C,e).                 (4)
```

Restore arbitrary nonzero source scalings `s_i` and homogeneous `D01` merge
weights `(rho:sigma)`.  After adjoining arbitrary extension entries, the
four kernel rows become

```text
A0=(sigma*s1,-s2,0,x0),
A1=(rho*s0,0,0,x1),
A2=(rho*s0,0,0,x2),
A3=(sigma*s1,s2,0,x3).                              (5)
```

All four rows miss the retained `C` channel.  Their four-row permanent is
therefore identically zero for every marking, extension, source scaling, and
homogeneous merge weight, including both projective slope endpoints.  Thus
`D01` can never be a required genuine binary neighbour, and the entire
replacement family has empty weighted-`H22` fibre within the established
local framework.  No elimination or finite-field computation is involved.

## Exact generic genuine-binary projections

For either weighted direction, let `M_D(h,r)` be the fourteen-row mixed
matrix in extension coordinates `z=(x0,...,x3;y0,...,y3)`, and let `A_D,B_D`
be the two diagonal rows.  The finite-slope incidence was normalized as

```text
I_D=<M_D z,A_D z-1,w B_D z-1>,                     (6)
```

then `z,w,r` were eliminated exactly over the relevant characteristic-zero
function field.  Infinity uses the same normalization and eliminates `z,w`.

Over `C(a,lambda)`, `B_full` has

```text
J01^finite=J23^finite=<h3,h0,h1*h2>,
J01^infinity=<h3,a*h1+(a+1)*h2,h0,h2^2>,
J23^infinity=<1>.                                   (7)
```

Over `C(a)`, `B_drop` has

```text
J01^finite=J23^finite=<h3,h0,h1*h2>,
J01^infinity=<h3,h0,h1*h2>,
J23^infinity=<1>.                                   (8)
```

These are projection closures.  They classify possible markings but do not,
by themselves, assert that every displayed point has an actual extension.
The common finite marking locus is the union

```text
S1: h=(0,0,t,0),
S2: h=(0,t,0,0).                                    (9)
```

## Complete finite-`D01` certificates for `B_full`

Use extension order

```text
z=(x0,x1,x2,x3;y0,y1,y2,y3).
```

On `S1`, the complete mixed kernel is generated by

```text
v_full,S1=(
 -t(a+1)(2a lambda+a r+lambda)/(a lambda), -1,-1,
 r t/(a lambda);
 (a+r t)/a,
 t(a+1)(lambda+r)/(a lambda),
 t(2a lambda+a r+lambda)/(a lambda),1).             (10)
```

On `S2`, it is generated by

```text
v_full,S2=(
 -a t(2a lambda+a r+lambda+r)/(lambda(a+1)), -1,-1,
 r t/(lambda(a+1));
 (a+r t+1)/(a+1),
 t(2a lambda+a r+lambda+r)/(lambda(a+1)),
 a t(lambda+r)/(lambda(a+1)),1).                    (11)
```

In both cases the mixed rank is seven and the kernel dimension is one.  A
common exact rank witness uses zero-based mixed rows

```text
[1,2,3,4,6,9,13]
```

and extension columns `[0,1,2,3,4,5,6]`, with determinant

```text
-4 a^3 lambda r^6 (a+1)^3 s0^2.                   (12)
```

The two diagonals are

```text
             A                         B
S1   -2 r s0       -2(a+r t)s0(lambda+r)/a
S2   -2 r s0       -2s0(lambda+r)(a+r t+1)/(a+1).  (13)
```

For mode three and row set `0147`, both sheets satisfy, in the displayed
normalization with `z7=1`,

```text
det N3[0147]/(A B)=-2 a^2 lambda r.                (14)
```

On the generic chart open, genuineness gives `A*B!=0`; the remaining factors
in (14) are units.  Replacing the kernel generator by a nonzero projective
multiple `c v` scales `A` and `B` by `c`, the displayed minor by `c^3`, and
the ratio by `c`.  Its nonvanishing, and hence the rank-four conclusion, is
projectively invariant.  Thus every genuine finite-`D01` extension on either
line has marked rank four.

## Complete finite-`D01` certificates for `B_drop`

The complete kernel generators are

```text
v_drop,S1=(
 -t(a+1)s0/a,-1,-1,0;
 0,t(a+1)/a,t s0/a,1),

v_drop,S2=(
 -a t s0/(a+1),-1,-1,0;
 0,t s0/(a+1),a t/(a+1),1).                        (15)
```

Both mixed matrices again have rank seven and one-dimensional kernel.  The
same row and column sets as (12) give determinant

```text
-4 a^3 r^6 (a+1)^3 s0^2.                          (16)
```

The diagonal and fixed-minor identities are

```text
             A                         B
S1   -2 r s0       -2(a+r t)s0/a
S2   -2 r s0       -2s0(a+r t+1)/(a+1),

det N3[0147]/(A B)=-2 a^2 r.                       (17)
```

Hence every generic genuine finite-`D01` extension on `B_drop` also has
marked rank four.

The generic finite-`D01` branch is therefore obstructed on both charts.  The
only remaining generic weighted-`H22` pairing has `D01` at infinity and
`D23` at finite slope, because `J23^infinity=<1>`.

## `D23` audit-only diagnosis

At the exact rational sample

```text
(a,lambda,r,t)=(2,3,5,7),                           (18)
```

the finite-`D23` mixed ranks are

```text
             S1   S2
B_full       8    8
B_drop       7    7.                               (19)
```

This is an exact rational calculation, but it is only diagnostic.  It shows
why a generic-rank-seven `B_full/D23` kernel ansatz is invalid: its incidence
must occur on special slope relations that were removed by the marking
projection.  Equation (19) is not a characteristic-zero nonexistence proof
and is not used to promote any `D23` claim.

## Failure ledger and scope wall

- A first eight-case symbolic minor batch exceeded 142 CPU seconds, was
  terminated, emitted no retained output, and contributes no evidence.
- A later four-case finite-`D23` batch ran about 66 seconds without output and
  was terminated; it also contributes no evidence.
- A fixed-rank-seven `B_full/D23` attempt failed immediately because the
  rational diagnostic matrix has rank eight.  The failed ansatz was not
  repaired silently.
- The denominators and rank witnesses in (10)--(17) vanish at `a=0` or
  `a=-1`; those fibres require direct reconstruction and are not covered.
- At this checkpoint, `D01` infinity and finite `D23` were open.  Later
  no-import replays close the generic `D01`-infinity obstruction, while a
  complete finite-`D23` construction retains compatible neighbours.
  Projection closures are never treated as actual fibres.
- No finite-field computation, parameter grid, all-minor brute force, or
  global graph search is used.
- This checkpoint makes no new `H31`, arbitrary-order reduction, or global
  Krenn--Gu claim.

## Exact replay

```text
uv run --with sympy python \
  claims/p5/h22/disputed-ownership/p-plus-q-wall/verify_p5_h22_common_active_binary_triangle_p_plus_q_boundary_partial.py

uv run --with sympy python \
  claims/p5/h22/disputed-ownership/p-plus-q-wall/audit_p5_h22_common_active_binary_triangle_p_plus_q_boundary_partial.py
```

The verifier reconstructs the half-family Hall support, all four finite and
four infinity projection ideals, the four complete
finite-`D01` kernel and rank witnesses, the fixed `0147` identities, and the
audit-only rational `D23` ranks.  The audit independently rebuilds those
objects without importing the primary verifier and explicitly checks
projective rescaling.  Overall status remains `UNKNOWN`; the stated partial
subclaims, and only those subclaims, are `VERIFIED`.
