# Component twenty-one `q=+/-p`, `ell=+/-1` complete obstruction

## Status

**Exact characteristic-zero endpoint theorem.**  Put `q=epsilon*p`, with
`epsilon=+1` or `-1`, and assume only `p!=0`.  On both endpoint divisors
`ell=epsilon` and `ell=-epsilon`, the complete shared
`D01`-pure/`D23`-binary incidence is empty at the ternary weighted-`H22`
level for every homogeneous weight and every `kappa`, including the
`kappa=0` rank jumps.

The opposite orientation is impossible because the homogeneous all-alpha
`D01` diagonal is Hall-zero.  Thus this closes the weighted-`H22` fibre on
all four sign/endpoint intersections in the fixed contraction order.

At the simultaneous degeneration `p=q=0`, the raw `q=+/-p` chart has
`alpha_0=0` and direct expansion shows that all sixteen pure coefficients
vanish identically.  Its basis has already collapsed, so this is only a
zero-basis degeneration of the displayed chart, not a classification of the
intrinsic projective point.  Separate regular/replacement or blow-up charts
are required, and its normal/projective `P5` fibre remains **UNKNOWN**, as do
arbitrary ambient/source orders and the global local-to-global reduction.
The global Krenn--Gu conjecture remains **UNRESOLVED**.  No finite-field
computation is used.

## Saturated projection ideals

Use the component-twenty-one bases from the companion sign-divisor theorem.
For each chart, set the fifteen unwanted `D01` coefficients and fourteen
mixed `D23` coefficients to zero, normalize the `D01` beta diagonal, and
invert both `D23` diagonals before eliminating the common extension.

On `ell=epsilon`, exact bidirectional standard-basis reduction over
`Q(p,kappa)` gives

```text
<h3, 2*h1+epsilon, 2*p*h0+epsilon>,                (1)
```

with `h2=t` free.  Specializing first to `kappa=0` and repeating the
elimination over `Q(p)` gives the smaller fibre

```text
<h3, h2, 2*h1+epsilon, 2*p*h0+epsilon>.            (2)
```

On `ell=-epsilon`, the function-field projection is

```text
<h3, h2+epsilon*kappa, h1, 2*p*h0+epsilon>,        (3)
```

whereas the direct `kappa=0` projection is

```text
<h3,h2,h1>,                                        (4)
```

with `h0=r` free.  Equations (1)--(4) hold separately for both signs and
both finite weight `[lambda:1]` and weight infinity `[1:0]`.  The direct
`kappa=0` eliminations rule out hidden specialization-only marking branches.

## The endpoint `ell=epsilon`

First suppose `kappa!=0`, write `h2=t`, and put

```text
d=t-epsilon*kappa,
N=kappa*(lambda-1)+t*(lambda+1).                   (5)
```

Away from `d=0`, the complete common kernel is generated by

```text
w=(-2 epsilon p(t+epsilon kappa), -2 epsilon d, 2 kappa, 0;
   d, t+epsilon kappa, 0, 2d).                     (6)
```

For `z=Cw`, the finite-chart diagonals are

```text
B01=4CpN,
A23=16C kappa p(lambda-1),
B23=4Cd(lambda+1),                                 (7)
```

and the `D23` mode-three rows `0147` determinant is

```text
epsilon*512 C^3 kappa p^3 d^2(lambda+1)^3.         (8)
```

At weight infinity, (7) becomes

```text
B01=4Cp(t+kappa), A23=16C kappa p, B23=4Cd,        (9)
```

and (8) loses its weight factor.  Each determinant is nonzero wherever all
three diagonals are nonzero.  On `d=0`, the complete kernel is instead the
line generated by

```text
(-2p,0,1,0;0,epsilon,0,0),                        (10)
```

and its `B23` diagonal is identically zero, so that branch is nongenuine.

At `kappa=0`, (2) forces `t=0` and the common kernel is exactly the plane
spanned by

```text
vX=(-2p,0,1,0;0,epsilon,0,0),
vY=(0,-epsilon,-epsilon/2,0;1/2,0,0,1).           (11)
```

For `z=XvX+YvY`, the finite diagonals are

```text
epsilon=+1: (2p(2X lambda+Y),  4p(2X-Y)(lambda-1), 2Y(lambda+1)),
epsilon=-1: (2p(2X+Y lambda), -4p(2X-Y)(lambda-1), 2Y(lambda+1)). (12)
```

At infinity they are respectively

```text
(4Xp, 4p(2X-Y), 2Y),
(2Yp,-4p(2X-Y), 2Y).                              (13)
```

In every case the same mode-three rows `0147` have determinant

```text
32 Y^2 p^3(2X-Y)(lambda+1)^3                      (finite),
32 Y^2 p^3(2X-Y)                                  (infinity), (14)
```

which is nonzero on the genuine diagonal open.

## The endpoint `ell=-epsilon`

For `kappa!=0`, (3) has complete common-kernel line

```text
u=(-epsilon*p,epsilon,0,0;-1/2,0,kappa,1).         (15)
```

For `z=Cu`, its finite diagonals and a fixed `D23` mode-three minor are

```text
(B01,A23,B23)=
 (2Cp(lambda+1),4 epsilon Cp(lambda-1),4C kappa p(lambda+1)),

det N3[0137]=-epsilon*64 C^3 kappa p^4(lambda+1)^3. (16)
```

At infinity remove the weight factors.  Again the minor is nonzero whenever
the three required diagonals are nonzero.

At `kappa=0`, (4) leaves `h0=r` and the complete common-kernel line is

```text
u0=(-epsilon*p,epsilon,0,0;epsilon*p*r,0,0,1).     (17)
```

The finite diagonals are

```text
(2Cp(lambda+1),
 4 epsilon Cp(lambda-1),
 2 epsilon C(lambda+1)(2pr+epsilon)),              (18)
```

and

```text
det N3[0137]=-32 C^3 p^3(lambda+1)^3(2pr+epsilon). (19)
```

At infinity the same formulas lose their weight factors.  The last factor
in (19) is precisely the extra beta-diagonal factor in (18), so every
genuine point is obstructed.

## Kernel completeness and replay

The verifier does not infer completeness from samples.  It checks each
displayed vector against the full `29 x 8` unwanted matrix and certifies
rank seven by fixed `7 x 7` minors, or rank six by fixed `6 x 6` minors in
the plane case.  Two-minor covers handle the apparent finite-weight
`lambda=0` factors.  Therefore (6), (10), (11), (15), and (17) are the full
kernels on their stated genuine opens.

```text
uv run --with sympy python claims/p5/frontier/verify_p5_component21_q_plus_minus_p_ell_endpoint_complete_obstruction.py
uv run --with sympy python claims/p5/frontier/audit_p5_component21_q_plus_minus_p_ell_endpoint_complete_obstruction.py
```

The primary performs all sixteen saturated eliminations and all symbolic
kernel, rank-minor, diagonal, and ternary-minor checks.  The audit imports no
repository mathematics, reconstructs the permanent models independently,
repeats the exact projections and certificates, and then replays the primary.
