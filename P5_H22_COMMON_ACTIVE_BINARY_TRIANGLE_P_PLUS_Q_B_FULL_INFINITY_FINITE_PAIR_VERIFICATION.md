# Independent verification of the generic `B_full` infinity--finite `H22` pair

## Status and frozen scope

**VERIFIED.**  A fresh verifier reconstructed the chart without importing
either discovery program or the partial-checkpoint helper.  Over
`Q(a,lambda)` on

```text
a(a+1)(2a+1)lambda != 0,
```

the generic `B_full,D01`-infinity obstruction and the stronger pairing with
every finite `D23` slope both replay exactly.

This is a local theorem inside the established diagonal-DVR weighted-`H22`
framework.  It does not cover `a=0,-1,-1/2`, `B_drop`, non-diagonal source
changes, arbitrary-order gluing, or the global Krenn--Gu conjecture.

## Independent reconstruction and common marking

Put

```text
e=X0, L=A-B, M=A+B, C=X3, s0=2a+1,
k0=s0 C-a(a+1)L,

alpha=(k0,e,e,M),
beta=(e+lambda L,(a+1)L+C,aL+C,e).
```

For every marking, the verifier rebuilt `beta_i+h_i alpha_i`, applied

```text
D01^infinity(z,x)=(z0,z2,z3,x),
D23^r(z,x)=(z0,z1,r z2+z3,x),
```

and reconstructed all fourteen mixed permanent rows and both binary
diagonals.  Exact normalized incidence elimination over `Q(a,lambda)` gives

```text
J01^infinity=<h3,a h1+(a+1)h2,h0,h2^2>,
J23^finite  =<h3,h0,h1 h2>.
```

Their common geometric marking is exactly `h=0`: `h2^2=0` implies `h2=0`,
then `a h1=0` implies `h1=0`.  The nilpotent projection direction is scheme
structure, not an additional geometric point.

## Complete `D01`-infinity certificate

At `h=0`, rows `[1,2,3,4,6,9]` and extension columns `[0,1,2,3,4,5]` of
the mixed matrix have determinant

```text
2 a^4(a+1)^3(2a+1).
```

The verifier directly annihilated the full fourteen-row matrix with the two
independent vectors

```text
v0=(-a-1,0,0,1/a; lambda/a,(a+1)/a,1,0),
v1=(0,-1,-1,0;1,0,0,1).
```

The witness and the two-dimensional frame prove this is the complete kernel.
For `z=Xv0+Yv1`, direct substitution gives

```text
A=-2Y(2a+1),
B=-2(2a+1)(X lambda+aY)/a,

det N3[0147]
 =-8Y^2 a lambda(2a+1)^2(X lambda+aY),

det N3[0147]/(A B)=-2Y a^2 lambda.
```

Thus `A B!=0` forces every factor in the ratio to be nonzero, and every
genuine projective extension has marked rank four.  A separate scaling check
confirmed that `A,B` have degree one and the minor degree three in the kernel
vector, so nonvanishing is projectively well defined.  This independently
verifies the `B_full` subclaim in the broader generic-`D01` candidate.

## Finite `D23` exhaustion

For `r!=0`, exact saturation of the fourteen mixed equations by `r` gives,
by bidirectional standard-basis reduction,

```text
<z0,z3,z5,z6,z2-z1,z4+z1,z7+z1>.
```

Hence the complete kernel is generated by

```text
w=(0,-1,-1,0;1,0,0,1).
```

For `z=Cw`,

```text
A=-2C(2a+1),
B=-2C(2a(a+1)r-(2a+1)).
```

The only nonzero diagonal-zero slope is
`r=(2a+1)/(2a(a+1))`; it has `B=0` on the entire kernel and therefore no
genuine binary neighbour.

At `r=0`, the verifier rebuilt rather than specialized the kernel.  The mixed
rank is six with the same fixed witness, and the complete kernel is

```text
u0=(-a-1,0,0,-1/a;lambda/a,(a+1)/a,1,0),
u1=(0,-1,-1,0;1,0,0,1).
```

For `z=Uu0+Vu1`,

```text
A=-2V(2a+1),
B=2(2a+1)(U lambda+aV)/a,

det N3[0147]=8V^2 a lambda(2a+1)^2(U lambda+aV),
det N3[0147]/(A B)=-2V a^2 lambda.
```

Every genuine zero-slope extension also has marked rank four.  The saturated
nonzero chart, its diagonal-zero slope, and the direct zero-slope rebuild
exhaust all finite slopes.

## Verdict and boundaries

- `B_full,D01` infinity at the generic centre: **VERIFIED**.
- The generic `B_full,D01`-infinity plus finite-`D23` pair: **VERIFIED**.
- The broader candidate's `B_drop` claims: **not audited here**.
- Exceptional centres, other source changes, global gluing, and Krenn--Gu:
  **UNKNOWN / outside scope**.
- No finite-field computation, parameter grid, broad minor scan, or discovery
  helper was used.

The verifier encountered no failed algebraic branch or timeout.  The exact
machine-readable run report, including the required role, UTC date, commit,
input/output SHA-256 hashes, method, replay command, and limitations, is
emitted by

```text
uv run --with sympy python \
  audit_p5_h22_common_active_binary_triangle_p_plus_q_b_full_infinity_finite_pair_verifier.py
```
