# Verified obstruction for the generic `B_full` infinity--finite `H22` pair

## Status and frozen scope

**VERIFIED after a separate no-import replay.**  This note treats exactly the last
generic weighted-`H22` pairing left by
`P5_H22_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_BOUNDARY_PARTIAL.md` on the
verified `p+q=0` diagonal-DVR wall:

```text
B_full, D01 at infinity, paired with finite D23.      (1)
```

All algebra is over `K=C(a,lambda)` on

```text
a(a+1)(2a+1)lambda != 0.                              (2)
```

The direct fibres `a=0,-1`, `B_drop`, the half-centre replacement,
non-diagonal source changes, arbitrary-order gluing, and the global
Krenn--Gu conjecture are outside this claim.  The discovery agent retained
`CANDIDATE`; a separate verifier later rebuilt both directions from the
planes and promoted this exact scope.

## The only common geometric marking

The independently replayed projections are

```text
J01^infinity=<h3,a h1+(a+1)h2,h0,h2^2>,
J23^finite  =<h3,h0,h1 h2>.                          (3)
```

At a geometric point, `h2^2=0` gives `h2=0`, and then `a h1=0` gives
`h1=0`.  Thus the common marking is exactly

```text
h=(0,0,0,0).                                         (4)
```

The nilpotent direction in (3) is retained as projection-scheme structure;
it is not silently treated as an additional geometric fibre.

## Complete `D01`-infinity kernel

Put `s0=2a+1` and order the extension coordinates as `z0,...,z7`.  At (4),
the `D01`-infinity mixed matrix has rank six.  Rows

```text
[1,2,3,4,6,9]
```

and columns `[0,1,2,3,4,5]` have determinant

```text
2 a^4 (a+1)^3 s0.                                    (5)
```

A complete kernel basis is

```text
v0=(-a-1,0,0, 1/a; lambda/a,(a+1)/a,1,0),
v1=(0,-1,-1,0; 1,0,0,1).                            (6)
```

For `z=Xv0+Yv1`, the two binary diagonals are

```text
A01=-2Y s0,
B01=-2s0(X lambda+aY)/a.                             (7)
```

Hence genuineness `A01 B01 !=0` forces

```text
Y!=0,       X lambda+aY!=0.                          (8)
```

For marked mode three, the fixed rows `0147` obey

```text
det N3[0147]
  =-8Y^2 a lambda s0^2(X lambda+aY),

det N3[0147]/(A01 B01)=-2Y a^2 lambda.               (9)
```

Every factor on the right of (9) is nonzero on a genuine extension over
(2).  Therefore every genuine `D01`-infinity extension has marked rank four.
In the established local weighted-`H22` framework, the second neighbour
would have to factor this marked map through a three-dimensional retained
target space.  The fixed rank-four minor already obstructs the pair.

## Exact finite-`D23` kernel exhaustion

Although (9) is sufficient, the paired finite direction is rebuilt rather
than assumed to have generic rank seven.

Let `r` be the finite `D23` slope.  After saturating the fourteen mixed
equations by `r`, exact bidirectional ideal reduction gives

```text
<M23(r)z>:r^infinity
 =<z0,z3,z5,z6,z2-z1,z4+z1,z7+z1>.                 (10)
```

Thus for every `r!=0` the complete kernel is the single line generated by

```text
w1=(0,-1,-1,0;1,0,0,1).                             (11)
```

For `z=Cw1`,

```text
A23=-2C s0,
B23=-2C(2a(a+1)r-s0).                               (12)
```

The exceptional nonzero slope

```text
r=s0/(2a(a+1))                                      (13)
```

does not create a new kernel: (10) still applies, while `B23` vanishes on
the whole line.  Hence (13) contains no genuine neighbour.

At the slope omitted by the saturation, `r=0`, the mixed rank drops to six;
the same rows and columns as (5) again have determinant (5).  The complete
kernel is

```text
u0=(-a-1,0,0,-1/a; lambda/a,(a+1)/a,1,0),
u1=(0,-1,-1,0;1,0,0,1).                             (14)
```

The sign of the fourth coordinate of `u0` is negative, unlike the positive
sign in the `D01`-infinity vector (6).  This correction is essential.  For
`z=Uu0+Vu1`,

```text
A23=-2V s0,
B23= 2s0(U lambda+aV)/a,                             (15)
```

and a direct marked check gives

```text
det N3[0147]=8V^2 a lambda s0^2(U lambda+aV),
det N3[0147]/(A23 B23)=-2V a^2 lambda.               (16)
```

Thus the zero-slope rank jump is a genuine two-dimensional kernel, not a
rank-seven specialization, but it also has marked rank four at every genuine
point.  Equations (10)--(16) exhaust all finite projective slopes.

## Conclusion, corrections, and boundaries

The sole generic `B_full` infinity--finite pairing is obstructed already by
the complete `D01`-infinity kernel and fixed minor (9).  The independent
finite-`D23` reconstruction confirms that no exceptional finite slope
escapes through a hidden kernel: the nonzero chart is the line (11), its
diagonal-zero slope is nongenuine, and `r=0` is the explicit rank-six fibre
(14).

- No generic rank-seven assumption is used at `D01` infinity or `D23,r=0`.
- Projection closures are used only to locate (4); both actual kernels are
  reconstructed there.
- No finite field, parameter grid, broad minor scan, or global graph search
  is used.
- The two long symbolic attempts already recorded in the prerequisite note
  remain failures and contribute no evidence here.  This run added no
  timeout or discarded computation.
- This is a generic function-field result only.  Its separate verification is
  recorded in
  `P5_H22_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_B_FULL_INFINITY_FINITE_PAIR_VERIFICATION.md`.

## Discovery run report (pre-verification)

```yaml
role: proof_a
date_utc: 2026-08-01T10:40:15Z
git_commit: a0764e34b14d56ec76471f646755c067e8cb9ff2
claim_label: CANDIDATE
scope: generic B_full weighted-H22 pair D01 at infinity with finite D23
inputs:
  P5_H22_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_BOUNDARY_PARTIAL.md: e2a0e57269890e621e797875af3bbf635a5a4268650fab1749cbfceb48f587ae
  verify_p5_h22_common_active_binary_triangle_p_plus_q_boundary_partial.py: 5f3df560d0db922fc729d73253ff5d331cebb9a337175315d6e5cfec67e8d407
  audit_p5_h22_common_active_binary_triangle_p_plus_q_boundary_partial.py: 059b932257ee63f26aa6abbbb2c80014646aa2038c4f3c2997e7581d9bb29499
method: exact characteristic-zero complete kernels, one fixed marked minor, and r-saturated finite-D23 kernel ideal
command: uv run --with sympy python derive_p5_h22_common_active_binary_triangle_p_plus_q_b_full_infinity_finite_pair_obstruction.py
outputs: candidate note and derivation script, with sha256 values emitted at replay
limitations: generic B_full only; pending independent verification; no a=0,-1, B_drop, non-diagonal, arbitrary-order, or global claim
```

Replay with

```text
uv run --with sympy python \
  derive_p5_h22_common_active_binary_triangle_p_plus_q_b_full_infinity_finite_pair_obstruction.py
```
