# Root-exit Stage 31 P5/H22 forest dry run

Status: **EXACT DRY RUN; NOT YET EXECUTED**

The global Krenn--Gu conjecture remains **UNRESOLVED**.  This stage is a
layout, path, catalog, and narrowly scoped shared-infrastructure transaction.
It changes no theorem, quantifier, scope, lifecycle state, evidence role,
audit-independence claim, or global status.

## Reviewed base and arithmetic

```text
base commit: 6f7c7db486685e62a4971841b560d51f194c795d
root:        887 files + 9 directories = 896 entries
justified:   7 files + 9 directories = 16 entries
debt:        880 grandfathered files, 0 new files
```

The exact transaction contains 351 root sources and projects the live debt
from 880 to 529 while preserving all sixteen justified root entries.  The
frozen historical root universe remains unchanged:

```text
root-universe count:   2363
root-universe SHA-256: 2f4f1af23a89fa3ca56fe2114676c6324385aa1dbd7e5b6ddf35863511edd76c
```

Catalog membership changes from `2226 classified + 137 unclassified` to
`2244 classified + 119 unclassified`.  The eighteen newly classified paths
move from one side of that exact partition to the other; no path is added to
or removed from the frozen universe.

## Exact selection and ownership

The 351 sources are the disjoint union of:

1. 305 pending manifest rows already proposed under `claims/p5/h22/`;
2. all 28 root `derive_p5_h22*.py` helpers; and
3. all 18 unclassified root paths whose basename contains `p5_h22`.

The ordinary 311 members move basename-preserving into their live theorem,
boundary, history, or evidence packages.  Claim-owned derivations and durable
certificates remain with the documents that name and interpret them; they do
not go to generic `tools/explore`, `tools/generate`, or `src` destinations.
The three embedded-P3 helpers remain with `claims/p5/h22/embedded-p3/`, and
the Component19 derivations/certificates follow their exact boundary or
proof-B package rather than a broad Component19 bucket.

Exactly forty basenames containing `_p_plus_q_` move to the narrow neutral
package:

`claims/p5/h22/disputed-ownership/p-plus-q-wall/`

That path asserts only uncontested P5/H22/topic colocation.  Its README
preserves the simultaneous aggregate `VERIFIED`, historical `UNKNOWN`,
discovery `CANDIDATE`, `SUPERSEDED`, and scoped `REFUTED` records and says
explicitly that the disagreement is not adjudicated.  No first/second-
component conflict member is included.  This is an owner-directed Tier-2
layout resolution, not a Tier-3 scientific decision.

## Inverse taper

`derive_p5_h22_common_active_binary_triangle_component_generic_obstruction_candidate.py`
contains both Component20 claim/evidence logic and a neutral exact
four-source weighted-H22 contraction constructor.  It therefore moves as the
primary adapter beside its document, no-import audit, and certificate in:

`claims/p5/h22/common-active-binary-triangle-component-generic/`

The primary's scientific implementation stays local and unchanged apart
from necessary repository/path plumbing.  A byte-equivalent neutral copy of
`WORDS`, the private four-bit/permutation closure, `permanent4`, `project`,
and `build_model` will live in
`src/krenn_gu/p5_weighted_h22_contraction.py`.  Its public API is scoped to
the exact four-source, eight-extension, `D01`/`D23` P5 model; it is not
arbitrary-order infrastructure.

All 39 current direct consumers will import that shared module after standard
bootstrap.  The six audits that share this constructor remain only
downstream-independent at their stated layers; extraction does not upgrade
their independence.  Singular discovery has only one import consumer and
will not be smuggled into the mathematics module.  The Component20 primary
and its no-import audit will not be rerun through Singular merely because
their paths moved.

The separate marked-basis provider also has a broad consumer graph, but it is
part of the later H31/conflict transaction.  Stage 31 preserves those typed
dependency edges without deciding that separate ownership surface.

## Frozen identity

```text
canonical mapping SHA-256:
  0cda48692776290671d971fca1c590cad8462582721fda8de0f5f0308ec4409b
canonical source-identity SHA-256:
  ffb03f9d55d687ad6f9e0afc381cc0f75d32bcdd11cda61ed9ef165668753848
```

The committed schema-v2 batch records every old path, destination, and base
Git blob.  Before its first `git mv`, the executor must verify the reviewed
base is an ancestor of `HEAD` and every base/current source blob matches the
frozen value.

## Repair and validation floor

After a pure 351-file transaction, repair only the necessary imports,
bootstrap calls, repository-relative paths, subprocess commands, hashes,
ledger paths, links, and navigation.  Preserve both ledger entries for the
Branch-B document: one open target and one verified descent-only identity
record.  Preserve Component19 exploratory/refuted asymmetry and every
candidate, partial, superseded, failed, and asymmetric-evidence record.

Validation is bounded to source/path behavior:

- destination blobs equal the 351 frozen source blobs immediately after the
  pure move;
- exact parity tests cover all sixteen shared contraction coefficients,
  finite/infinity `D01`/`D23` projection, and `permanent4` without a solver;
- all 39 shared-core consumers compile/import through bootstrap;
- the link/replay rewriter reaches a fixed point;
- hygiene, migration-tool tests, and the focused fourteen-vertex tests pass
  on an index-complete candidate tree; and
- two fresh Tier-2 referees accept semantic and mechanical closure before
  exact-head hosted CI, guarded merge, and merged-main CI.

No SAT, Singular, brute-force, broad theorem, numerical, or sampling job is
authorized merely by relocation.
