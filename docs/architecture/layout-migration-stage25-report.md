# Layout migration Stage 25 report

Status: **MIGRATION COMPLETE AND VALIDATED ON BRANCH; AWAITING
FINAL REPORT REPIN, EXACT-HEAD CI, AND HEAD-GUARDED MERGE.**

The global Krenn-Gu conjecture remains **UNRESOLVED**. Stage 25 changes
filesystem ownership, replay paths, navigation, and mechanically derived hash
metadata. It does not promote one generic finite `lambda=1` slice to special
component fibres, projective marking charts, the complete pointwise component,
component exhaustiveness, `P5 -> Delta3`, arbitrary-order gluing, or the global
conjecture.

## Exact reviewed transaction

- Merged-main baseline:
  `6bff7b460c3e04ae0538ba38553b5f51bbf4fd7d`.
- Branch:
  `codex/stage25-h22-finite-lambda-one-all-marking-migration`.
- Dry-run approval commit:
  `7c52483e0a47694a8bf3d8a6abc85f732766cd77`.
- Frozen-batch commit:
  `58e5c907d5a97302b4ec04ccdb60190ed5950d87`.
- Pure-move commit:
  `5df6631fe1796459834d1c9aa1de744d190afcb3`.
- Package/path repair and navigation commit:
  `ef0051bada1adaeb93fb7b4472e29d1496c90eb9`.
- Substantive tree:
  `2b2b48aa3ba303854c104e9af769271340160194`.
- Batch ID:
  `p5-h22-finite-lambda-one-all-marking-stage25`.
- Canonical mapping SHA-256:
  `611abb78c553a124a4cf02308950ec5ace6c9f5f1e2e727ece7f043f3b1f59ba`.
- Corrected classifier raw Windows-checkout SHA-256:
  `f4e9dba9f245b9bfe9eea71a24940f71b6d4dc7b6bc5ff23269f8d74a28b75ce`.
- Approval-time manifest raw Windows-checkout SHA-256:
  `25b051239004b967c7bbb00017462b8daad63e55947549e11b66ba70c2d8d1e2`.
- Actual mapping reviewer:
  `Codex (exact mapping reviewer under repository-owner standing delegation
  dated 2026-08-08)`.
- Publication surface:
  [draft pull request #57](https://github.com/YesterdaysLemon/open-graph-theory-with-prize/pull/57).

The two catalog hashes are checkout-specific provenance. The canonical
mapping hash is the portable authority for the exact three old-to-new pairs.
The approved ownership analysis is recorded in
[`p5-h22-common-center-kernel-star-component-finite-lambda-one-all-marking-stage25-dry-run.md`](p5-h22-common-center-kernel-star-component-finite-lambda-one-all-marking-stage25-dry-run.md),
and the executable batch is frozen in
[`catalog/batches/p5-h22-finite-lambda-one-all-marking-stage25.json`](../../catalog/batches/p5-h22-finite-lambda-one-all-marking-stage25.json).

The selected catalog records remained medium-confidence `review_required`
until the frozen transaction. Classifier confidence supplied proposal
evidence, not review authority. Separate proof-topology, status, consumer,
and mechanical reviews established that this was a routine, non-ambiguous,
evidence-backed exact mapping within the owner's standing delegation.

## Moved proof-obligation boundary

The exact theorem/primary/audit family moved flat into
`claims/p5/h22/common-center-kernel-star-component-finite-lambda-one-all-marking/`:

1. `P5_H22_COMMON_CENTER_KERNEL_STAR_COMPONENT_FINITE_LAMBDA_ONE_ALL_MARKING_OBSTRUCTION.md`;
2. `verify_p5_h22_common_center_kernel_star_component_finite_lambda_one_all_marking_obstruction.py`; and
3. `audit_p5_h22_common_center_kernel_star_component_finite_lambda_one_all_marking_obstruction.py`.

The frozen source identities were:

| role | Git blob | raw Windows-checkout SHA-256 |
|---|---|---|
| theorem | `1074a957b3a6d50d137ff3f92f2756f268b873f9` | `0963088f40499e4372a600b7a69eea6182375a439dd8da194475f4a02b65016d` |
| exact-`Q` specialization audit | `e43c41f06c803a1ed9090577788a83cb1fd3558f` | `5763de5079ce4330f95a46248e20f81765325857d8db55eeda7a999321e2b0ef` |
| characteristic-zero primary | `3a3cfb661c15a00bf0618fa48672db6760f4a698` | `299e1f784659b57ca0dd7a79be3842ede51c2dd6cf23c744f52224845b6a98f7` |

The three artifacts are one complete, separable case-coverage leaf:

```text
common-center-kernel-star component 23 at its generic parameter point
  -> coefficient field K = Q(r,t)
  -> finite homogeneous-weight chart with lambda=1
  -> every affine marking (h0,h1,h2,h3)
  -> D01 and D23 binary contractions
  -> their 28 shared mixed coefficient rows
  -> exact bidirectional module equality
       M = <e1,e2,e3,e4,e6,e7,e8>
  -> A01, A23, B01 in M; B23 not in M
  -> every shared mixed kernel has B01=0
  -> genuine weighted H22 requires inherited all-beta support,
       hence B01 and B23 both nonzero
  => contradiction
  => the finite lambda=1 all-affine-marking fibre is empty

other finite weights, special/projective component parameters, wider
source-torus or ambient degenerations, component exhaustiveness,
arbitrary-order gluing, and the global conjecture remain outside this leaf
```

The field `Q(r,t)` makes this a generic-component function-field result. It
does not quantify over special parameter fibres where generic denominators
vanish. Likewise, every affine marking means the four variables on this
generic `lambda=1` slice; it does not mean every projective marking chart.

The primary remains the characteristic-zero proof replay. It constructs the
two binary models, the 28 mixed rows, and four diagonal rows and asks Singular
to check both module inclusions and all four membership results over
`Q(r,t)[h0,h1,h2,h3]`. It retains:

- `status: pass`, field `Q(r,t)`, component 23, and direction
  `finite lambda=1 all markings`;
- label `VERIFIED_EMPTY`;
- mixed module `e1,e2,e3,e4,e6,e7,e8` and bidirectional equality true;
- membership in reported `A01,A23,B01,B23` order
  `[true,true,true,false]`;
- `lambda_one_all_markings_closed: true`;
- `generic_finite_all_markings_closed: false`; and
- finite-field-proof and global-resolution fields false.

The last generic-finite field and the theorem's earlier residual-`UNKNOWN`
prose are chronological and scope-local. A later ordinary-residual theorem
consumes the complete `lambda=0,1,-1` slices and closes the generic finite
case union. Recording that downstream semantic edge in navigation does not
rewrite this leaf's historical output, downgrade the later theorem, or merge
the surrounding component-23 forest into one filesystem package.

The primary reuses three outside providers for model construction,
coefficient serialization, Singular launching, and generic component rows:
the staying candidate-labelled common-active model builder, the staying
partial common-center provider, and the migrated H31 common-center row
provider. Their implementation reuse does not consume a candidate, partial,
or H31 obstruction conclusion as a mathematical premise.

The audit imports no repository module. It reconstructs the component rows,
marking shifts, projections, permanents, coefficient vectors, mixed module,
and diagonal rows over exact `Q` at `(r,t)=(2,4)`. It obtains the same seven
generators and `[true,true,true,false]` membership pattern. This is
no-repository-import exact-`Q` corroboration, not an independent generic
`Q(r,t)` proof: it shares Singular standard-basis and reduction machinery.
Its `audit_only_not_generic_proof`, generic-finite-false, finite-field-false,
and global-false boundaries remain explicit.

No outside Python module imports the selected primary or audit. Neither has
a subprocess or hash consumer, curated theorem-ledger entry, or formal
counterpart. The later ordinary-residual theorem is a semantic downstream
consumer only.

## Preserved conflicts and exclusions

The leaf's chronological residual wording and generic-finite-false field were
preserved rather than silently rewritten into a current whole-family
synthesis. The separately proved later case union remains untouched. The old
partial theorem, dense-open supplement, `lambda=0` sibling, ordinary
branches, and special/projective/source-torus boundary descendants remain at
their existing paths and statuses.

Candidate-labelled or partial provider files do not become verified theorem
evidence through import or proximity. The audit point `(2,4)` does not
replace the generic field, and no finite scan or specialization is promoted
to exhaustive function-field coverage.

Stage 25 also does not consume or adjudicate the separate root H31
chart-boundary marked-fibre family whose theorem says fourteen certificate
strata while its primary constructs, asserts, and reports sixteen. That
claimed-proof/verifier contradiction remains owner-gated and outside this
transaction.

This package does not close special or projective component fibres, the
wider source-torus or ambient degenerations, component exhaustiveness,
`P5 -> Delta3`, arbitrary-order/local-to-global gluing, the prize graph, or
the global conjecture.

## Pure move and mechanical repair

Against its direct parent, commit `5df6631f` contains exactly three `R100`
moves plus the corresponding manifest transaction. Scientific bytes are
identical across the move. The manifest changes only the three selected
records from `review_required` to `moved`, records the frozen batch, and
updates deterministic summary fields:

| measure | before | after |
|---|---:|---:|
| total classified records | 2,015 | 2,015 |
| manifest `moved` | 405 | 408 |
| manifest `proposed_high_confidence` | 242 | 242 |
| manifest `review_required` | 1,368 | 1,365 |
| unclassified | 348 | 348 |
| moved-only manifest root projection | 1,967 | 1,964 |
| high-confidence manifest root projection | 1,725 | 1,722 |
| all-classified manifest root projection | 357 | 357 |
| classifier inherited all-migration estimate | 356 | 356 |
| measured root files | 1,959 | 1,956 |
| measured root directories | 9 | 9 |
| measured root entries | 1,968 | 1,965 |
| allowlisted root files | 7 | 7 |
| grandfathered root debt | 1,952 | 1,949 |
| new root debt | 0 | 0 |
| enforceable retired/provenance paths | 405 | 408 |
| actual tracked files under `claims/` | 409 | 412 |
| H22 package directories | 18 | 19 |
| H31 package directories | 31 | 31 |

The frozen root baseline remains 2,363 files plus 3 directories, the exact
end-state allowlist is unchanged, and the classifier estimate 356 remains a
separate pre-existing metric from the manifest projection 357.

Commit `ef0051ba` repairs exactly eight files: the moved theorem, primary,
and audit; root README; the P5 and H22 navigation READMEs; the theorem ledger;
and the historical next-instance handoff. No mathematical equation, module,
assertion, timeout, JSON field, or status changes.

The primary installs shared bootstrap before repository imports, derives
`REPO_ROOT` from `bootstrap(__file__)`, exposes the migrated H31 provider
package, and delays all three repository-provider imports until after path
setup. The audit remains free of repository imports. Its portability repair
adds a local launcher selecting native `Singular -q`, then
`wsl.exe --exec /usr/bin/Singular -q`, and otherwise failing clearly. The
primary's 120-second and audit's 90-second timeouts remain unchanged.

The deterministic rewriter changes exactly one Markdown link and four fenced
commands across two files: the moved theorem and
`docs/NEXT_INSTANCE_HANDOFF_2026-07-31.md`. It has zero ambiguity, and its
second pass is a `0/0/0` fixed point. A manual ordinary Markdown link repairs
the moved theorem's dense-open-supplement reference. The final normalized-LF
SHA-256 values are:

| repaired file | normalized-LF SHA-256 |
|---|---|
| moved theorem | `bb666b91513a8694a5b12a10b28b9b58b9162a8f400056be231e748f2e90555e` |
| historical handoff | `1ffca39ec9bf0dc305feef7ec4b1604fe7e0db2b90bcac401996c66fc637ff2b` |
| root README | `e088b31ea75020ac527e053d684400723076723e8aba6339b1ff8b8476733db0` |
| theorem ledger | `d80c4c2fd20e73fa6b0ac52e70bba23664fbafc3d3bd0495c0d6ea1d91e6e39b` |

Root README gains exactly the direct `lambda=0` and moved `lambda=1` evidence
links inside its already stated generic finite case union. This is a
provenance-completeness repair, not a new closure claim. The ledger remains
at 86 entries and gains no selected-theorem entry. Exactly three existing
root-README-backed `document_sha256_16` values become
`e088b31ea75020ac`, while their statuses remain `open`,
`verified_generic`, and `partial`.

Navigation records 19 H22 and 31 H31 package directories, labels this as the
scoped generic-parameter `lambda=1` all-affine-marking leaf, retains the
generic common-center H22 core at root, and states the audit limitation. No
package README was invented for the three-file leaf.

The post-repair file identities of the moved package are:

| artifact | raw Windows-checkout SHA-256 | Git blob |
|---|---|---|
| theorem | `1451a9e458a7b7cafad3c9ebd5db93c061f2d6e0e71f7722f25087e8cb3eecd2` | `5f01947d91a4ac51f4ae219e244bf3b1e789dec8` |
| primary | `b9a04dc6cda1dd1f79e3eb3836faa67524ac5766105170dbf8be856ca8e31267` | `66ed11bae5fc2ccf15c778b33d1d165c12acf03c` |
| audit | `2e59bde78e2e0c8ecba5138f4bf5acae3f221f8731213d8112f06a4d86d60c4d` | `940f29d1864fb0a974467079e4c813d02aa4c108` |

## Scientific replay matrix

At clean pre-move baseline `6bff7b46`, the selected primary and audit each
ran once, strictly serially, through
`uv run --quiet --python 3.13 --with sympy python`. The primary passed in
21.253 seconds with stdout SHA-256
`d822509c388a4f761a1a79b6eed1cc8d2b77e03e8ddca56eb5293e057b4d8c33`;
the audit passed in 11.350 seconds with stdout SHA-256
`a9a9413b5fecec324d73dc73197652e9f23f4fe900a5eda3fc14fc0972ba3669`.
Both returned rc=0, empty stderr, and one valid JSON object. These frozen-byte
baseline results are preserved outside the repository at
`C:\Users\Yeste\.codex\run-artifacts\stage25-20260809T142217Z`.

The complete affected post-move scientific closure is two unique
executables and four invocations. Every invocation ran exactly once,
strictly serially, through the same `uv` command. No `python-sat` dependency,
retry, priority change, process intervention, or automatic rerun was used.

| rows | executable | root s | foreign s | byte-exact stdout SHA-256 |
|---|---|---:|---:|---|
| 1--2 | moved characteristic-zero primary | 9.068102 | 7.053790 | `eaf8f3ff6fe816d4f6600f5475e1831c054e39b66c622a12c0327f325a154de6` |
| 3--4 | moved exact-`Q` specialization audit | 6.057483 | 6.054147 | `dcd4d8922a0a39c4af1a1a84a3626afc1821bb4b67d53dc8d547a02e651e0f6c` |

All four returned rc=0 with empty stderr and exactly one JSON object. The
root and foreign bytes and parsed objects matched for each executable. The
primary retained the exact `Q(r,t)` scope, component 23, seven-generator
module, bidirectional equality, `[true,true,true,false]`, `VERIFIED_EMPTY`,
lambda-one closure true, chronological generic-finite false, and
finite-field/global false. The audit retained exact `Q` at `(2,4)`, no
repository imports, the same module and membership, audit-only true, and
generic-finite/finite-field/global false.

Both executables remained stdout-only. The two scientific foreign
directories stayed empty, the list of ignored JSON files remained exactly
the same four pre-existing files, and neither tracked nor index state
drifted. Frozen source blobs, the canonical mapping, all projected repair
pins, all three unchanged provider blobs, ledger invariants, and HEAD/tree
pins matched.

Every first stdout, stderr, rc, timing, argument vector, and metadata record
is preserved outside the repository at
`C:\Users\Yeste\.codex\run-artifacts\stage25-acceptance-ef0051b-20260809T152855Z`.

## Isolated import probes and offline tooling record

The import-only probes were separate from the four scientific invocations.
Each used the preserved wrapper from its own fresh foreign directory and ran
exactly once:

| row | moved module | elapsed s | result |
|---|---|---:|---|
| 5 | primary | 2.028936 | rc=0, empty stderr, `IMPORT_OK:stage25_primary`, empty foreign directory |
| 6 | audit | 2.020038 | rc=0, empty stderr, `IMPORT_OK:stage25_audit`, empty foreign directory |

The invocation collector preserved all six first-run rows and, as instructed,
stopped before the final offline assertions. Its provisional `BLOCK` denoted
unfinished assigned aggregation, not a failed verifier, audit, import, or
solver run. Root completed the offline closure over those immutable
artifacts, and a fresh offline referee separately audited the preserved
artifacts without rerunning scientific code. Both final offline reviews
passed.

During the read-only aggregation, one unquoted `HEAD^{tree}` expression was
misparsed by PowerShell. The corrected source/tree calculation passed. This
offline shell-syntax incident launched no Python, Singular, verifier, audit,
or import process, changed no file, and is not a scientific invocation or
theorem evidence.

The fresh offline audit confirmed byte equality and the two stdout hashes,
all exact JSON schemas and scope boundaries, six rc=0/empty-stderr records,
empty foreign/probe directories, unchanged ignored JSON ownership, exact
HEAD `ef0051ba` and tree `2b2b48aa`, clean tracked/index state, canonical
mapping recomputation, frozen source and provider blobs, all projected file
identities, and the 86-entry ledger with only the approved three README hash
changes.

## Validation and publication boundary

At substantive head `ef0051bada1adaeb93fb7b4472e29d1496c90eb9` and tree
`2b2b48aa3ba303854c104e9af769271340160194`, the complete pre-report local
index floor passed:

- `check_hygiene.py` in 28.3 seconds: all 1,698 Python files compile, all 824
  pre-report Markdown files have resolving local links, all 86 ledger hashes
  match, root is 1,956 files plus 9 directories = 1,965 entries, root debt is
  `1,949 grandfathered / 0 new`, all 408 retired-path/provenance records pass,
  manifest counts are `408 moved / 242 proposed / 1,365 review_required`, and
  all five fast verifiers pass;
- all 152 migration-tool tests in 12.518 seconds;
- all 14 fourteen-vertex cycle-cover lattice tests in 0.003 seconds;
- deterministic rewriter output exactly
  `links=0 / replay=0 / files=0 / ambiguity=[] / ledger=0`;
- targeted Ruff `E402,I001` and compile-from-source checks for both moved
  scripts; and
- clean worktree, index, HEAD, and tree checks after the replay matrix.

Fresh substantive semantic/status and mechanical/provenance referees both
passed the exact tree. The mechanical referee confirmed the eight-file
repair surface, first deterministic rewrite of one link and four commands in
two files followed by the zero fixed point, all projected hashes, the
86-entry/three-hash ledger boundary, 19/31 H22/H31 directory counts, all 824
links, all 408 provenance records, and no stale, unused, or line-ending
issue.

Pre-report pull-request workflow
[`hygiene` run 31322008920](https://github.com/YesterdaysLemon/open-graph-theory-with-prize/actions/runs/31322008920)
succeeded on the `pull_request` event at the exact substantive head. Job
`93266349400` concluded success, and the workflow completed in 33 seconds.

Adding this report raises the Markdown count from 824 to 825. On exact index
tree `77577fdc48f3456afd9a183ff3d9ff3e9b2a5c9d` with report blob
`a4510238043f42eaa0021065aa3dbc719d9fbf4e`, the complete report-inclusive
floor passed: 1,698 Python files, 825 Markdown files, 86/86 ledger hashes,
`1,949 grandfathered / 0 new` root debt, 408 provenance records, all 152
migration-tool tests, all 14 lattice tests, rewriter `0/0/0`, and a clean
index-complete candidate.

Two fresh read-only final referees then returned PASS on that exact candidate.
The semantic/status referee audited the full scientific diff, scope,
chronology, provider roles, preserved conflict, collector attribution, and
global boundary. The mechanical/provenance/bypass referee independently
reproduced the commit, mapping, R100, repair, hash, ledger, arithmetic, and CI
transaction facts; reconstructed the artifact equality/schema surface; and
audited the preserved replay, floor, and prior-referee evidence. Neither
referee edited or replayed anything.

This final record necessarily changes only the report blob and index tree.
The superseding append-only candidate must therefore be repinned and pass the
same report-inclusive floor plus narrow semantic and mechanical delta review.
Those superseding verdicts belong in the pull-request trail, not in another
self-referential report edit. Final exact-head pull-request CI and the normal
head-guarded merge remain publication gates.

## Stop boundary

Stage 25 stops at the complete finite `lambda=1` all-affine-marking fibre
over `Q(r,t)` at the generic common-center-kernel-star component point. It
does not move or adjudicate the surrounding component-23 proof forest,
change the later generic case-union theorem, cover special or projective
component fibres, resolve wider source-torus or ambient degenerations, prove
component exhaustiveness, establish `P5 -> Delta3`, supply arbitrary-order
local-to-global gluing, or solve the prize conjecture.

The global Krenn-Gu conjecture remains **UNRESOLVED**.
