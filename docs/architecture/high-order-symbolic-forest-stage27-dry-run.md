# Stage 27 dry run: high-order symbolic forest

Status: **EXACT PRE-EXECUTION REVIEW; NO STAGE 27 FILE MOVE HAS OCCURRED.**

The global Krenn-Gu conjecture remains **UNRESOLVED**. This dry run freezes
filesystem ownership, executable repair obligations, and two ledger mapping
corrections for already-documented evidence carriers. It does not change an
underlying theorem, quantifier, support condition, field, lifecycle, evidence
role, case cover, formalization state, or global status.

This stage is the first tranche of the independently reviewed
[Stages 27-29 program](layout-migration-stages27-29-reviewed-plan.md).

## Frozen base and proposed batch

```text
base commit: 7c332c7a307542395add3420ed62b47ebd72287c
base tree:   a6ee0c3ada3a7863d36763c5082fec58f62073da
batch id:    high-order-symbolic-forest-stage27
members:     587
```

The only repository delta from the original reviewed base `d618936...` is
the merged program-plan document. All 587 source Git blobs and all 587
old/new mappings are identical across the two bases.

The run-local freeze artifact was independently reconstructed twice and has
file SHA-256:

```text
5dab5a54366709d7d4adcc391134b21db9401a733e70fae5cbd352593fb47ca3
```

The durable batch file will be committed separately before execution. At
that point its `moves` array, not a predicate or classifier family name, is
the executable authority.

## Exact mapping identity

Canonical mapping SHA-256:

```text
cc379f4cf74ebb7c8ffbf428e8a68acca6f18703e3f5e95d70183f2f5887f7b0
```

Canonical source identity SHA-256:

```text
b644cf42195fe50215f33bc5dbf39917bc316cea3e3d01c9b4c443cb710801aa
```

The source digest uses dictionaries of the exact form
`{"old_path": old, "new_path": new, "git_blob": index_oid}`, sorted by
`(old_path, new_path)` and serialized with sorted keys and compact JSON
separators. It pins source identity without depending on working-tree line
endings.

Destination totals are:

| destination | members |
|---|---:|
| `claims/arbitrary-order/` | 352 |
| `claims/p7/` | 183 |
| `claims/p6/` | 39 |
| `tools/generate/` | 10 |
| `tools/explore/` | 3 |

The 587 sources are unique tracked root files, the 587 destinations are
unique, and there are no destination collisions or source/destination cycles.

## Reviewed membership

The exact set comprises:

- 522 files in the closed arbitrary-order/P6/P7 core;
- 33 files in eleven additional theorem/primary/audit packages;
- 19 asymmetric-evidence leaf files whose one-replay or no-replay shape is
  preserved exactly;
- 13 bounded non-claim tools at their existing classifier destinations.

The core contains every current P6 and P7 mapping, 92 complete
arbitrary-order triples, the complete 42-file `ROOT_M7` ownership subtree,
three supplemental triples, and the Two-port, Odd-residual-port, and
One-nonblocker-surplus antecedent triples.

"Complete" here means ownership/evidence closure only. In particular,
`ROOT_M7` excludes 908 relative-shell supports rather than all 4,598,126
four-subsets, and arbitrary P7/local-to-global remains **UNKNOWN**.

## Durable classifier refinement

The frozen mapping exposed exactly 43 catalog deltas:

- 41 previously unclassified paths: three antecedent documents and 38 named
  primary/audit/replay carriers;
- two `ROOT_M7` primaries misclassified as shared libraries under
  `src/krenn_gu/`.

The 43 reviewed tuples use `arbitrary-order` ownership and
`claims/arbitrary-order/<basename>`. The three documents are medium-confidence
`claim_document` records. All 40 Python carriers are high-confidence
`claim_script` records. Confidence remains proposal metadata, not approval or
scientific status.

Portable canonical tuple digest (ordinal case-sensitive order, UTF-8, LF
between records and one trailing LF):

Each record is exactly
`old_path|proposed_path|category|claim_family|confidence`, with an empty field
for null, and records are sorted by `old_path` in ordinal case-sensitive
order.

```text
09c4dcdf94f3ee39364016f47902a2eb4785f62a4a9d3bc41780c440822bed44
```

Catalog counts change as follows:

| field | before | after |
|---|---:|---:|
| classified | 2,015 | 2,056 |
| unclassified | 348 | 307 |
| arbitrary-order family | 323 | 366 |
| claim documents | 679 | 682 |
| claim scripts | 1,073 | 1,113 |
| shared libraries | 35 | 33 |
| high confidence | 410 | 450 |
| medium confidence | 1,179 | 1,180 |

After refinement and deterministic manifest regeneration:

```text
layout-classification SHA-256:
67e0f278e8213be6f6c66cb9e51746a57f179bb833d3f7cd7869a0d19f6dbcf9

moved-paths manifest SHA-256:
17d477c63ebc0aef8b420913df767be9b2958eb160598eebefef3e89b3745a7c

unclassified-files SHA-256:
e8aa8362aefc13d2b31756c7734ae347fd1399c973d23a313eda582e14473100
```

The manifest reports 2,056 records, 411 already moved, 282
`proposed_high_confidence`, 1,363 `review_required`, 307 unclassified, and
zero collisions, double moves, or cycles. Every Stage 27 mapping agrees
exactly with the manifest; 230 selected records are high-confidence proposals
and 357 require review. The committed exact batch will approve both classes
under the standing delegation.

## Proof and evidence boundaries

The proof-forest review accepted every selected owner and rejected every
conflict-bearing alternative. This stage does not consume or adjudicate:

1. the H31 marked-fibre theorem/primary 14-versus-16 contradiction;
2. the P4 internal-`E=0` versus chart-`D=0,a!=0` attribution ambiguity;
3. the first/second-component provenance and closure disagreement across
   marked-basis, toric, high-coordinate, outer-boundary, README, and synthesis
   artifacts;
4. the weighted-H22 `p+q=0` status ambiguity.

Component20 and its two P4 proof-B dependencies, Component21/23,
embedded-P3, H31 common-center, Branch B, high-coordinate, and all legacy
routing also remain outside this batch.

The selected structural reductions retain their stated walls. Among them:

- Hessian unmixing remains conditional on legal response-jet exposure;
- Two-port leaves synchronization of three overlapping P5 systems open;
- exact-three-blocker and fourth-order subrank are structural/tight-endpoint
  claims whose finite-field audits are corroborating layers;
- partial and correction documents retain those lifecycle/scope labels;
- bounded tools remain generators/analyzers/solvers rather than theorem
  evidence.

No exact counterexample was found. Nothing in the selected forest resolves
the global conjecture.

## Executable and provenance repair freeze

The exact rebased repair inventory has SHA-256:

```text
769ffecc5308d457d447f8369f8085491bd2eb732ed643aa6855be5305b1c450
```

The final consumer graph has no staying-to-selected Python import and no
selected internal cross-destination import. Five selected-to-staying imports
come from two leaf verifiers:

- `verify_integer_signed_lattice_transport.py` imports four root helpers;
- `verify_odd_full_factor_one_term_mechanism.py` imports
  `explore_random_even_cycle_forks.py`.

Both receive the shared guarded bootstrap and retain those exact root imports.
No helper extraction is needed.

### Selected external paths

Four selected verifiers receive `REPO_ROOT` paths while retaining `HERE` for
co-moving siblings:

- `verify_arbitrary_hafnian_jet_line_projective_affine_separation_and_p7_ghz_boundary.py`
  -> `claims/p7/verify_p7_full_mixed_root_219_label_sensor_and_pinned_star_gating_boundary.py`;
- `verify_arbitrary_surplus_common_row_full_span_obstruction.py`
  -> `claims/p6/P6_SIMULTANEOUS_KERNEL_AND_NATURAL_LIFT_OBSTRUCTIONS.md`;
- `verify_six_blocker_arbitrary_cofactor_projectively_constant_row_obstruction.py`
  -> `claims/p6/P6_PROJECTIVELY_CONSTANT_SOURCE_ROW_OBSTRUCTION.md`;
- `verify_six_blocker_maximal_overlap_port_swap_collapse.py`
  -> `claims/p6/P6_COMMON_PORT_111_FROBENIUS_REDUCTION.md`.

### Selected package-local theorem paths

A folded-AST review found these 18 co-moving scripts constructing a bare
`Path(<theorem basename>)`. Each becomes package-local through `HERE` or
`Path(__file__).resolve().with_name(...)`:

```text
audit_arbitrary_order_degree_six_kotzig_port_obstruction.py
audit_five_regular_balanced_bridge_diagonal_backbone.py
audit_hafnian_convolution_split.py
audit_three_colour_diagonal_matching_balance.py
verify_adjacent_port_determinant_transport_lemma.py
verify_arbitrary_order_degree_six_kotzig_port_obstruction.py
verify_exact_three_blocker_permanent_rank.py
verify_five_regular_balanced_bridge_diagonal_backbone.py
verify_five_root_no_torus_codimension_two.py
verify_five_root_torus_resultant_dichotomy.py
verify_four_blocker_ideal_obstruction.py
verify_four_regular_balanced_bridge_obstruction.py
verify_hafnian_convolution_split.py
verify_multi_star_blocker_factorisation.py
verify_partial_minimal_singleton_circuit_dichotomy.py
verify_reciprocal_port_orientation.py
verify_three_colour_blocker_union.py
verify_three_colour_diagonal_matching_balance.py
```

This path surface was absent from the first external-edge scan. It changes
the repair inventory, not the frozen mapping or the mathematical forest.

### Staying consumers

The exact staying executable/navigation repairs are:

- four `FAST_VERIFIERS` paths in `check_hygiene.py`;
- `NORMAL_PATH` in the P5 pair-signature verifier and audit;
- one `Containerfile` invocation example;
- six finite CLI defaults reading
  `THREE_COLOUR_DIAGONAL_MATCHING_BALANCE_THEOREM.md`;
- four subprocess argv sites for the binomial-support augmenter: one in the
  mandatory-unit caller and three adjacent-string sites in the symmetry
  caller.

The six finite files are:

```text
analyze_ten_vertex_degree_six_kotzig_port_survivors.py
analyze_ten_vertex_permuted_potential_survivors.py
audit_ten_vertex_degree_six_kotzig_ports.py
audit_ten_vertex_permuted_potential_survivors.py
explore_ten_vertex_degree_six_kotzig_ports.py
scout_twelve_vertex_six_potential_cells.py
```

The folded inventory contains 16 actionable staying Python literal sites in
eleven files, plus the `Containerfile` example.

### Ledger and link provenance

Ledger entries 11-17 receive their exact moved document/evidence paths.
Entries 14 and 17 also correct stale `none_exists` mappings already
contradicted by their owning documents:

- Exact Three Blocker maps its SymPy primary and distinct F5 audit;
- Support-four maps its existing primary and distinct F5/F7 audit.

Both use `independent_modular_audit`. The Exact Three audit independently
rechecks the slice-space and polarization calculation and reads the primary
artifact only for rank agreement. The Support-four audit reconstructs the
coefficient calculation from the missing-coordinate rule using the displayed
integer maps. These are scoped modular corroboration layers, not independent
witness selection or second characteristic-zero proofs. Both primaries use
`script_is_the_verifier`; both audits use `independent_modular_audit`. Status,
theorem scope, and dependencies do not change.

Fixed-point link rewriting will also refresh the document hashes of directly
moved ledger entries 11-17 and hash-only entries 0, 65, 71, and 83. Historical
source hashes in `docs/research-notes.md` remain historical and are not
repinned.

The original `d618936` review-oracle simulation found 303 Markdown links and
565 replay commands across 203 files, zero ambiguity, and a zero-change second
pass. The actual `7c332c7` post-move tree must reproduce a zero-ambiguity fixed
point; its observed counts will be recorded rather than assumed.

## Execution and acceptance contract

The next commit will contain an exact batch generated from this refined
manifest with:

```text
approved_by: Codex (exact mapping reviewer under repository-owner standing
             delegation dated 2026-08-08)
base_sha:    7c332c7a307542395add3420ed62b47ebd72287c
members:     587
mapping:     cc379f4cf74ebb7c8ffbf428e8a68acca6f18703e3f5e95d70183f2f5887f7b0
```

Before repair, the staged move-only tree must show exactly 587 R100 renames,
zero additions/deletions, `0 0` numstat for every pair, and destination index
blobs equal to the frozen `git_blob` records.

After only the frozen repairs:

1. parse every changed Python file in a fresh no-bytecode process;
2. import the two outbound-import leaves and four external-path verifiers
   without calling `main()` or the P7 loader;
3. assert all 18 same-package document paths, four external targets, 16
   staying Python targets, and the `Containerfile` example resolve from their
   intended invocation contracts;
4. assert ledger entries 14 and 17 carry the reviewed primary/audit paths and
   provenances and cannot regress to `none_exists` while those carriers remain
   tracked;
5. run the link rewriter to a zero-ambiguity fixed point;
6. recompute directly moved and hash-only ledger hashes from staged Git blobs;
7. run the authoritative index-complete floor:

```text
git add -A
python check_hygiene.py
python -m unittest -v tests.test_migration_tools
python -m unittest -v test_fourteen_vertex_cycle_cover_lattice.py
python tools/migration/rewrite_links.py
git diff --exit-code
```

`check_hygiene.py` is the single execution of the four `FAST_VERIFIERS`; the
focused probes must not duplicate those theorem replays.

No SAT, Singular, brute-force, or broad theorem rerun is justified by these
path-only changes. A wrapper, compiler, bootstrap, or optional-tool failure is
infrastructure evidence, not authority to rerun expensive science.

Execution remains blocked until the exact batch is committed, independently
reviewed against this dry run, and accepted by the executor's own dry run.
