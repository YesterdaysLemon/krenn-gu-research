# P5 H31 canonical component-chart boundary - Stage 22 exact migration dry run

Status: **APPROVED FOR AN EXACT FROZEN BATCH under the repository-owner
standing delegation dated 2026-08-08. No move has yet been executed.**

> **Scientific status will not change.** The global Krenn-Gu conjecture
> remains **UNRESOLVED**. This review resolves filesystem ownership only. It
> does not promote a canonical marked-row obstruction to a complete
> marked-basis fibre, a whole-component theorem, component exhaustiveness,
> `P5 -> Delta3`, or a global result.

## Review authority and audited baseline

- Exact merged baseline:
  `2040f9ff6ce077908eae83d3ce40e7cb8254fd39`.
- Branch: `codex/stage22-h31-component-chart-boundary-migration`.
- Actual mapping reviewer:
  `Codex (exact mapping reviewer under repository-owner standing delegation
  dated 2026-08-08)`.
- Delegated-review test: routine, non-ambiguous, evidence-backed exact layout
  mapping; no scientific status decision, ambiguous proof-boundary decision,
  or owner-preference architecture choice is required.
- Batch ID to freeze: `p5-h31-component-chart-boundary-stage22`.
- Approval-time raw Windows-checkout manifest SHA-256:
  `bd9b23bc2dbe624f646c27d665394917ec6afc1ac8ad0c71eb9e92ad0c667d91`.
- Canonical mapping SHA-256:
  `7130acd031ab499906c6c463298292de459ce7a60eac566a35986d40d3763837`.

The manifest hash is informational approval-time provenance over raw CRLF
checkout bytes. The canonical mapping hash is the portable authority for the
reviewed old-to-new pairs.

All three manifest records are `review_required` with medium classifier
confidence. Classifier confidence is proposal evidence only. Approval comes
from independent proof-topology/status and mechanical/consumer audits and
applies only to the exact mapping below.

## Exact three-file mapping

All three files move flat into a new
`claims/p5/h31/component-chart-boundary/` package.

| role | source | destination |
|---|---|---|
| theorem | `P5_H31_COMPONENT_CHART_BOUNDARY_OBSTRUCTION.md` | `claims/p5/h31/component-chart-boundary/P5_H31_COMPONENT_CHART_BOUNDARY_OBSTRUCTION.md` |
| characteristic-zero primary | `verify_p5_h31_component_chart_boundary.py` | `claims/p5/h31/component-chart-boundary/verify_p5_h31_component_chart_boundary.py` |
| modular audit | `audit_p5_h31_component_chart_boundary.py` | `claims/p5/h31/component-chart-boundary/audit_p5_h31_component_chart_boundary.py` |

Every source is tracked grandfathered root debt, every destination is absent,
and the durable classifier and generated manifest contain exactly these
source-to-destination pairs. There are no selected duplicate sources or
destinations, case-folded destination collisions, double moves, overlap
cycles, or package-name collisions.

The exact baseline source identities are:

| source | Git blob | raw Windows-checkout SHA-256 |
|---|---|---|
| theorem | `c566ed523e4ac21ad5a39decc2977744a869a0cf` | `e4ccabec543b11ac0902201157b6a93b76572059bc2df3ae9d14ecc9874b1753` |
| audit | `6b9ab5daca4cbaf19af7e33992fdac8ccd571a66` | `86d676e9c5fc5ef72761d2788f47461677dd82a4ac89b58db45905c9b91a1817` |
| primary | `0802a24dcb5c4f3473f1d82c4fc4cd74a8db1367` | `478abb4b785f4c764d39844232c640e7dcde1c9a2c81f8464291aaae65d64e13` |

## Proof-obligation ownership and exact scope

The selected triple is one complete, separable canonical-section proof leaf:

```text
P4 nonzero all-rank-two divisor in the preferred first-component chart
  -> parameters A,H,N nonzero and R arbitrary
  -> displayed canonical marked-row normal form
  -> four distinguished-source orientations q=0,1,2,3
  -> exact mixed-kernel ranks and transverse marked determinants
  => no canonical marked section on this divisor lifts to H31

arbitrary kernel-row shifts, complete marked-basis fibre, first-plane
Schubert boundary, additional components, H22, P5 -> Delta3, gluing, global
remain outside this leaf
```

The mandatory scope description is:

> The selected theorem is an exact characteristic-zero obstruction for the
> displayed canonical marked-row normal form on the nonzero all-rank-two
> preferred-chart divisor of the first known pure rank-two component, with
> `A,H,N != 0`, `R` arbitrary, and all four distinguished-source
> orientations. It excludes only those canonical marked sections.

It is not a generic theorem, a complete marked-fibre theorem, a
whole-component theorem, or component-closure evidence.

The later
`P5_H31_COMPONENT_CHART_BOUNDARY_MARKED_FIBRE_OBSTRUCTION.md` is a distinct,
strict strengthening: it adds arbitrary kernel-row shifts and excludes the
complete marked-basis fibre on the same divisor. It consumes the canonical
normal form but rederives its broader algebra. The canonical theorem remains a
live, valid narrower checkpoint; it is neither withdrawn nor superseded.

The successor's theorem, primary, audit, and uniquely consumed
`derive_p5_h31_chart_boundary_marked_fibre_elimination.py` remain root-owned
for this stage and should move together in a later four-file batch after the
generator is reclassified from the prefix-only `tools/explore` proposal into
that claim package. No generator is owned by the selected canonical triple.

Upstream P4 chart closure and the root rank-two orbit theorem stay separately
owned. The marked-basis open branch explains why row shifts are additional
data. High-coordinate references are frontier/provenance edges, not scope
inheritance.

One pre-existing cross-artifact scope conflict is explicit. The P4
toric-boundary theorem attributes its internal `E=0` canonical section to the
selected `P5_H31_COMPONENT_CHART_BOUNDARY_OBSTRUCTION.md`, while the P4 chart
closure identifies the selected divisor as `D=0, a!=0`, and the later full
marked-fibre successor treats internal `E=0` as a separate divisor. Stage 22
only preserves and retargets that P4 link; it neither endorses nor adjudicates
the mismatched attribution. Replaying the P4 primary can validate current
hashes and its executable calculation, not that scientific prose edge. A
focused owner-gated status/provenance correction remains separate.

No selected artifact has a curated theorem-ledger entry or recorded formal
counterpart. Migration adds no entry and changes no mathematical status,
assumption, quantifier, scope, evidence role, lifecycle, or global-status
field.

## Evidence roles and exclusions

The primary reconstructs the permanent coefficients, symbolic mixed kernels,
all rank strata, and the displayed marked determinants with exact SymPy
arithmetic. It is the characteristic-zero proof replay.

The audit imports neither the primary nor any repository helper. It separately
uses a dynamic-programming permanent, modular row reduction, and exhaustive
projective-extension enumeration over `F_5` and `F_7`. It is independent of
the primary implementation downstream of the shared theorem-specified normal
form, but it is modular QA only, not the characteristic-zero proof.

The following remain separately owned:

- arbitrary kernel-row shifts and the later complete marked-fibre successor;
- the first-plane infinity/Schubert boundary, internal `E=0`, toric marked
  fibres, and marked-basis open/classification packages;
- diagonal-quadric and any additional component;
- weighted `H22`, component exhaustiveness, `P5 -> Delta3`, arbitrary-order
  gluing, and the global conjecture.

The selected leaf neither consumes nor adjudicates the pre-existing broader
first/second-component status-provenance conflict. False whole-H31, P5, and
global output fields remain scope-local to this verifier and do not downgrade
separately established results.

## Mechanical repair surface

Both moved scripts currently derive `ROOT` from their root-level parent. After
the pure move:

- both install shared `krenn_gu.bootstrap` before repository use;
- `REPO_ROOT, HERE = bootstrap(__file__)` supplies stable ownership;
- the selected theorem and the audit's sibling primary resolve from `HERE`;
- P4 chart and root rank-two orbit dependencies resolve through `REPO_ROOT`;
  and
- ignored JSON outputs continue to write under repository `tmp/`, not a
  package-local directory.

Exactly four staying primaries need operational path retargets:

1. P4 toric-boundary primary's `CHART_BOUNDARY`;
2. complete chart-boundary marked-fibre primary's `CANONICAL`;
3. canonical first-plane-infinity primary's `PREVIOUS`; and
4. high-coordinate primary's dependency tuple.

No staying module importer or subprocess caller targets either selected
script name.

The deterministic virtual post-move rewriter predicts exactly eight
Markdown-link rewrites and two fenced replay-command rewrites across five
Markdown files, with zero ambiguities and zero theorem-ledger relocations.
The moved theorem receives four reanchored outbound links and two full
destination replay commands; four inbound links are retargeted in the later
marked-fibre theorem, high-coordinate frontier, root README, and P4
toric-boundary theorem. The second pass must be a `0/0/0` fixed point.

The moved theorem's projected normalized-LF post-rewriter SHA-256 is
`196ed12d832b797593c4cb5fd4c84ef8a1204c8977e839f263d5cec42e00a7f8`.

Four existing ledger hashes must refresh mechanically while all status and
semantic fields remain fixed:

- verified high-coordinate frontier: `66ea30eaa865fd66` -> projected
  `bc51eb1ed19d017e`;
- the three root-README entries: `8b8d7ee9a4cfe4ac` -> projected
  `25c075592c445ed6`, with statuses `open`, `verified_generic`, and `partial`
  unchanged.

Navigation must add a narrowly labelled canonical-section divisor leaf to
`claims/p5/h31/README.md`, change its scoped-exception count from five to six,
record the Stage 22 batch/mapping hash, and state that the later full
marked-fibre successor remains root-owned. Parent P5 navigation must make the
same canonical-only qualification and change the H31 package-directory count
from 28 to 29. Neither navigation file may call this a complete marked fibre
or component closure.

## Projected transitions

| measure | before | after |
|---|---:|---:|
| manifest `moved` | 395 | 398 |
| manifest `proposed_high_confidence` | 242 | 242 |
| manifest `review_required` | 1,378 | 1,375 |
| moved-only manifest root projection | 1,977 | 1,974 |
| high-confidence manifest root projection | 1,735 | 1,732 |
| all-classified manifest root projection | 357 | 357 |
| measured root files | 1,969 | 1,966 |
| measured root directories | 9 | 9 |
| measured root entries | 1,978 | 1,975 |
| grandfathered root debt | 1,962 | 1,959 |
| new root debt | 0 | 0 |
| enforceable retired/provenance paths | 395 | 398 |

The move creates one approved nested claim-package directory and changes no
root baseline or end-state allowlist. H31 package directories increase from
28 to 29.

## Exact baseline replay

One serial baseline replay used `uv run --with sympy python` for each selected
script. The characteristic-zero primary returned rc=0, empty stderr, and one
JSON object in 2.7 seconds:

- `verified: true`, field `C`;
- parameters `[A,H,N,R]`, with `[A,H,N]` nonzero;
- generic ranks `[7,7,6,6]`, collision ranks `[6,6]`, and `R=0` profiles
  `[6,2,5,6]` and `[5,1]`;
- all four distinguished orientations excluded true; and
- `H31_excluded`, `P5_to_Delta3_resolved`, and global resolution false.

The field
`remaining_known_component_geometry="preferred_Pluecker_Schubert_boundary"`
is temporal checkpoint metadata. Current theorem prose records that boundary's
later separate closure. Preserve the field unchanged as nonblocking
provenance debt rather than silently editing scientific output.

The modular audit returned rc=0, empty stderr, and one JSON object in 42.3
seconds. It reports no primary imports and exactly:

- `F_5`: 320 boundary points, 4,608 projective binary extensions, all 4,608
  marked-injective, zero successful third-row lifts;
- `F_7`: 1,512 points, 31,104 extensions, all 31,104 marked-injective, zero
  successful third-row lifts; and
- zero ambient-local-map or Grassmannian enumeration, with explicit
  finite-field-audit/characteristic-zero-theorem scope.

The generated JSON files are ignored under repository `tmp/`; the tracked
tree remained clean.

## Post-move acceptance matrix

Use
`uv run --quiet --python 3.13 --with sympy --with python-sat python`,
strictly serial, and preserve every first rc=0, empty-stderr, valid-JSON
output before semantic assertions. The common environment is required because
three transitive consumers import the SAT-bearing high-coordinate helper. A
wrapper or post-parse error must not launch an automatic solver rerun or
become theorem evidence.

The complete recursive path/hash-consumer closure is 16 unique executables
and 22 invocations.

Run these six primaries/audits or directly retargeted primaries from both
repository root and a fresh foreign working directory by absolute path:

1. moved canonical primary;
2. moved canonical audit;
3. P4 toric-boundary primary;
4. complete chart-boundary marked-fibre primary;
5. canonical first-plane-infinity primary; and
6. high-coordinate primary.

Run these ten additional consumers once from repository root:

7. P4 toric-boundary audit;
8. complete chart-boundary marked-fibre audit;
9. canonical first-plane-infinity audit;
10. high-coordinate audit;
11. P4 toric-slice Segre primary;
12. H31 toric marked-fibre primary;
13. H31 internal-`E=0` marked-fibre primary;
14. H22 actual mask-6 independent audit;
15. component-19 `phi=+/-1` derivation; and
16. component-19 `qphi=-1` axes derivation.

The complete marked-fibre primary, toric marked-fibre primary, internal-`E=0`
primary, and component-19 `phi=+/-1` derivation require the established WSL
timeout/Singular route on Windows. Do not overlap external-Singular jobs. The
complete marked-fibre audit and `qphi=-1` axes derivation do not require
Singular.

Exactly nine matrix executables write only ignored repository-`tmp/` JSON and
seven are stdout-only; none writes a tracked output.

Require:

- all six root/foreign pairs agree exactly after removing only a demonstrably
  volatile `elapsed_seconds` field from the complete marked-fibre primary;
- canonical primary ranks and conservative false fields remain exact;
- canonical audit counts and finite-field QA scope remain exact;
- P4 toric boundary stays 44 divisor/orientation pairs, 21 gate-excluded and
  23 all-rank, with H31/P5/global false;
- complete marked successor retains its complete-divisor-fibre result while
  projective/internal/additional/global fields remain false;
- first-plane canonical checkpoint excludes all four orientations without
  promoting whole H31, P5, or global;
- high-coordinate census stays 6,495 / 1,680 / 1,170 / 510 and P5/global
  remain false;
- mask-6 remains `VERIFIED`, both component-19 derivations remain
  `CANDIDATE`, and every global field remains false where present;
- every theorem, primary, source, and dependency hash matches current bytes;
- all generated files remain ignored under repository `tmp/`, every foreign
  directory remains empty, and no tracked output drifts.

Both moved modules must pass isolated foreign-CWD import probes with SymPy.
Confirm targeted Ruff and byte compilation, rewriter fixed point, the
index-complete validation floor, exact-head CI, and fresh semantic plus
mechanical final referees before a normal head-guarded merge.

The global Krenn-Gu conjecture remains **UNRESOLVED**.
