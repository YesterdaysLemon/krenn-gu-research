# Layout migration Stage 18 report

Status: **SUBSTANTIVE MIGRATION COMPLETE ON BRANCH; AWAITING FINAL
EXACT-CANDIDATE REVIEW AND MERGE.**

The global Krenn-Gu conjecture remains **UNRESOLVED**. Stage 18 changes
filesystem ownership and portable replay paths only. It does not promote a
claim, extend a theorem's scope, reinterpret a timeout as evidence, close
weighted `H22`, prove a component compactification, or establish a
local-to-global theorem.

## Exact reviewed transaction

- Merged baseline:
  `fb87d7c33bfa75e24bc521074dca78c7f546b776`.
- Branch: `codex/stage18-h31-common-active-migration`.
- Dry-run approval commit:
  `3b5db11a3e47d840402e5dad6ef6b71b17f2a5da`.
- Frozen-batch commit:
  `48da1754a3019c82ab83314a3444e9c28fd36819`.
- Pure-move commit:
  `f5e51137c4bbf5f3766ea1e6c2551bb120eaa090`.
- Substantive repair commit:
  `1c914ba30381375b183a321f6a636d596e2caa43`.
- Substantive tree:
  `9a20caffdda2521fe62913a4abd4ef7289604d3f`.
- Batch ID: `p5-h31-common-active-p-plus-q-stage18`.
- Mapping SHA-256:
  `7595460669d3e45b4a5c12924f846d02e3dddf36385822a577b2826aebcb04d9`.
- Approval-time raw Windows-checkout manifest SHA-256:
  `e11e8c3e329936375e95ad513c715fd31d8eb5b4375bee06bb3fe0c013a6894a`.
- Actual mapping reviewer:
  `Codex (exact mapping reviewer under repository-owner standing delegation
  dated 2026-08-08)`.

The platform-specific approval-manifest hash is informational. The canonical
mapping hash is the portable authority for the nine old-to-new pairs. The
approved ownership analysis is recorded in
[`p5-h31-common-active-p-plus-q-stage18-dry-run.md`](p5-h31-common-active-p-plus-q-stage18-dry-run.md),
and the executable batch is frozen in
[`catalog/batches/p5-h31-common-active-p-plus-q-stage18.json`](../../catalog/batches/p5-h31-common-active-p-plus-q-stage18.json).

## Moved proof-obligation boundary

The exact nine members are three theorem/primary/audit triples moved flat to
`claims/p5/h31/common-active-binary-triangle/`:

1. whole diagonal-source-torus `p+q=0` wall;
2. component-15 exceptional `a=0,-1` lower-pair fibres; and
3. the two component-14 infinity-endpoint faces.

Their ownership topology is:

```text
shared P4 diagonal-DVR arc exhaustion (stays P4-owned)
  -> marked H31 on the diagonal-source-torus p+q=0 wall
       -> direct B_full/B_drop and a=-1/2 certificates
       -> component-15 exceptional a=0,-1 lower-pair child
       -> two component-14 infinity-endpoint child faces
       -> already-migrated embedded-P3 projective closure
       => the stated whole p+q=0 marked-H31 wall is closed

weighted H22 on this wall is separately VERIFIED and outside Stage 18;
broader component closure, gluing, and global stay open
```

The aggregate explicitly consumes both direct children. The P4 arc
classification and embedded-P3 closure remain separately owned upstream.
The other 12 common-active generic, normalized, special-divisor, and
intrinsic-boundary H31 files remain root siblings; the aggregate's generic
theorem hash is provenance, not a specialization premise.

Evidence roles remain distinct:

- all three theorem documents retain their exact characteristic-zero
  `VERIFIED` scopes;
- the whole-wall and exceptional-lower-pair audits are no-primary-import
  exact reconstructions;
- the infinity primary and audit share the root `marked_extension` and
  `mixed_matrix` construction, so independence is only downstream of that
  shared layer;
- the timed-out redundant 68-minor global ideal remains non-evidence; and
- the invalid generic component-14 specialization remains a failed route,
  not endpoint evidence.

No selected document has a curated theorem-ledger entry. Stage 18 adds no
entry and changes no status, assumption, evidence role, lifecycle, or global
field. The only ledger changes refresh the three existing root-README blob
hashes to `2d2c48d2364b4b34`; their statuses remain `open`,
`verified_generic`, and `partial`.

## Explicit exclusions

Stage 18 does not move or decide:

- the shared P4 `p+q=0` arc theorem and replays;
- the already-migrated embedded-P3 projective closure;
- the shared root marked-basis implementation;
- the remaining 12 common-active H31 sibling files;
- any H22 common-active wall obligation;
- component-20 intrinsic-zero, exceptional-fibre, or diagonal-atlas hybrid
  obligations;
- non-diagonal or arbitrary `GL4` source transformations;
- older-component placement or identification of `B_drop`;
- broader component compactification or component exhaustiveness;
- arbitrary-order local-to-global gluing; or
- the global Krenn-Gu conjecture.

Closing this diagonal wall is not a proof of all common-active H31, all
`H31`, weighted `H22`, `P5 -> Delta3`, or the global conjecture.

## Pure-move acceptance

Against the pure move commit's direct parent:

- exactly nine source paths disappear and nine destinations appear;
- all nine moves are `R100`, and every destination Git blob equals its
  source-parent blob frozen by the batch;
- the only non-rename change is `catalog/moved-paths.json`;
- exactly three `proposed_high_confidence` theorem records and six
  `review_required` script records become `moved` and gain
  `executed_batch: p5-h31-common-active-p-plus-q-stage18`;
- no other manifest record or non-count metadata changes; and
- collision, double-move, and overlap-cycle counts remain zero.

Observed arithmetic:

| measure | before | after |
|---|---:|---:|
| manifest `moved` | 377 | 386 |
| manifest `proposed_high_confidence` | 246 | 243 |
| manifest `review_required` | 1,392 | 1,386 |
| moved-only manifest root projection | 1,995 | 1,986 |
| high-confidence manifest root projection | 1,749 | 1,743 |
| all-classified manifest root projection | 357 | 357 |
| measured root files | 1,987 | 1,978 |
| measured root directories | 9 | 9 |
| measured root entries | 1,996 | 1,987 |
| grandfathered root debt | 1,980 | 1,971 |
| enforceable retired paths | 377 | 386 |

There are zero new root-debt paths. The frozen root baseline and end-state
allowlist are unchanged.

## Mechanical repair

All six moved executables now install the shared `krenn_gu.bootstrap` path
machinery before any root-helper import. Package-owned theorem and child
documents use `HERE`; the P4 wall, staying generic theorem, migrated
embedded-P3/full-support dependencies, and Git/Singular working directories
use `REPO_ROOT`. All six emitted replay commands name the full destination
path.

Exactly eight operational paths were retargeted across six staying Python
consumers:

- the H22 exceptional-fibre primary and audit;
- the H22 endpoint compatibility and integration audits;
- three H31 endpoint paths in the H22 endpoint derivation; and
- one aggregate path in the component-20 exceptional-fibre derivation.

Frozen certificate input labels and hashes remain historical provenance.
No generated-output path changed.

The deterministic rewriter made exactly seven Markdown-link changes and 12
fenced replay-command changes across five Markdown files, with zero
ambiguities and zero ledger relocations. Its second pass is a `0/0/0` fixed
point. Navigation records 26 H31 directories, one complete flat `p+q=0`
wall subforest, and the 12 sibling files that remain at root.

## Exact committed-head replay

Every affected executable ran from repository root by relative path and from
a fresh foreign working directory by absolute path at exact substantive head
`1c914ba30381375b183a321f6a636d596e2caa43`. Each emitted one JSON object,
empty stderr, and that exact `git_commit`. Removing only volatile top-level
`date_utc` made every root/foreign pair exactly equal.

| executable | root | foreign | preserved result |
|---|---:|---:|---|
| whole-wall primary | 16.353 s | 16.373 s | displayed charts and whole wall true; no lower-pair gaps; 68-minor replay false; H22/global false |
| whole-wall audit | 12.579 s | 12.578 s | independent exact wall audit true; finite-field inference false; H22/global false |
| exceptional-lower-pair primary | 4.997 s | 4.940 s | exact component-15 `a=0,-1` result true; finite-field false |
| exceptional-lower-pair audit | 2.377 s | 2.357 s | no-import exact replay true; finite-field false |
| infinity-endpoint primary | 3.604 s | 3.555 s | both endpoint faces `VERIFIED`; invalid generic specialization unused; global false |
| infinity-endpoint audit | 3.522 s | 3.541 s | separate exact reconstruction `VERIFIED`; global false |
| H22 exceptional primary | 5.587 s | 5.607 s | exact scoped H22 result `VERIFIED`; global false |
| H22 exceptional audit | 5.226 s | 5.207 s | no-primary-import replay true; global false |
| H22 endpoint compatibility audit | 2.145 s | 2.281 s | frozen off-wall compatibility obstruction `VERIFIED`; global false |
| H22 endpoint integration audit | 2.718 s | 2.718 s | finite-slope reduction true; unrelated source label remains `REFUTED`; global false |
| H22 endpoint derivation | 50.259 s | 50.550 s | both endpoint H22 results true; original rank sentence remains refuted; global false |
| component-20 exceptional-fibre derivation | 14.895 s | 14.888 s | replay `VERIFIED`; discovery label remains `CANDIDATE`; no new H31/H22 inference |

The final three pairs were repeated after a harness assertion was corrected
to read the integration verifier's nested `source_overall_label`; no
executable failed. Native Singular is absent. The existing fallback through
WSL Singular 4.3.2 passed all required exact elimination calls. Python is
3.13.14 and SymPy is 1.14.0.

All six moved modules also pass isolated foreign-CWD import probes under
`uv run --with sympy python -I` in about 1.17-1.26 seconds each. Replays and
probes leave the tree clean.

## Validation and nonblocking baseline debt

The clean substantive head passes:

- workflow-dispatch hygiene run
  [31294646401](https://github.com/YesterdaysLemon/open-graph-theory-with-prize/actions/runs/31294646401),
  including all repository jobs;
- `check_hygiene.py`: 1,698 Python files compile, all 808 pre-report Markdown
  files have resolving local links, all 86 ledger hashes match, root debt is
  `1,971 grandfathered / 0 new`, and all 386 retired-path/provenance records
  pass;
- all 152 migration-tool tests;
- all 14 fourteen-vertex cycle-cover lattice tests;
- targeted Ruff fatal/error checks and byte compilation for all 12 affected
  executables;
- exact root/foreign replay equality for all 12 pairs;
- six isolated moved-module import probes; and
- rewriter fixed point plus clean index/worktree diff checks.

An explicit `E702,E731` Ruff 0.15.21 scan still reports three pre-existing
`E702` findings and one pre-existing `E731` finding at lines 197-198 of the
exceptional-lower-pair audit. Those lines and findings predate Stage 18 and
are outside every Stage 18 hunk. Full default Ruff also applies `I001` and
`RUF100` to the repository's established shared-bootstrap pattern, including
prior migrated packages and these six new moves. Those non-authoritative
style findings are nonblocking; Stage 18 does not mix unrelated formatting
into the migration.

Adding this report raises the final Markdown count from 808 to 809. The final
documentation candidate (this report plus the dry-run topology correction)
must rerun the index-complete floor, receive fresh Tier-2 semantic/status and
mechanical/bypass referee passes, and pass final exact-head PR CI before a
normal guarded merge.

## Stop boundary

Stage 18 stops at the complete diagonal-source-torus `p+q=0` H31 wall
subforest. The broader common-active H31 family, every H22 common-active wall
obligation, component compactification, local-to-global gluing, and all
global work remain separate. The global Krenn-Gu conjecture remains
**UNRESOLVED**.
