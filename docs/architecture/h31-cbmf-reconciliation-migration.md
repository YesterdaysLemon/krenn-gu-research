# H31 chart-boundary marked-fibre reconciliation migration

Status: **EXECUTED AND LOCALLY VALIDATED; AWAITING FINAL MECHANICAL REVIEW**

The owner-authorized bounded characteristic-zero audit supports precise
outcome A.  The scientific evidence and terminology are recorded in
[`h31-cbmf-characteristic-zero-reconciliation.md`](../audits/h31-cbmf-characteristic-zero-reconciliation.md).
The global Krenn--Gu conjecture remains **UNRESOLVED**.

## Exact layout transaction

The correction commit `463532dc130e1a34804aa2e78b3ead835793ccd3`
is the reviewed layout base.  The four corrected/current root artifacts move
basename-preserving into their unique claim package:

`claims/p5/h31/component-chart-boundary-marked-fibre/`

They are the theorem, exact characteristic-zero primary, finite-field QA
audit, and directly owned elimination generator.  This is one coherent
proof/evidence unit; no status or ownership conflict remains after the
outcome-A reconciliation.

```text
member count:                    4
canonical mapping SHA-256:
  9d9c85b5dc89958ea0127f6ed5f7fe8000b060294e17966a70321a36b709f39e
canonical source-identity SHA-256:
  9a1eccdb780be9ed6cc084921bf397b779560419688b8210bf0be123e98f27ae
root before:       536 files + 9 directories = 545 entries
root after move:   532 files + 9 directories = 541 entries
debt before/after: 529 / 525 grandfathered, 0 new
justified root:    16 unchanged
```

The schema-v2 batch freezes the four corrected Git blobs at the correction
base.  The reconciliation audit separately records the original pre-audit
paths and blobs, so scientific correction provenance and move provenance are
both durable.

## Repair and validation boundary

After the pure `git mv`, only bootstrap/import, repository-relative link,
command, hash, ledger, and navigation repairs are allowed.  The three Python
entry points currently consume root providers, so they receive the shared
bootstrap before those imports; their mathematical implementations and
modular/exact roles do not change.  The later residual-P5 tranche may replace
those provider imports with reviewed source-core/package imports, but that is
a separate mechanical transaction.

No additional Singular, SAT, brute-force, numerical, sampling, or theorem
search is triggered by the move.  The four selected characteristic-zero unit
ideals were already replayed serially and pinned in the scientific audit.

Final publication requires fresh scientific and mechanical acceptance, the
seven focused reconciliation tests, default verifier replay including all
four selected unit ideals, modular QA replay, hygiene, migration tests,
fixed-point link rewrite, exact-head hosted CI, and merged-main CI.

## Execution and local replay

The committed batch executed as exactly four R100 renames plus the manifest
status/provenance update.  The repaired package uses the shared bootstrap,
all local links and replay commands are at a fixed point, and root now has
`532 files + 9 directories = 541 entries`: 16 justified and 525
grandfathered, with zero new debt.

The relocated exact primary completed its default replay in 784.612 seconds.
All four selected saturated ideals were the unit ideal, and every generated
program/stdout hash exactly matched the independently reviewed values in the
scientific audit.  The report recorded 13 projection components, 16 factor
records, 3 exceptional-basis records, 2 closure artifacts, 4/4 selected runs,
and `selected_saturation_exhaustiveness_confirmed: true`.

The relocated modular audit also passed independently:

```text
F5: 193 projection points, 5 closure artifacts,
    1152 binary extensions, 1548 selected-minor tests
F7: 421 projection points, 7 closure artifacts,
    4248 binary extensions, 5370 selected-minor tests
```

Its report explicitly records
`characteristic_zero_exhaustiveness_proved_here: false`.  The seven focused
reconciliation tests pass, and the link rewriter's second pass touches zero
files.  Hosted exact-head and merged-main CI remain publication gates.
