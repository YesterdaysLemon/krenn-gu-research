# Layout migration Stage 21 report

Status: **SUBSTANTIVE MIGRATION COMPLETE ON BRANCH; AWAITING FINAL
EXACT-CANDIDATE REVIEW AND MERGE.**

The global Krenn-Gu conjecture remains **UNRESOLVED**. Stage 21 changes
filesystem ownership, replay paths, navigation, and mechanically derived hash
metadata. It does not promote a function-field calculation to a pointwise
divisor theorem, close the opposite or coupled slopes, close parameter or
projective boundaries, prove all weighted `H22` empty, prove `P5 -> Delta3`,
or resolve the global conjecture.

## Exact reviewed transaction

- Merged baseline:
  `7352a0615ae1bfbccb118a1a0ec2d725ea432ff1`.
- Branch: `codex/stage21-h22-sixdim-equal-weight-migration`.
- Dry-run approval commit:
  `b22cf656491427514dba1b9381f0af0b91d057a0`.
- Frozen-batch commit:
  `2e084c479d856baaf361a3406cecbbf99d08c357`.
- Pure-move commit:
  `29428f5bef09d7a4c6bf4c9218280a911eab5f70`.
- Package repair commit:
  `1f0ab786c5bd38378b01b038a243e524b305a7db`.
- Bounded portability repair commit:
  `820753e1e28945a9becf9e62c9971675e3fb7dd4`.
- Substantive tree:
  `01eb668e7bd4265c90403c5cba3c8e13f1d9579a`.
- Batch ID: `p5-h22-six-dimensional-equal-weight-stage21`.
- Mapping SHA-256:
  `f7427206126ecc290b0a926c1731eb5eb557aca7d784547d4c64df2dc2b41cf0`.
- Approval-time raw Windows-checkout manifest SHA-256:
  `3b82e4fec1304eea532dd7f88eb816c55d0048ea50a2fc2c18ecb13402b9c181`.
- Actual mapping reviewer:
  `Codex (exact mapping reviewer under repository-owner standing delegation
  dated 2026-08-08)`.

The approval-manifest hash is platform-specific and informational. The
canonical mapping hash is the portable authority for the three old-to-new
pairs. The approved ownership analysis is recorded in
[`p5-h22-six-dimensional-equal-weight-stage21-dry-run.md`](p5-h22-six-dimensional-equal-weight-stage21-dry-run.md),
and the executable batch is frozen in
[`catalog/batches/p5-h22-six-dimensional-equal-weight-stage21.json`](../../catalog/batches/p5-h22-six-dimensional-equal-weight-stage21.json).

## Moved proof-obligation boundary

The exact theorem/primary/audit triple moved flat into the existing
`claims/p5/h22/six-dimensional/` package:

1. `P5_H22_SIX_DIMENSIONAL_EQUAL_WEIGHT_BINARY_OBSTRUCTION.md`;
2. `verify_p5_h22_six_dimensional_equal_weight_binary_obstruction.py`; and
3. `audit_p5_h22_six_dimensional_equal_weight_binary_obstruction.py`.

Its exact ownership topology is:

```text
generic point of the six-dimensional P4 component over C(s,d,u,v)
  -> equal source weight r=1
  -> every marked basis and fifth-coordinate extension on that normal form
  -> fourteen mixed equations plus two nonzero diagonal contractions
  -> exact saturated D01 and D23 projections are both the unit ideal
  => equal-weight binary H22 incidence is empty at that generic point

full geometric r=1 divisor, r=-1, coupled slopes, parameter/projective
boundaries, other components, all H22, P5 -> Delta3, gluing, and global
remain outside this leaf
```

The selected result is an exact characteristic-zero binary-incidence
obstruction at the **generic component function-field normal-form point**
`K=C(s,d,u,v)` after the equal-weight normalization `r=1`. It covers every
marking and extension at that point. It is **not a pointwise theorem for the
full geometric `r=1` divisor** and is not a whole-component theorem.

Evidence roles remain distinct:

- the theorem and primary own the exact characteristic-zero identities and
  two unit-ideal projections;
- the audit imports no primary and separately implements its
  dynamic-programming permanent, modular nullspaces, and exhaustive `p^4`
  marking census;
- the audit still consumes the theorem-specified normal form and one fixed
  admissible component sample in each of `F5` and `F7`; and
- the finite-field census is modular QA only, not the characteristic-zero
  proof or evidence for excluded slope, specialization, or boundary claims.

No selected artifact has a curated theorem-ledger entry. Stage 21 adds no
entry and changes no mathematical status, assumption, quantifier, evidence
role, lifecycle, or global-status field. The leaf consumes neither the
second-component `H31` status conflict nor the common-active `p+q=0`
weighted-`H22` status conflict and adjudicates neither.

## Pure-move acceptance

Against the pure-move commit's direct parent:

- exactly three source paths disappear and three destinations appear;
- all three moves are `R100`, with destination blobs identical to the frozen
  source-parent blobs;
- the only non-rename change is `catalog/moved-paths.json`;
- the theorem's `proposed_high_confidence` record and both scripts'
  `review_required` records become `moved` and gain the exact executed batch;
- no other manifest record or non-count metadata changes; and
- collision, double-move, and overlap-cycle counts remain zero.

The frozen source blobs are:

| artifact | Git blob |
|---|---|
| theorem | `3c5e8c88dab90ca2c8d6d17c9e45249ba207d02d` |
| modular audit | `b9a477a53818bbda85d4458e279edce6dc34d8ae` |
| characteristic-zero primary | `bbe942bf8f1e19a6da8543b32d1762f67d236169` |

Observed arithmetic:

| measure | before | after |
|---|---:|---:|
| manifest `moved` | 392 | 395 |
| manifest `proposed_high_confidence` | 243 | 242 |
| manifest `review_required` | 1,380 | 1,378 |
| moved-only manifest root projection | 1,980 | 1,977 |
| high-confidence manifest root projection | 1,737 | 1,735 |
| all-classified manifest root projection | 357 | 357 |
| measured root files | 1,972 | 1,969 |
| measured root directories | 9 | 9 |
| measured root entries | 1,981 | 1,978 |
| grandfathered root debt | 1,965 | 1,962 |
| enforceable retired paths | 392 | 395 |

There are zero new root-debt paths. The frozen root baseline and exact
end-state allowlist are unchanged.

## Package and evidence repair

Both moved executables now install shared `krenn_gu.bootstrap` before any
bare root-helper import. Package-owned theorem and primary paths use `HERE`;
P4 inputs and the shared high-coordinate helper use `REPO_ROOT`; ignored JSON
outputs remain under repository `tmp/`. The staying
`verify_p5_high_coordinate_partial_frontier.py` received the sole selected
dependency-path retarget. No operational importer or subprocess caller names
either moved script.

The deterministic rewriter made exactly five Markdown-link changes and two
fenced replay-command changes across five Markdown files, with zero
ambiguities and zero ledger relocations. Its second pass is a `0/0/0` fixed
point. Navigation adds a separately labelled normal-form-point leaf while
keeping the H22 package-directory count at 18 and repeating the non-pointwise
full-divisor boundary.

Exactly three existing ledger hashes changed mechanically while every status
and other semantic field remained fixed:

- verified high-coordinate frontier: `66ea30eaa865fd66`;
- verified-generic coincident-support H22 theorem: `ed0df77897f3f9f5`;
- verified-generic six-dimensional H22 theorem: `401808df53fb1a09`.

The moved theorem's normalized-LF post-rewriter SHA-256 is
`65f049ebbbbcc13444cbc45c125b4999b94842e3a05de018b8d1760e6701048c`.

## Exact selected baseline replay

The first primary command used only `sympy` and failed in 1.305 seconds
before mathematical computation because a shared helper requires
`python-sat`. It emitted no JSON. This dependency failure is not theorem
evidence.

One corrected serial baseline replay used `sympy` plus `python-sat`. The
primary passed in 53.813 seconds with field `C(s,d,u,v)`, pure coefficient
`2*s*u`, fourteen mixed equations, both saturated projections equal to
`[1]`, both extension-exists fields false, and equal-weight binary incidence
empty true. Weighted slopes, the primary's scope-local generic-H22 field,
component boundaries, all components, all H22, and global resolution stayed
false.

The audit passed in 24.916 seconds: 625 `F5` and 2,401 `F7` markings on each
of two diagonals, 6,052 total markings, zero viable extensions, and 3,026
direct `D23` checks. It reports both modular incidence fibres empty and the
finite-field results as corroboration only.

## Exact affected-evidence replay at package-repair head

The complete eleven-executable evidence closure ran serially at exact head
`1f0ab786c5bd38378b01b038a243e524b305a7db`. The three portability pairs ran
from both repository root and a fresh foreign working directory, yielding 14
total invocations. Thirteen returned rc=0, empty stderr, and one valid JSON
object. All generated outputs were ignored or stdout-only, and the tracked
tree remained clean.

| executable | repository root | foreign CWD | preserved semantic boundary |
|---|---:|---:|---|
| moved equal-weight primary | 58.571 s | 56.476 s | both exact unit ideals; equal-weight leaf true; broader/global false |
| moved modular audit | 25.178 s | 26.196 s | 6,052 markings, zero viable; modular corroboration only |
| high-coordinate primary | 1.023 s | 1.014 s | existing census and component synthesis fields; `P5 -> Delta3`/global false |
| high-coordinate audit | 3.068 s | - | census 6,495 / 1,680 / 1,170 / 510; global false |
| six-dimensional generic primary | 176.667 s | - | generic incidence true; all-H22/global false |
| six-dimensional generic audit | 138.966 s | - | independent modular corroboration; no scope promotion |
| coincident-support generic primary | **environment failure, 74.568 s** | - | no JSON; native `Singular` missing (`WinError 2`) |
| coincident-support generic audit | 25.207 s | - | 197,765 main-census markings, zero genuine survivors |
| actual mask-6 wall audit | 1.016 s | - | twelve of twelve flags obstructed; label `VERIFIED`; global false |
| component-19 `phi=+/-1` derivation | 14.119 s | - | exact construction checks pass; label remains `CANDIDATE` |
| component-19 `qphi=-1` axes derivation | 4.047 s | - | obstruction true, actual lift false; label remains `CANDIDATE` |

The three root/foreign pairs agree exactly. The generic-primary failure
produced no stdout or JSON; its preserved stderr records only
`FileNotFoundError [WinError 2]` from direct native `Singular -q`. No timeout,
failed search, or environment failure was interpreted as mathematical
evidence, and the failed invocation was not silently rerun.

## Bounded coincident-support portability repair

The complete matrix exposed a pre-existing Windows portability defect in the
otherwise unchanged coincident-support primary: it directly invoked native
`Singular -q`, although native Singular is absent and the repository's
established WSL Singular 4.3.2 route is available.

Commit `820753e1e28945a9becf9e62c9971675e3fb7dd4` changes only that verifier:

- shared bootstrap now resolves the same repository root and package-local
  theorem independently of working directory;
- Windows conditionally imports and uses the established repository WSL
  hard-timeout command;
- POSIX imports no Windows helper or transitive `python-sat` dependency and
  retains the original exact `("Singular", "-q")` command;
- the generated Singular program, stdin/stdout handling, outer timeout,
  return-code and stderr fail-closed checks, algebra, assertions, schema, and
  scientific booleans are unchanged; and
- the output remains ignored under package `tmp/`.

At exact repaired head `820753e1e28945a9becf9e62c9971675e3fb7dd4`, the
corrected verifier passed once from repository root in 392.4 seconds and once
by absolute path from a fresh foreign working directory in 395.8 seconds.
Both emitted `verified: true`, all four marking projections `[1]`, the generic
coincident-support H22 component field true, boundary closure false, all-H22
false, and global false. The theorem, source, and dependency hashes match;
only measured projection/fitting runtimes differ. The current source SHA-256
is `ea4153f5d3340ecba1e101a6cfd097fdd82f5c503c652c7a31579099dbcb3498`.
The foreign directory remained empty and the tracked index/worktree remained
clean.

Targeted Ruff, byte compilation, and a foreign-CWD isolated import probe pass
for the final one-file patch. Two independent read-only referees confirmed
that the conditional import closes the POSIX dependency regression and that
the patch changes no mathematical or status-bearing code.

## Validation and publication boundary

At the package-repair head, the index-complete validation floor passed:

- `check_hygiene.py`: 1,698 Python files compile, all 815 pre-report Markdown
  files have resolving links, all 86 ledger hashes match, root debt is
  `1,962 grandfathered / 0 new`, and all 395 retired-path/provenance records
  pass;
- all 152 migration-tool tests;
- all 14 fourteen-vertex cycle-cover lattice tests;
- deterministic rewriter fixed point;
- targeted Ruff, byte compilation, and foreign-CWD isolated import probes;
- clean index/worktree diff checks.

The eleven-script/14-invocation affected-evidence matrix also completed at
that head with thirteen successful JSON invocations and the single
coincident-support `WinError 2` described above; it was therefore not a green
replay closure. The bounded portability commit then closed that failed row
with its focused Ruff, compilation, import, root replay, foreign replay, and
clean-tree checks.
Adding this report raises the final Markdown count to 816. The final
documentation candidate must rerun the complete index floor, receive fresh
Tier-2 semantic/status and mechanical/provenance/bypass referee passes, and
pass exact-head PR CI before a normal guarded merge.

## Independent final referees

The report candidate at index tree
`6b03d0c2633e7d90e34b305b7b28097551d496fe`, with substantive HEAD
`820753e1e28945a9becf9e62c9971675e3fb7dd4` and staged report blob
`4a5bed7206e103cdb4c97dd6c6d46ccab81f484c`, received two independent
read-only final passes:

- the Tier-2 semantic/status referee returned **PASS**, confirming the exact
  generic normal-form-point scope, non-pointwise divisor boundary, limited
  modular-audit independence, unchanged ledger semantics, unconsumed status
  conflicts, transport-only portability repair, honest 13-plus-1 replay
  account, and global **UNRESOLVED** boundary; and
- the mechanical/provenance/bypass referee returned **PASS**, independently
  reproducing the commit chain, batch and approval hashes, three `R100`
  blobs, selected-only manifest transaction, root/debt arithmetic, rewrite
  and ledger surface; reconstructing the affected-evidence matrix; auditing
  the preserved bounded-portability and full-floor evidence; and confirming
  the absence of bypass or candidate drift.

This referee record is the sole report change after that reviewed pin. A
lightweight read-only superseding check must confirm the append-only delta;
its final-tree verdict belongs in the Stage 21 pull-request review trail
without another self-referential report edit.

## Stop boundary

Stage 21 stops at the equal-weight `r=1` binary-incidence obstruction at the
generic six-dimensional component function-field normal-form point. It does
not claim pointwise closure of the full geometric divisor. Opposite and
coupled slopes, parameter and projective boundaries, other components,
component exhaustiveness, all weighted `H22`, `P5 -> Delta3`, local-to-global
gluing, and all global work remain separately owned. The global Krenn-Gu
conjecture remains **UNRESOLVED**.
