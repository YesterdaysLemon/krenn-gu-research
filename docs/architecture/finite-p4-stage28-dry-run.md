# Stage 28 dry run: bounded finite forest and conflict-free P4 packages

Status: **EXACT PRE-EXECUTION REVIEW; NO STAGE 28 FILE MOVE HAS OCCURRED.**

The global Krenn-Gu conjecture remains **UNRESOLVED**. This dry run freezes
filesystem ownership and executable/provenance repair obligations. It does not
change a theorem, quantifier, support condition, field, lifecycle, evidence
role, case cover, formalization state, or global status.

Stage 27 is merged and validated. This is the second tranche of the
[Stages 27-29 program](layout-migration-stages27-29-reviewed-plan.md).

## Frozen base and proposed batch

```text
base commit: 5726180037986d27b9e445ee058e6c221b6d2d03
base tree:   07195d8c8720901f31fe35b5d037f01bf04d1b0f
batch id:    finite-p4-stage28
members:     201
```

The independently reproduced run-local mapping artifact has portable file
SHA-256:

```text
a2fc55c46ac5806c87888e43fbe15a8f5a6e08fea77b139dec8012701d67d433
```

Its mapping verifier has SHA-256
`bbf759b9bbd3e75d56b59b95dba5d15c3346da5401f19c5d479c84ed9aa7f075`
and passes on the exact commit and tree above. The durable batch file will be
committed separately. Its exact `moves` array, not a predicate or classifier
family name, will be the executable authority.

## Exact mapping identity

Canonical aggregate mapping SHA-256:

```text
2715a521e40f5ad6815af2044af1b1c075bf1cf29de81b076c4de239db5cf9a6
```

Canonical aggregate source-identity SHA-256:

```text
d64ecf0b34d83efbd24d9cef24841e17719c427159f07b5eea7dcc5fdb776871
```

The mapping digest uses compact sorted-key JSON records containing
`old_path` and `new_path`. The source digest adds each exact index
`git_blob`. Both record lists are sorted by `(old_path, new_path)`, so the
identities do not depend on working-tree line endings.

The two disjoint reviewed subsets are:

| subset | documents | carriers | members | mapping SHA-256 | source SHA-256 |
|---|---:|---:|---:|---|---|
| finite | 49 | 84 | 133 | `09206352818278ce66e9112ea6045ca34e0df12365c8caf84c08c16eb4439d30` | `25d1c183c59efeb142285db366c33437d1aafb19ac5b2309a873f990861f4fe9` |
| P4 safe | 24 | 44 | 68 | `70cdd782d78c47f1ae0f7deaaf9a1178c578df88a60bdd8c40ecad6b056b4295` | `018ecc42523b69ca8bc6dc9131aece536a3bc3f04d4f20183f5fb5df83870db4` |

Destination totals are:

| destination | members |
|---|---:|
| `claims/finite/n14/` | 70 |
| `claims/p4/classifications/` | 50 |
| `claims/finite/n08/` | 28 |
| `claims/p4/boundaries/` | 18 |
| `claims/finite/n10/` | 15 |
| `tools/explore/` | 6 |
| `claims/finite/n12/` | 5 |
| `tools/generate/` | 4 |
| `src/krenn_gu/` | 3 |
| `claims/finite/n06/` | 2 |

All 201 sources and destinations are unique. There are no missing sources,
existing destinations, collisions, double moves, or source/destination
cycles.

## Reviewed membership and boundaries

The finite subset selects all 49 current claim documents in the five bounded
finite families and every current-root Python carrier named by them. Every
finite statement retains its stated order, support, factor, orbit, selector,
connectivity, equality-boundary, or conditioned-CNF hypotheses. A finite
survivor is not a counterexample, sampling is not exhaustive computation, and
an exhaustive claim remains confined to its named finite family.
The order-twelve Kotzig-port record remains finite regression evidence
subsumed by the Stage 27 arbitrary-order theorem, not a second arbitrary-order
proof. The order-fourteen frontier remains partial wherever its document and
ledger say it is partial.

The P4 subset selects the 24 package-complete documents whose named root
Python evidence remains P4-owned, together with those 44 carriers. It
explicitly excludes:

- `P4_COMMON_ACTIVE_BINARY_TRIANGLE_P_PLUS_Q_BOUNDARY` and its two scripts;
- both `P4_COMPONENT20_*_PROOF_B` documents and their conflict carriers; and
- `P4_INOUT_PATH_STRATUM_WORKING_NOTE` and its external exploratory tools.

`P4_FIRST_COMPONENT_APOLAR_TRIANGLE_NORMAL_FORM` remains only a dense P4
normal form; other Borel orientations and special boundaries stay open. The
selected diagonal-quadric results are lower bounds or subcases, not component
exhaustiveness. `P4_BOREL_GAUGE_CORRECTION` preserves the withdrawn lifecycle
of its antecedents.

This batch does not consume or adjudicate any of the four owner-gated
surfaces:

1. the H31 marked-fibre theorem/primary 14-versus-16 contradiction;
2. the P4 internal-`E=0` versus chart-`D=0,a!=0` attribution ambiguity;
3. the first/second-component provenance and closure disagreement; or
4. the weighted-H22 `p+q=0` status ambiguity.

The first is a genuine theorem/primary contradiction and repository stop
condition. The other three remain unadjudicated ownership/provenance
ambiguities.

The Component20 intrinsic-wall/zero-diagonal package, Component21/23,
embedded-P3, H31 common-center, Branch B, high-coordinate, and legacy routing
remain excluded. No exact counterexample was found, and no selected result
resolves arbitrary P7/local-to-global or the global conjecture.

## Durable classifier refinement

The freeze exposed exactly 68 previously unclassified Python carriers. Every
one is named by a bounded finite owner and has one finite family/destination.
They are added as high-confidence `claim_script` records:

| family | additions |
|---|---:|
| `finite/fourteen-vertex` | 39 |
| `finite/eight-vertex` | 17 |
| `finite/ten-vertex` | 8 |
| `finite/twelve-vertex` | 3 |
| `finite/six-vertex` | 1 |

Confidence remains classifier metadata, not scientific status or execution
authority. The portable 68-record tuple serialization is
`old_path|proposed_path|category|claim_family|confidence`, sorted by
`old_path` in ordinal case-sensitive order, UTF-8 with LF separators and one
trailing LF. It is 9,980 bytes with SHA-256:

```text
0e7a0cb7e4c5de69546463cdd9f08756f554081d831e155ef2d15917b0a3c488
```

Catalog counts change as follows:

| field | before | after |
|---|---:|---:|
| classified | 2,056 | 2,124 |
| unclassified | 307 | 239 |
| claim scripts | 1,113 | 1,181 |
| high confidence | 450 | 518 |
| estimated root entries after full classified migration | 315 | 247 |

After refinement and deterministic manifest regeneration, the portable
Git-content SHA-256 values are:

```text
layout-classification: dd1b0803f5295594ce5a8d7e2c8b07617a7868fc94028ce741beea3badac5ddf
moved-paths manifest:  1b44664f26a50b310e3f4a984fef0e50449017ee7ccd434c8b3ac450ae4d5a32
unclassified-files:    1d1cacfa4c59e95a73e26d4ba68d3d5e81f1295ee300777266809ee225174e28
```

The manifest reports 2,124 classified records: 998 moved, 120
`proposed_high_confidence`, 1,006 `review_required`, and 239 unclassified,
with zero collisions, double moves, or cycles. Within this exact batch, 75
records are high-confidence proposals and 126 require review. The committed
batch will approve both classes under the standing delegation.

## Ledger status freeze

Only zero-based ledger entries 5-10 and 66 have selected documents. Migration
may update their paths and staged-Git-blob document hashes, but it must not
change status, scope, provenance, dependencies, or evidence coverage:

- entries 5-9 remain `verified_finite`, with null primary/audit paths and
  `historical_certificate_chain` provenance;
- entry 7 retains the open exact-18-edge degree-four case, and entry 9 retains
  the open sub-105-entry and non-5-regular exact-25-support branches;
- entry 10 remains `partial`; its three order-14 families remain explicitly
  incomplete;
- entry 66 remains `verified_generic` only within the all-pair-rank
  exceptional-graph reduction and source/mode symmetry, with its mapped
  primary, null independent audit, `not_yet_mapped` audit provenance, and
  `BOTTLENECK B3` quantifier-review note.

No audit is populated merely because a carrier co-moves.

## Executable and provenance repair freeze

The independently reproduced repair inventory has SHA-256:

```text
ff9823b70f711c257dd57bb6ae24cceb72ddb601d14cc37281612902efd7382f
```

Its verifier has SHA-256
`1fda519b7516f8140accfeb83329c29282ddce39ede8f27b861f38b8312cd48f`
and its recorded output has SHA-256
`63fa5bd7df5ddc682583cd55c71913f92fe19dadd53e0218f970773954c45a92`.

### Import and path topology

The exact crossing-import surface is 82 edges in 46 distinct importer files:

| repair route | edges |
|---|---:|
| guarded bootstrap to staying root modules | 52 |
| `expose_claim_package` for moved claim/tool directories | 15 |
| qualified `krenn_gu` imports | 15 |

By direction, these are 52 selected-to-staying-root, 23
staying-to-selected, and seven selected cross-destination edges. Finite owns
73 edges/37 importer files; P4 owns nine edges/nine importer files, with no
importer overlap. No helper extraction, algorithm change, assertion change,
or evidence-schema change is required.
Thirty-nine importer files need the guarded shared bootstrap; seven already
have bootstrap machinery that must be retained and checked. Same-destination
selected sibling imports remain package-local and are not crossing edges.

The folded-literal inventory contains 100 actionable staying sites across 68
files: finite contributes 27 sites/12 files and P4 contributes 73 sites/56
files. All receive exact `REPO_ROOT` paths. Five docstring mentions and three
deliberate migration-test fixtures are excluded from the actionable set.

Selected scripts contain 150 explicit repository-path records across 48 files.
For the 75 same-final-directory records, runtime filesystem/argv sites use
`HERE`/`with_name`; non-path semantic strings remain byte-identical. For the
75 nonlocal records across 32 files, runtime filesystem/argv sites resolve
from `REPO_ROOT`, while old-path semantic keys remain only behind the reviewed
manifest-aware resolver rooted at `REPO_ROOT`; finite contributes 46 and P4
contributes 29.

The migration-aware Markdown simulation rewrites exactly 242 links and 221
replay commands across 118 files, with zero ambiguity. Its second pass is an
exact `0/0/0` fixed point.

### Component20 two-anchor provenance guard

The staying Component20 audit's historical commit remains
`f997c8366b461f3952faef0d35b512318341909d`. That commit and its old-path
source blobs remain historical provenance; current mechanically rewritten
bytes must not be compared directly to it.

The historical anchor must verify that
`f997c8366b461f3952faef0d35b512318341909d` is an ancestor and that its exact
old-path Git blobs are:

- `P4_COMMON_ACTIVE_BINARY_TRIANGLE_COMPONENT.md`:
  `1581b3f3ff509c125a1fd3ec7cf00893be1b4bbf`;
- `P4_COMMON_SINGLETON_COMPONENT.md`:
  `4cde2ae57902e10024430666071aafe3ec2e6933`;
- `P4_SUPPORT_ONE_SECANT_BOUNDARY_INCLUSION.md`:
  `09e000de55598cc296119404f5a0ef5756a2826f`; and
- `P4_TRIPLE_KERNEL_RANK_ONE_TRIANGLE_CLASSIFICATION.md`:
  `fbdc270ba57ff790078a8d36de2b859c751a229f`.

After the pure moves and exact first rewriter pass, an intermediate commit
must be created before editing the audit. The checkpoint must contain these
four current paths and predicted Git blobs:

| source | current path after Stage 28 | checkpoint blob |
|---|---|---|
| Component20 | `claims/p4/classifications/triangle-211/common-active-binary-triangle/P4_COMMON_ACTIVE_BINARY_TRIANGLE_COMPONENT.md` | `eb3d499dc6c39902f9e0053c3280fdc1e1b16464` |
| Component18 | `claims/p4/classifications/P4_COMMON_SINGLETON_COMPONENT.md` | `099c4b8c943a79ecd28d72b5653cf59791bcb6d8` |
| Component15 boundary | `claims/p4/boundaries/pair-geometry/support-one-secant/P4_SUPPORT_ONE_SECANT_BOUNDARY_INCLUSION.md` | `4d3b26491b1e94d33da89fc52af3f3e31eb18c88` |
| Component16 boundary | `claims/p4/classifications/triangle-211/triple-kernel-rank-one-triangle/P4_TRIPLE_KERNEL_RANK_ONE_TRIANGLE_CLASSIFICATION.md` | `358503edee64f8a52136a71e70c5391892dfe6a7` |

The repaired guard must verify checkpoint ancestry, verify each
`checkpoint:path` blob, and run subsequent `git diff --quiet` checks with all
four full repository-relative paths. It must never use `Path.name`. A focused
probe imports the audit without `main()`, asserts the guard is true, verifies
the four pathspecs, and proves a source-byte mutation in an isolated temporary
Git fixture makes the guard false. This is provenance/path repair only; it
does not rerun or adjudicate Component20 mathematics.

## Root and manifest projections

The live base has 1,366 tracked root files and nine root directories: 1,375
entries, with root debt 1,359. Moving exactly 201 files gives 1,165 root files
and nine directories: 1,174 live entries and debt 1,158.

The manifest's historical starting-tree projection is one lower because the
later retained `AGENTS.md` is intentionally outside its classifier universe:

```text
post-Stage-28 moved-only projection: 1173
observed live expectation:           1174
```

After execution, manifest statuses must be 1,199 moved, 45
`proposed_high_confidence`, 880 `review_required`, and 239 unclassified. The
remaining-high-confidence historical projection will be 1,128; the
all-classified projection remains 248.

## Execution and acceptance contract

The next commit will freeze a batch with:

```text
approved_by: Codex (exact mapping reviewer under repository-owner standing
             delegation dated 2026-08-08)
base_sha:    5726180037986d27b9e445ee058e6c221b6d2d03
members:     201
mapping:     2715a521e40f5ad6815af2044af1b1c075bf1cf29de81b076c4de239db5cf9a6
```

Before repair, the staged move-only tree must show exactly 201 R100 renames,
zero additions/deletions, `0 0` numstat for every pair, and destination index
blobs equal to the frozen `git_blob` records. Only the executor-owned manifest
state may change with the moves.

Repair then proceeds in two evidence-preserving checkpoints:

1. run the migration-aware rewriter, verify the exact `242/221/118` result and
   zero-ambiguity fixed point, and commit the intermediate source checkpoint;
2. verify the four predicted Component20 source blobs, then apply only the
   frozen import/path, guard, ledger-path/hash, navigation, and report repairs.

The repaired candidate must:

1. compile every edited Python source from the candidate index in a fresh
   no-bytecode process;
2. import every changed importer from a foreign working directory without
   calling `main()` or starting scientific computation;
3. reproduce all 82 import edges, assert every exposed claim/tool directory
   exists, and import all three qualified `krenn_gu` providers;
4. AST/string-fold every repaired filesystem `Path`/subprocess expression
   among the 100 staying and 150 selected records and require its resolved
   target to exist in the candidate index; separately assert that preserved
   old-path semantic keys remain byte-identical and resolve only through the
   reviewed manifest-aware resolver;
5. pass the Component20 ancestry/blob/pathspec/mutation probe;
6. preserve ledger entries 5-10 and 66 exactly except for moved paths and
   staged-Git-blob document hashes;
7. retain a zero-ambiguity rewriter fixed point; and
8. pass the authoritative index-complete floor:

```text
git add -A
python check_hygiene.py
python -m unittest -v tests.test_migration_tools
python -m unittest -v test_fourteen_vertex_cycle_cover_lattice.py
python tools/migration/rewrite_links.py
git diff --exit-code
```

Focused import/path probes, not broad SAT, Singular, sampling, or theorem
reruns, are the appropriate validation for these path-only repairs. Execution
remains blocked until the exact batch is committed, independently reviewed
against this dry run, and accepted by the executor's own dry run.
