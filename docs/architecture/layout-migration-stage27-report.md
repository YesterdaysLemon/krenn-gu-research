# Layout migration Stage 27 report

Status: **MIGRATION COMPLETE AND VALIDATED; PUBLICATION IS TRACKED IN PULL
REQUEST #61.**

The global Krenn-Gu conjecture remains **UNRESOLVED**. Stage 27 changes
filesystem ownership, executable paths, navigation, and mechanically derived
catalog/ledger metadata. It also corrects two stale ledger mappings for
already-documented evidence carriers. It does not change an underlying
theorem, scope, lifecycle, evidence role, finite-case quantifier,
formalization status, owner-gated conflict, or global status.

## Exact reviewed transaction

- Reviewed merged-main base: `7c332c7a307542395add3420ed62b47ebd72287c`.
- Dry-run/catalog checkpoint: `4622d37`.
- Frozen-batch commit: `e0177ca`.
- Pure-move commit: `5ab2f65`.
- Repair/navigation/report commit:
  `8a62b06e776abc34b842a55d112227fd0353b8b6`.
- Substantive tree: `960fb55880119d59ad102e45de4c13775cccae90`.
- Batch ID: `high-order-symbolic-forest-stage27`.
- Members: 587 distinct root files.
- Canonical mapping SHA-256:
  `cc379f4cf74ebb7c8ffbf428e8a68acca6f18703e3f5e95d70183f2f5887f7b0`.
- Frozen source-identity SHA-256:
  `b644cf42195fe50215f33bc5dbf39917bc316cea3e3d01c9b4c443cb710801aa`.
- Approval-time manifest SHA-256:
  `17d477c63ebc0aef8b420913df767be9b2958eb160598eebefef3e89b3745a7c`.
- Final batch-file SHA-256:
  `2459bfd5b7c0f9819794ae577826a51f43b4419bcfb0cb8d866e49967f030a2d`.
- Repair-inventory SHA-256:
  `769ffecc5308d457d447f8369f8085491bd2eb732ed643aa6855be5305b1c450`.
- Actual reviewer: `Codex (exact mapping reviewer under repository-owner
  standing delegation dated 2026-08-08)`.
- Publication surface:
  [pull request #61](https://github.com/YesterdaysLemon/open-graph-theory-with-prize/pull/61).

The mapping and source-identity digests are portable approval evidence. The
reviewed ownership analysis is in
[the dry run](high-order-symbolic-forest-stage27-dry-run.md), and the exact
executable mapping is in
[the batch](../../catalog/batches/high-order-symbolic-forest-stage27.json).
Classifier confidence supplied proposal metadata, not execution authority.

The Stage 27 classifier refinement added exactly 41 previously unclassified
records and corrected two `ROOT_M7` destinations. Its portable 43-row tuple
digest is
`09c4dcdf94f3ee39364016f47902a2eb4785f62a4a9d3bc41780c440822bed44`.
The resulting raw catalog hashes are:

- layout classification:
  `67e0f278e8213be6f6c66cb9e51746a57f179bb833d3f7cd7869a0d19f6dbcf9`;
- moved-path manifest at approval:
  `17d477c63ebc0aef8b420913df767be9b2958eb160598eebefef3e89b3745a7c`;
- unclassified inventory:
  `e8aa8362aefc13d2b31756c7734ae347fd1399c973d23a313eda582e14473100`.

## Moved ownership forest

The exact destination totals are:

| destination | migrated members |
|---|---:|
| [`claims/arbitrary-order/`](../../claims/arbitrary-order/) | 352 |
| [`claims/p7/`](../../claims/p7/) | 183 |
| [`claims/p6/`](../../claims/p6/) | 39 |
| `tools/generate/` | 10 |
| `tools/explore/` | 3 |
| **total** | **587** |

The source set contains 196 Markdown documents and 391 Python carriers. P6
and P7 move as 13 and 61 document/primary/audit packages respectively. The
arbitrary-order set preserves both exact triples and genuine asymmetric
evidence leaves; it does not manufacture a missing verifier or audit.

The complete 42-file `ROOT_M7` ownership subtree moves together, including
the two primary verifiers previously misrouted toward `src/krenn_gu`. Its
completeness is filesystem/provenance closure only. The relative
fourth-incidence shell excludes 908 supports, not all
`binomial(104,4) = 4,598,126` supports; arbitrary P7/local-to-global remains
`UNKNOWN`.

The three supplemental arbitrary-order packages and three bridge antecedents
retain the scope walls recorded in the reviewed plan. In particular, the
two-port reduction leaves synchronization of its three overlapping P5
systems open. Bounded tools remain generators, analyzers, materializers, and
solver runners rather than theorem evidence.

## Preserved exclusions and status walls

Stage 27 does not consume or adjudicate any of the four owner-gated surfaces.
Every currently rooted artifact excluded because of them remains at root and
unmoved:

1. the H31 fourteen-versus-sixteen theorem/primary contradiction;
2. the P4 internal-`E=0` versus chart-boundary ownership ambiguity;
3. the first/second-component provenance and closure disagreement; and
4. the weighted-H22 `p+q=0` ownership/status ambiguity.

Component20 and its two P4 proof-B dependencies, Component21/23,
high-coordinate packages, H31 common-center, embedded-P3 H22, Branch B, and
the mixed wholly/partially withdrawn legacy lineages are likewise excluded.
No exact counterexample was found. The first item remains a repository stop
condition; items 2-4 remain ownership/provenance questions rather than
adjudicated mathematical contradictions.

## Pure execution and root arithmetic

Against its parent, pure-move commit `5ab2f65` contains exactly 587 `R100`
renames and the executor-owned manifest transaction. Every rename has `0/0`
numstat, and every destination index blob equals its frozen source blob. No
mathematical or executable byte changes in that commit.

| measure | before | after |
|---|---:|---:|
| measured root files | 1,953 | 1,366 |
| measured root directories | 9 | 9 |
| measured root entries | 1,962 | 1,375 |
| grandfathered root debt | 1,946 | 1,359 |
| manifest `moved` | 411 | 998 |
| manifest `proposed_high_confidence` | 282 | 52 |
| manifest `review_required` | 1,363 | 1,006 |
| unclassified | 307 | 307 |
| enforceable moved paths | 411 | 998 |
| moved-only base-ref projection | 1,961 | 1,374 |

The measured tree is intentionally one entry above the manifest projection:
`AGENTS.md` was added after the manifest's recorded starting commit and is
outside its classifier-era projection universe. Committed tooling derives
the projection from that starting commit; reports use the observed live
count. The actual Stage 27 reduction is exactly 587 entries.

## Mechanical repair and provenance

The first migration rewriter pass changed exactly 303 Markdown links and 565
fenced replay commands across 203 files with zero ambiguity. Every later pass
reported zero links, zero commands, zero touched files, and zero ambiguity.

Manual executable repairs are limited to the frozen inventory:

- six selected bootstrap/external-path verifiers;
- 18 package-local theorem paths changed from root-relative `Path(...)` to
  paths derived from `__file__`;
- four `FAST_VERIFIERS` entries in `check_hygiene.py`;
- two P5 pair-signature normal-form consumers;
- six finite CLI theorem defaults;
- four binomial-support augmenter argv sites; and
- one `Containerfile` example.

Exactly 35 changed Python files compile from the staged index. Fresh imports
of the two outbound-import leaves and four external-path verifiers pass from
a non-repository working directory without calling `main()` or the P7 loader.
All 18 package-local paths, four external paths, 16 staying Python literal
sites, four folded augmenter argv sites, and the container example resolve.

The theorem ledger remains schema v3 with 86 entries. Entries 11-17 now use
their final paths. Entries 14 and 17 correct stale absent-evidence metadata:
their on-disk primaries use `script_is_the_verifier`, and their distinct
finite-field audits use `independent_modular_audit`. At index 14, the F5 audit
independently rechecks the slice-space and polarization calculations but reads
the primary artifact only for rank agreement. At index 17, the F5/F7 audit
reconstructs the coefficient calculation from the missing-coordinate rule
using the displayed integer maps. These are scoped modular corroboration
layers, not second characteristic-zero proofs; the index-17 audit is also not
independent witness selection. Status, theorem scope, and dependencies do not
change. Hashes for entries 11-17 and hash-only consumers 0, 65, 71, and 83
were recomputed from the final staged Git blobs; all 86 ledger hashes match.

## Validation

The index-complete candidate tree passes:

- `python check_hygiene.py` (1,698 Python files compile, all 834 Markdown
  files link-clean, ledger 86/86 hashes, 998 provenance paths, five fast
  verifiers, zero new root debt);
- `python -m unittest -v tests.test_migration_tools` (152 tests);
- `python -m unittest -v test_fourteen_vertex_cycle_cover_lattice.py`
  (14 tests);
- fixed-point `python tools/migration/rewrite_links.py` (`0/0/0`, no
  ambiguity); and
- `git diff --exit-code` plus `git diff --cached --check`.

The substantive head
`8a62b06e776abc34b842a55d112227fd0353b8b6` also passed both GitHub Actions
executions of the complete workflow:

- [pull-request run 31337593052](https://github.com/YesterdaysLemon/open-graph-theory-with-prize/actions/runs/31337593052): `success`;
- [manual workflow-dispatch run 31337600486](https://github.com/YesterdaysLemon/open-graph-theory-with-prize/actions/runs/31337600486): `success`.

Each run checked hygiene, all migration-tool tests, the self-contained lattice
tests, and rewriter closure against that exact substantive SHA.

Singular, msolve, kissat, glucose, and drat-trim are not installed on this
runner. No SAT, Singular, brute-force, or broad theorem rerun was justified by
these path-only changes. The four moved fast verifiers execute once through
the hygiene floor; unchanged scientific results are preserved by source-blob
identity.

Stage 27 alone does not put the repository below GitHub's 1,000-entry
contents cap. Stage 28 must be frozen against the actual merged Stage 27 base,
independently reviewed, executed, validated, and merged before Stage 29 is
refrozen. No later stage may inherit approval merely from this report.

The global Krenn-Gu conjecture remains **UNRESOLVED**.
