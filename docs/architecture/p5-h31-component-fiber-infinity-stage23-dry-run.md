# P5 H31 canonical first-plane Schubert-infinity section - Stage 23 exact migration dry run

Status: **APPROVED FOR AN EXACT FROZEN BATCH under the repository-owner
standing delegation dated 2026-08-08. No move has yet been executed.**

> **Scientific status will not change.** The global Krenn-Gu conjecture
> remains **UNRESOLVED**. This review resolves filesystem ownership only. It
> does not promote a canonical marked-row obstruction to a complete marked
> fibre, an entire projective boundary, a whole-component theorem, component
> exhaustiveness, `P5 -> Delta3`, gluing, or a global result.

## Review authority and audited baseline

- Exact clean baseline:
  `97abc5f2c0cedc3707d0c97b85382df8d1747d74`.
- Branch: `codex/stage23-h31-component-fiber-infinity-migration`.
- Actual mapping reviewer:
  `Codex (exact mapping reviewer under repository-owner standing delegation
  dated 2026-08-08)`.
- Delegated-review test: routine, non-ambiguous, evidence-backed exact layout
  mapping; no scientific status decision, ambiguous proof-boundary decision,
  or owner-preference architecture choice is required.
- Batch ID to freeze: `p5-h31-component-fiber-infinity-stage23`.
- Approval-time raw Windows-checkout manifest SHA-256:
  `c6c5b192a13da3016fe4d70f784c8cc7991b471e26c119eeec5b8a8d36f1dc36`.
- Corrected classifier raw Windows-checkout SHA-256:
  `8561e714ce1aafe77a9d274cc62feb756ba9b7335c8cc1cf920641f89f22c397`.
- Canonical mapping SHA-256:
  `3874be216b1210251aea1150fa655e7ea5bde0c035df0d8c9d51d18b0d57a454`.

The manifest hash is informational provenance over raw CRLF checkout bytes;
the mapping hash is the portable authority for the reviewed pairs. All three
records are `review_required` with medium classifier confidence. Confidence
is proposal evidence only; approval comes from independent topology, status,
consumer, and mechanical audits and applies only to this exact triple.

This baseline also records why the initially preferred four-file complete
chart-boundary marked-fibre family was deferred: its theorem says fourteen
certificate strata while its primary constructs, asserts, and reports
sixteen. That claimed-proof/verifier contradiction is owner-gated and no batch
was frozen. Commit `025ee3c` separately corrected that deferred family's
generator ownership; commit `97abc5f` records the blocked candidate audit.
Neither commit changes this selected triple, its manifest status, or its
scientific evidence.

## Exact three-file mapping

All three files move flat into `claims/p5/h31/component-fiber-infinity/`.

| role | source | destination |
|---|---|---|
| theorem | `P5_H31_COMPONENT_FIBER_INFINITY_OBSTRUCTION.md` | `claims/p5/h31/component-fiber-infinity/P5_H31_COMPONENT_FIBER_INFINITY_OBSTRUCTION.md` |
| characteristic-zero primary | `verify_p5_h31_component_fiber_infinity.py` | `claims/p5/h31/component-fiber-infinity/verify_p5_h31_component_fiber_infinity.py` |
| modular audit | `audit_p5_h31_component_fiber_infinity.py` | `claims/p5/h31/component-fiber-infinity/audit_p5_h31_component_fiber_infinity.py` |

Every source is tracked grandfathered root debt, every destination is absent,
and the classifier plus manifest contain exactly these pairs. There are no
duplicate sources or destinations, case-folded destination collisions, double
moves, overlap cycles, or package-name collisions.

| source | Git blob | raw Windows-checkout SHA-256 |
|---|---|---|
| theorem | `9de3e9dfb61cf6904f1b2169e45eb56a680ea2cf` | `957ed77d54bc89ce81f44eef7919f72024b2580f1461bbd41598c284b60dedfd` |
| audit | `565cd8e720336c6743f04a4f5864e755b9d95960` | `a4caac40aefd174490fefa2f3635d5685ac52804cd2637866cdbdd1d55ec63f1` |
| primary | `3adfdde02c912df1775fd440374a321c231c1fdf` | `7b34af4446e20e7d941894c3f2ba95d77f4a83a3fe9c64d991e4d83182fb3e27` |

## Proof-obligation ownership and exact scope

The selected triple is one complete, separable canonical-section leaf:

```text
first known pure-rank-two component
  -> first-plane Schubert divisor Delta_0(01)=0
  -> other three selected preferred Pluecker coordinates remain nonzero
  -> H,N != 0; E arbitrary; projective direction (A,D)!=(0,0)
  -> displayed canonical marked-row normal form
  -> four exhaustive symbolic parameter cases
  -> all distinguished-source orientations q=0,1,2,3
  -> exact mixed-kernel and marked-minor contradictions
  => no selected canonical marked section lifts to H31

kernel-row shifts, complete marked-basis fibre, the rest of the projective
boundary, later components, H22, P5 -> Delta3, gluing, and global stay out
```

The mandatory scope description is:

> An exact characteristic-zero obstruction on the first component's
> first-plane Schubert divisor `Delta_0(01)=0`, restricted to the locus where
> the other three selected preferred Pluecker coordinates remain nonzero, for
> the displayed canonical marked-row normal form with `H,N != 0`, `E`
> arbitrary, `(A,D)!=(0,0)`, and all four distinguished-source orientations.

The theorem's four symbolic cases (`A*E*D!=0`; `A!=0,E=0`, including its
`D=0` intersection; `A*E!=0,D=0`; and `A=0,D!=0`, split by `E`) exhaust that
domain. Its displayed equations (1)--(5), survivor strata, and determinants
(6)--(11) agree with the primary.

This is not a complete marked-basis fibre, an entire projective-boundary
closure, a generic theorem, or a whole-component theorem. The root
`P5_H31_COMPONENT_FIBRE_INFINITY_MARKED_FIBRE_OBSTRUCTION.md` family is a
strict later strengthening on the same plane locus: it adds arbitrary
kernel-row shifts and the complete marked basis. Its theorem, primary, audit,
and uniquely consumed elimination generator remain together at root for a
later four-file batch. The canonical triple stays live; it is neither
withdrawn nor superseded.

The migrated Stage 22 canonical chart-boundary theorem and P4 chart closure
remain separately owned upstream dependencies. Internal `E=0` and genuine
toric-boundary packages are sibling closures; intersections lying inside this
selected Schubert divisor are covered by this triple only for the displayed
canonical marking. The internal-`E=0` and genuine toric-boundary packages
separately provide complete marked-fibre coverage on their own scoped
intersections. No generator belongs to this triple.

No selected artifact has a curated theorem-ledger entry or formal
counterpart. Migration adds none and changes no mathematical status,
assumption, quantifier, scope, lifecycle, evidence role, or global field.

## Evidence roles, exclusions, and preserved debt

The primary reconstructs the plane normal form, permanent coefficients,
every mixed-kernel survivor case, and determinants (6)--(11) using exact
SymPy arithmetic. It is the characteristic-zero proof replay.

The audit imports neither primary nor any scientific/computational repository
helper. After repair, its sole repository import is path-only
`krenn_gu.bootstrap`. It independently implements a dynamic-programming
permanent, modular row reduction/nullspaces, and exhaustive projective scans
over `F5/F7`. It is strong modular QA downstream of the theorem-specified
normal form, not the characteristic-zero proof.

The primary output field
`remaining_known_component_geometry="Delta_1(12)*Delta_2(12)*Delta_3(03)=0"`
is temporal checkpoint metadata from before the later toric closure. Current
theorem prose accurately records that later closure. Preserve the output field
unchanged rather than treating it as current-open status; a correction is
separate owner-gated scientific-output work.

This leaf does not cover arbitrary kernel-row shifts, the full finite chart,
the nonzero chart divisor, projective-boundary specializations away from this
locus, a second or further component, component exhaustiveness, weighted
`H22`, `P5 -> Delta3`, arbitrary-order gluing, or the global conjecture. It
neither consumes nor adjudicates the pre-existing P4 `E=0` versus `D=0,a!=0`
attribution conflict or the broader first/second-component provenance
conflict; both remain unadjudicated and owner-gated.

## Mechanical repair and deterministic rewrite surface

Both moved scripts currently resolve repository paths from their root-level
parent. After the pure move:

- both install shared `krenn_gu.bootstrap` before repository use;
- `REPO_ROOT, HERE = bootstrap(__file__)` supplies stable ownership;
- theorem and sibling primary resolve from `HERE`;
- P4 chart and Stage 22 canonical dependencies resolve through `REPO_ROOT`;
  and
- generated JSON continues under ignored repository `tmp/`, not package-local
  output.

Exactly three staying primaries need operational theorem-path retargets:

1. P4 toric-boundary primary's `FIBER_BOUNDARY`;
2. complete first-plane marked-fibre successor primary's `CANONICAL`; and
3. high-coordinate primary's dependency tuple.

No staying importer or subprocess caller targets the selected primary or
audit.

The deterministic virtual post-move rewriter predicts exactly **five
Markdown links and two fenced replay commands across four Markdown files**,
with zero ambiguity and zero ledger relocation. It touches the moved theorem,
the full marked-fibre successor theorem, high-coordinate frontier, and root
README. The second pass must be a `0/0/0` fixed point.

The exact virtual post-move simulation projects these normalized-LF hashes:

- moved theorem:
  `5c8e32a2b2c8bbb745ec05ae87b18dd98d4a9ede52cfd538e3056d7dd93b28d1`;
- staying complete marked-fibre successor theorem:
  `e90db08f7415ae3b80da36fd0425ed84441fdf47a8baad19f16d717ca2acdf47`;
- high-coordinate frontier:
  `8da5855890f720c3835284891bf9ba91828b36a0c7fec7e676d316810f65f13b`;
  and
- root README:
  `3ad0a357fa8df313846c4893390c4925749d6ec3f73c64d9167bede5f079ceac`.

Exactly four existing ledger hashes refresh mechanically:

- verified high-coordinate frontier -> `8da5855890f720c3`; and
- the three root-README entries -> `3ad0a357fa8df313`, retaining statuses
  `open`, `verified_generic`, and `partial`.

All other ledger fields remain fixed.

Navigation adds a separately labelled canonical first-plane Schubert-infinity
section to `claims/p5/h31/README.md`, changes the scoped-exception count from
six to seven, records the Stage 23 batch/mapping hash, and says explicitly
that the complete marked-fibre successor is excluded from this batch and
remains together at root as grandfathered debt pending its separately reviewed
four-file batch. Parent P5 navigation mirrors that scope and changes the H31
package count from 29 to 30.

## Projected transitions

| measure | before | after |
|---|---:|---:|
| manifest `moved` | 398 | 401 |
| manifest `proposed_high_confidence` | 242 | 242 |
| manifest `review_required` | 1,375 | 1,372 |
| moved-only manifest root projection | 1,974 | 1,971 |
| high-confidence manifest root projection | 1,732 | 1,729 |
| all-classified manifest root projection | 357 | 357 |
| measured root files | 1,966 | 1,963 |
| measured root directories | 9 | 9 |
| measured root entries | 1,975 | 1,972 |
| grandfathered root debt | 1,959 | 1,956 |
| new root debt | 0 | 0 |
| enforceable retired/provenance paths | 398 | 401 |

The move creates one approved H31 package directory, increasing the count
from 29 to 30, and changes no root baseline or end-state allowlist.

## Exact baseline replay

The selected bytes at this baseline are unchanged from the Stage 22 serial
acceptance matrix. The primary passed from repository root and a fresh foreign
CWD in 4.054 and 3.023 seconds, with byte-identical stdout/generated JSON,
empty stderr, and no foreign-directory output. It reported:

- `verified: true`, field `C`, divisor `Delta_0(01)=0`;
- base-open values `[H,N]`, direction `(A,D)!=(0,0)`, and six marked
  certificates;
- all four distinguished orientations excluded true; and
- `H31_excluded`, `P5_to_Delta3_resolved`, and global resolution false.

The no-primary-import audit passed once from root in 96.680 seconds, with
empty stderr and:

- `F5`: 1,920 boundary parameter points, 17,408 projective binary extensions,
  all 17,408 marked-injective, and zero successful lifts;
- `F7`: 12,096 / 160,704 / 160,704 / 0; and
- zero ambient local maps or Grassmannians, with explicit finite-field-audit /
  characteristic-zero-theorem scope.

Both generated JSON files were ignored under repository `tmp/`; tracked bytes
remained clean.

## Post-move acceptance matrix

Use
`uv run --quiet --python 3.13 --with sympy --with python-sat python`,
strictly serial, and preserve every first rc=0, empty-stderr, valid-JSON
output before assertions. A wrapper or post-parse error must not launch an
automatic rerun or become theorem evidence.

The complete recursive path/hash closure is **11 unique executables and 16
invocations**.

Run these five from both repository root and a fresh foreign working
directory by absolute path:

1. moved canonical primary;
2. moved canonical audit;
3. P4 toric-boundary primary;
4. complete first-plane marked-fibre successor primary; and
5. high-coordinate primary.

Run these six once from repository root:

6. P4 toric-boundary audit;
7. complete first-plane marked-fibre successor audit;
8. high-coordinate audit;
9. H22 actual mask-6 independent audit;
10. component-19 `phi=+/-1` derivation; and
11. component-19 `qphi=-1` axes derivation.

The complete marked-fibre primary and component-19 `phi=+/-1` derivation use
the established WSL/Singular route on Windows and must not overlap. Exactly
six matrix executables write only ignored repository-`tmp/` JSON and five are
stdout-only; none writes a tracked output.

Require:

- four root/foreign pairs byte-identical: moved canonical primary, moved
  canonical audit, P4 toric-boundary primary, and high-coordinate primary;
- complete first-plane marked-fibre successor root/foreign objects identical
  after removing only their measured `elapsed_seconds` field;
- all five pairs carry current source/theorem/dependency hashes and leave
  their foreign directories empty;
- primary fields exactly `C`, `Delta_0(01)=0`, `[H,N]`, `(A,D)!=(0,0)`, six
  certificates, all four orientations true, and H31/P5/global false;
- audit `independent_of_primary_imports`, exact `F5/F7` counts, all four
  orientations true, finite-field QA scope, and zero lifts;
- P4 toric boundary stays 44 divisor/orientation pairs split 21 gate /23
  all-rank, with H31/P5/global false;
- full successor keeps the complete marked fibre true while its internal-E0,
  additional-component, and global fields remain false; the internal-E0 field
  concerns the separate whole divisor and does not deny the included
  first-plane intersection;
- high-coordinate census stays 6,495 / 1,680 / 1,170 / 510 and P5/global
  remain false;
- mask-6 remains `VERIFIED`, both component-19 derivations remain
  `CANDIDATE`, and every global field remains false where present;
- every theorem, primary, source, and dependency hash matches current bytes;
  and
- all generated files remain ignored, foreign directories remain empty, and
  no tracked output drifts.

Both moved modules must pass isolated foreign-CWD import probes. Confirm
targeted Ruff and byte compilation, rewriter fixed point, the index-complete
validation floor, exact-head CI, and fresh semantic plus mechanical final
referees before a normal head-guarded merge.

## Stop boundary

Stage 23 stops at the displayed canonical marked-row sections on the
first-plane Schubert-infinity locus. The full marked-basis fibre, the rest of
the projective boundary, the finite chart, internal and toric sibling
obligations, later components, component exhaustiveness, weighted `H22`,
`P5 -> Delta3`, local-to-global gluing, and global resolution remain separate.

The global Krenn-Gu conjecture remains **UNRESOLVED**.
