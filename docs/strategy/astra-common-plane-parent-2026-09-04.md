# Complete common-plane source parent attempt

Coordination and integration record, not a theorem-status authority.
Global Krenn--Gu UNRESOLVED.
Base origin/main cae8cb2975613039fcd5dde2d9ae90fdad1d3577.

The original global goal remains all even n>=6 and d>=3. The concrete
load-bearing parent in this run is exclusion of the ENTIRE eight-vertex
paired-root configuration below, not another normal-support subcase.

Let W over C have full tensor Delta_(8,3), with roots r,s and outsiders
A0,A1,A2,B0,B1,B2. Assume Q=W_rs, L_i=W_rAi, M_j=W_sBj are invertible,
W_sAi=alpha_i Eii and W_rBj=beta_j Ejj with nonzero scalars, and all six
physical AA/BB edges are nonzero matrix units. AB blocks are arbitrary.
Prove that no such source exists.

The new main theorem N8R2S supplies at least one common inactive image
plane and its full opposite-shore zero-hafnian/corrected-cofactor source.
Exchange the shores to name the common plane U=L_i(ker e_i), with normal
n!=0. The exact child cover is support(n)=3,2,1, and every opposite-shore
plane configuration is retained. The previous turn supplied a candidate
exclusion for support3, pending fresh independent audit. The support2
and support1 cases were the remaining research tasks at launch. Do not publish a
separate support-profile sibling instead of attacking this parent.

The named downstream consumer is exclusion of adjacent degree-four
vertices in the invertible-edge graph of a hypothetical n8/max-r2 witness.
N8R2S proves the root-configuration supply. Its additional AA/BB unit
condition follows from maximum-r2: two invertible edges of a triangle
force the third edge to be a nonzero matrix unit. In the torus-zero
hypersurface of one invertible edge, the cross-product obstruction has
rank at most two, so a coordinate projection vanishes identically; the
other invertible leg then forces a single-column third leg. A noncoordinate
coefficient row of that leg has a torus kernel and permits a third root,
so the third leg is a matrix unit. The same argument rules out a zero
third leg. This conditional consumer does not assert that adjacent
degree-four vertices exist in every witness.

All physical cofactor blocks must be retained. Scalar one-shore hafnian
factorizations and the adjugate-normal projection alone are not sufficient:
the previous exact control satisfies those, pure amplitudes, all killers,
and maximum-r2, but violates the full matrix source at first normal order.
Likewise H restricted to a shore being zero is not full H=0. A guarded
dead-edge deletion is legal only for the FULL complementary tensor.

Success is a complete parent implication or an exact obstruction to a
proposed parent mechanism that leaves a sharper necessary lemma. A proof
candidate receives independent exact-text audit before promotion; an
apparent exact GHZ witness receives dedicated validation. No computational
timeout or generic sample can close this support cover. Root alone owns
integration; research notes remain scratch until a reviewed parent delta.

## Complete parent outcome and proof distance

The [parent exclusion](../../claims/finite/n08/TWO_ROOT_DIAGONAL_LEG_SOURCE_EXCLUSION_THEOREM.md)
closes the entire stated configuration, with every opposite-plane case
and every normal support retained. The three cases are packaged as proof
leaves of one theorem, not separate research publications.

The common mechanism is stronger than the initial support-three candidate.
If the BB cofactor vanished, the full corrected source would force the AB
cofactor to vanish and leave at most one surviving inactive AA edge. Set
all three inactive root columns to one generic vector, then choose one
fully supported annihilator of that vector. Use those same values for all
three normal derivatives. This recovers the missing target colours even
when the actual common-plane normal has zero coordinates, and excludes
both possible AA situations with the AB-normal correction retained.

Each normal-support case then forces that same BB cofactor to vanish.
The support-three hollow-kernel case is immediate. The coordinate case
uses the retained free cofactor and a corrected matrix column. The
two-coordinate case uses a legal weighted two-column combination, covers
its zero and nonzero scalar parameters, and resolves its last branch by
the complete missing-colour mixed-normal equation. The initially tempting
full single-neighbour shortcut was rejected: inactive AB containment does
not control its normal rows. The final proof explicitly keeps those rows.

The live delta N8R2E is closure of this parent implication and its named
consumer: adjacent degree-four vertices cannot occur in the invertible-edge
graph of an n=8/max-r=2 witness. The two-invertible-path unit proof supplies
the additional AA/BB hypotheses on that same graph. The N8R2S theorem for
arbitrary outside blocks remains a proved reduction; its broader boundary
is not silently upgraded to an exclusion.

No existence theorem for this root configuration is supplied. Other
invertible components, all-rank-at-most-two source graphs, other root
orders, arbitrary even n, and the original global conjecture remain open.
The next research unit must address that wider source supply or a new
global-facing parent, rather than refine another already-closed normal case.
