# Cancellation-consistency handoff — stopped by user

## Stop and status

The user explicitly requested wind-down on 2026-09-13 at about 06:32 UTC
(September 12 locally). **Do not resume research without a new request.**
The conjecture remains **UNRESOLVED**. There is no claimed full eight-vertex
exclusion, all-order weighted Bogdanov proof, or exact counterexample.

The live account tool reported **8 percent used** in the available core weekly
window at wind-down, below the user's earlier 80-percent-used ceiling. The
other core window was unavailable, not zero. Checks during the run rose from
3 to 8 percent. This is an account-wide meter, not task token accounting;
external OMP usage was not inferred from it. No reset credit was consumed and
no recurring automation was created. The explicit stop supersedes the earlier
persistence request. The conjecture objective has not been marked achieved.

All owned research processes have exited. Only checkpoint validation followed
the stop. The older, unrelated OMP process tree was left untouched. Nothing was
pushed, published, deployed, or submitted as a PR; no memories were modified.

## Checkout and entry points

Active isolated worktree: `C:\w\kg-recursive-cancellation-20260912`.
Branch: `codex/recursive-cancellation-consistency-20260912`.
Upstream base: `dc4e7c9ef97e3d4d0d47e1d3403e61d66a498ddc` (PR 346),
confirmed unchanged by a read-only remote check at 05:54 UTC.
The checkpoint before this wind-down was `a303676d`.
Use `git log -1` for the final handoff commit; do not switch to another checkout.

The original OneDrive repository and its protected `test_patch_probe_root.txt`
were not edited. No other worker's branch or process was reset or stopped.

Read the repository README/AGENTS contract, then these scoped records:

- [Continuation and diagonal evidence](cancellation-consistency-continuation-2026-09-13.md).
- [Full-model parent attempt](full-model-recursive-cancellation-attempt-2026-09-13.md).
- [Source mechanisms and projection evidence](source-cancellation-mechanisms-2026-09-13.md).
- [Original recursive experiment and bridge](recursive-cancellation-consistency-experiment-2026-09-12.md).

The live frontier is deliberately unchanged: these are experimental packages
and conditional source obstructions, not promoted parent-branch closures.

## What was obtained

### 1. A locally replayable ten-vertex all-diagonal RZP exclusion

The truthful recursive hafnian support model includes both directions of the
singleton rule: a nonzero result needs a surviving Laplace term, and exactly
one surviving term cannot produce zero. Actual complex all-diagonal witnesses
map into this necessary model. It is stronger than AP', not a redefinition of it.

Thirteen exhaustive matching-normalized n=10 branches have frozen instances
and binary DRAT proofs. After Fable's reproducibility criticisms, all thirteen
instances were regenerated byte-for-byte and all thirteen proofs replayed,
together with three semantic positive/negative checker controls. The replay
took 560.844 seconds. The constructive matching-cover test covers all 945
third matchings in the shared-matching case.

The evidence manifest is
[recursive-cancellation-consistency-evidence-2026-09-12.json](recursive-cancellation-consistency-evidence-2026-09-12.json).
Replay entry point:
[replay_recursive_hafnian_drat.py](../../tools/explore/replay_recursive_hafnian_drat.py).
Local receipt: `tmp/recursive-hafnian-rzp-replay-v2/replay.json`, SHA-256
`3714e6bd534a2043d403834c43dcc0961ca9ba4ab7ca2b12b3e0eaa3e694f6e8`.

The roughly 500 MB proof payload is local and ignored, not publicly archived.
This does not supply a DRAT proof for WB2's original weaker AP' encoding, and
it is not an all-order result. Promotion still needs the stated package/audit
gates; do not rewrite the frontier merely from the summary here.

### 2. Reusable cancellation mechanisms, with exact guards

The work now retains original physical entries and coloured subset hafnians,
rather than choosing unrelated local hafnian tensors. It includes:

- truthful full-model recursive support constraints and necessary column killers;
- signed two-term circuits with **integer** exponent relations;
- ratio-path closure, including the live-third-term case;
- transport of binomial identities through larger cancellation sums;
- source-forced cofactor peeling, rather than freely assuming cofactor bits;
- small RUP proof cores projecting conditional algebraic contradictions back
  to constraints on physical entry supports.

Every cut retains the complete-fibre escape: a formerly zero cancellation term
may become nonzero and invalidate the cut's premises. Passing these tests is
never interpreted as a weighted realization.

The four-set coverage lemma also explains a dense-private all-diagonal family
uniformly in the order. Its hypotheses remain genuine density/coverage
assumptions; it does not exclude arbitrary weighted Bogdanov witnesses.
See [four-vertex coverage](four-vertex-cofactor-coverage-2026-09-13.md).

### 3. The physical cuts do not cover eight vertices

Three compact independently replayed source-core packets yield physical cuts
with 54, 38, and 32 literals. Their cores respectively contain:

| physical literals | recursive/target premises | total core clauses | RUP additions |
|---:|---:|---:|---:|
| 54 | 209 | 264 | 3 |
| 38 | 161 | 200 | 1 |
| 32 | 157 | 190 | 1 |

Each core uses one separately replayed algebraic clause. None relies on killer,
ratio, root-canonicalization, or preceding learned clauses. Native DRAT replay
and the separate small RUP checker both passed. The third packet is preserved
at [recursive_source_quotient_n8_third_core.json](../../tests/fixtures/recursive_source_quotient_n8_third_core.json),
SHA-256 `4d325e3ad2e1c8408ee72928a30c853da74918039817d338f316b4d143a62774`
for the tracked LF bytes.
Its native CNF/proof hashes are
`f1bea07c1f9ca01976a67cef8d8e8f4a14c76096401b4760962ede0b1430b8d4`
and `fd094b68ead5083b65b5f38979c28b4c219e1566319f3db5bccc2b59e1522879`.

The parent coverage test added **all** 8! times 3! vertex/global-colour
relabelings of all three cuts (241,920 per cut, possible duplicates retained).
The resulting full necessary model has 600,063 variables and 4,406,244 clauses.
It is **SAT**, and every clause was checked against the returned assignment.
The run completed in 40.200 seconds and has 136 nonzero physical entries.

Latest model: `tmp/full-n8-three-source-cut-full-orbits/model.json`, SHA-256
`b1e97248a93b8dd38f9cda680fb7a1d9f041dac1cce8be1bf86ac2eb06f3af02`.
The corresponding summary and CNF are in the same directory. This is a Boolean
countermodel to coverage by these cuts, **not** a graph counterexample.
Its particular cofactor assignment is already rejected by a unit-quotient
binomial kernel; that does not yet exclude all assignments on its physical support.

Simple local colour flips also fail to embed a cut. A broader small SAT search
for joint vertex/local-colour embeddings, preserving every target premise
actually used by the proof, returned UNSAT for each packet. Those embedding
statuses have no independent proof traces and are diagnostic only. Arbitrary
local colour permutations were never assumed to preserve GHZ. These probes are
`tmp/full-n8-local-colour-transport.json` and `tmp/full-n8-source-embedding.json`.

### 4. Newest small story: two larger sums, not another odd binomial circuit

A six-vertex 51-entry support assignment passes the existing recursive
binomial, ratio, full-word quotient, and individual recursive quotient tests.
Exact linear combinations of the larger recursive sums give a shorter
contradiction. Ten original binomial identities transport two complete
three-term recursive equations to

```text
-A - B + C = 0,
-A + B - C = 0,
```

where A, B, and C are nonzero monomials. Adding gives `-2A=0`, impossible over C.
With the encoder's physical-entry indices,
`A=g1*g24*g94`, `B=g11*g24*g64`, `C=g14*g21*g64`.
The two source coefficient labels are `(0124;0010)` and `(0145;0002)`.
The claim uses their complete Laplace identities, including the result
hafnian term when it is nonzero; they are not silently treated as zero hafnians.

Discovery took 0.215 seconds and retained a six-node DAG. A separate replay
reconstructed the original fibres directly, checked all integer transports,
and evaluated the Laurent operations to the final constant 1. The new reusable
[row-space replay](../../src/krenn_gu/recursive_tensor_row_space.py) performs no
quotient discovery or polynomial reduction. Its compact
[regression fixture](../../tests/fixtures/recursive_row_space_n6.json) has
SHA-256 `73602746e0631f2938591d81e21692b4632fdda0a226b841c6964e67a16ca19a`.
The guard has 60 literals and is **conditional on cofactor support**, not yet
a physical-support projection. This is not new six-vertex conjecture coverage.

The corresponding exploratory discovery/replay scripts remain under ignored
`tmp/probe_recursive_quotient_row_space.py`, `tmp/pack_recursive_row_space.py`,
and `tmp/replay_recursive_quotient_row_space.py`. The earlier independent
740-node localized polynomial DAG on this physical source also remains local.
No external reviewer has audited the new row-space implementation yet.

### 5. Linear Laplace incidence: sound strengthening, no frontier improvement yet

For one coefficient write `t_uv=W_uv*h_(A-uv)`. The shared equations are
`sum_(u!=v) t_uv=h_A` for every v. Consequently every integer potential y gives

```text
sum_(uv) (y_u+y_v)t_uv = (sum_v y_v)h_A.
```

A single surviving term is impossible. This can detect contradictions missed
by checking each vertex expansion separately. The
[incidence module](../../src/krenn_gu/laplace_incidence_support.py) supplies
displayed signed-potential certificates and sound support clauses. Its graph
search agrees with independent exact row reduction on every graph through
five vertices; actual integer hafnian controls and malformed certificates
are tested.

A sharp **local Boolean** control on six vertices sets every scalar edge
nonzero, the full hafnian nonzero, and the active full cofactor graph to K2,4.
It passes the recursive support rules and has no usable binomial relations.
The two bipartite shores would nevertheless give `2h=4h`, a contradiction.
This control does not satisfy the global GHZ constraints and is not a witness.

The existing six- and eight-vertex full-model survivors pass this incidence
test. A fresh n=12 all-diagonal shared-matching branch with ratio closure,
four-cofactor coverage, and the new clauses has 118,209 variables and 2,510,205
clauses. Its 330.029-second bounded run timed out: **UNKNOWN**, no proof trace.
Do not extend this deadline without a new mechanism. The retained instance is
`tmp/n12-shared-incidence-consistency/instance.cnf`; the wrapper receipt is under
`.research-runs/n12-shared-incidence-consistency/20260913T061838Z-10696/`.

Further negative scouts: all 3,915 supported five-neighbour zero-star patches
and 1,026 maximal coloured-star assignments (133 distinct linear support
patterns) left the latest n8 model intact. The latter checked the normalized
four-hafnian linear systems exactly. These are diagnostic controls, not
published theorems or proofs of source realizability.

## What Fable 5.1 said, and the assessment

All consultations used the installed OMP CLI with `anthropic/claude-fable-5-1`,
as single bounded read-only reviewers, not a council. The OMP skill supplied
scope, provenance, resource, and read-only safeguards. Raw reports remain
untrusted analysis and were not committed as mathematical proof.

1. The first completed review found no blocking mathematical defect but
   identified reproducibility gaps in the finite certificate package. The
   typed cover, exact regeneration, checker controls, and provenance were repaired.
2. The second completed review found no blocker in the reviewed parity/replay
   checkpoint and identified missing ratio-path closure. That suggestion was
   implemented and checked with both live- and dead-third-term controls.
3. The third completed review found no blocker in the earlier full-word
   encoder/quotient delta and hand-checked three certificates without executing
   code. Its most useful suggestion was to force cofactor information from
   physical source data. Peeling and subsequent source-core projection pursued
   that suggestion. Inaccurate side census/gauge remarks were not imported.
4. A fourth review of `e45d289d` reached its 12-minute CLI deadline after 721.127
   seconds and returned **no final verdict**. It is not approval of the newer
   quotient/core code, and no prior review covers the incidence/row-space delta.

The productive part of Fable's advice was the emphasis on path closure and
common-source constraints. The new two-sum derivation supports the same
assessment: consistent individual binomials and individual cancellation sums
can still be mutually incompatible. The hard missing result remains forcing
such an obstruction in every witness, or covering a complete physical parent.
This run does not establish that the mechanism will do so at all orders.

## Validation and evidence boundaries

At wind-down the 16 focused quotient/core/incidence/row-space tests passed,
including malformed-transport, incomplete-fibre, wrong-sign, invalid-division,
forward-reference, and guard mutation controls. The source-core fixtures replay
without SAT discovery; the row-space test explicitly disables unit-quotient
discovery during replay. The earlier checkpoint already had 63 focused tests
and the repository's migration/lattice validation floor green. The 205
migration/lattice tests were also freshly rerun at wind-down and all passed.

Final checkpoint commands are the index-complete repository floor:

```text
git add -A
python check_hygiene.py
python -m unittest -v tests.test_migration_tools
python -m unittest -v tests.test_fourteen_vertex_cycle_cover_lattice
python tools/migration/rewrite_links.py
git diff --exit-code
git diff --cached --check
```

The final task message reports the completed validation outcome. No claim is
made that every repository certificate or Lean theorem was freshly replayed.
Do not call the newer modules independently externally reviewed or formally
verified. The public archive and promotion gates for the n10 evidence remain.

## If the user later resumes

First inspect this branch's final commit, process state, and live usage. Do not
restart the 500 MB proof replay, repeated n12 timeouts, or Fable deadline attempt
just because their artifacts exist. Do not infer permission to publish.

The sharpest next research obligation is to combine larger source-coupled
cancellation sums and project the resulting contradiction to physical supports,
then test an actual parent cover. The two-equation derivation is a small exact
control for that mechanism. Preserve the separation between (a) all-order
weighted Bogdanov and (b) the full n8 residual root branches r=2,3; neither is
closed. Never infer invertible-edge supply from r>=2 or replace a common outside
graph by independently chosen cofactor tensors.

Before promoting the newest algebraic delta, obtain a genuinely independent
review of its witness-to-guard bridge and replay semantics. A uniform occurrence
theorem or a source-preserving reduction remains the global-facing missing edge.
Another isolated cut is not, by itself, that edge.
