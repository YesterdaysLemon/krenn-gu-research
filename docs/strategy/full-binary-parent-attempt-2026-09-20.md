# Full binary source parent: resumed attempt

## Authorization and starting point

The owner explicitly resumed research on 2026-09-20 after the completed
2026-09-14 publication and stop. This attempt starts from origin/main
ab250829b94af779a72e4a25f57b904e8b0d8f06. The global Krenn--Gu conjecture
remains **UNRESOLVED**. Earlier accepted claims keep their exact scope.

## Exact parent, supply, and consumer

For every finite k >= 2 over C, use exactly the common-star arrays in
[PSCS](../../claims/arbitrary-order/PROTECTED_SCAFFOLD_COMMON_STAR_COMPONENT_TARGET_OBSTRUCTION.md):
protected unit K4 components, one unequal endpoint-color label per edge
of a simple component graph, and two nonzero crossing factors per gadget.
The parent FB asserts that no such array satisfies S1, both same-color
and distinct-color S2, and the full binary component-constant target
for each of the three unordered color pairs.

A full CSQ4 witness supplies this antecedent. The named downstream consumer
is exclusion of the common-star branch of CSQ4. No reduction from arbitrary
protected arrays or arbitrary Krenn--Gu witnesses to this family is assumed.

For a fixed ordered pair a,b, set A_ij and B_ij to the actual center and
leaf factors for label (a at i,b at j), and zero otherwise. Their supports
coincide, their diagonals vanish, and A_ij A_ji = 0. For every cut S,

```
F_ab(S) = sum_(I subset S, J subset S^c, |I|=|J|)
                  per(A[I,J]) per(B[I,J]).
```

The empty term is one. Full binary targets mean F(empty)=F(V)=1 and
F(S)=0 for every proper nonempty S. Cofactors and exterior terms remain
the actual polynomials in these factors, never independently chosen data.

## Mechanisms and sharp controls

S1 plus both S2 already exclude all singleton ports. Thus a theorem that
the full binary identity forces some state to have degree one would prove
FB. This is a sufficient proposed implication, not an established reduction
or equivalence. A directed cycle with gadget products -1 satisfies the
binary target and has singleton ports, so any proof must allow that case.

The PSRA and PSBS exact controls satisfy S1, both S2 and full physical U4,
but fail explicit binary words. Neither low-order rows nor the uniform
Bernoulli average may replace the complete cut identity. The complete-
biclique consumer in PSCS survives, but its proposed local supplies are
refuted. The present synthesis must use additional full-cut information.

Success is a proof of FB, an exact countercontrol with an exhibited
remaining ternary failure, or a precise exact obstruction to the named
degree-one route that leaves a sharper load-bearing next lemma. An apparent
full GHZ witness triggers dedicated independent validation. A numerical
root or unsuccessful bounded search is neither a proof nor a counterexample.

## Work allocation and limits

The coordinator attacks the complete cut identity and integrates accepted
results. A bounded independent worker tests the proposed degree-one
implication directly; a second derives consequences of the full identity
with exterior terms retained. Research workers do not create sibling PRs
or promote claims. Independent exact review follows a candidate delta.

Computations expected to exceed 60 seconds use the bounded runner, with
an initial ceiling of 900 seconds and 8192 MiB each, and at most three
research computations concurrently. No GPU workload is presently justified.
Every launcher owns its processes through exit. No change to the live
frontier is claimed by this preregistration alone.

## Parent attempt outcome

The full-cut investigation produced
[induced-cycle resource localization](../../claims/arbitrary-order/PROTECTED_SCAFFOLD_FULL_BINARY_CYCLE_LOCALIZATION.md).
Every proper induced directed cycle must lie, with both color states of
every cycle component, inside one connected binary resource. A subset
induction using the complete target eliminates exterior terms exactly.
When no resource contains both states of a physical component, a full
binary target forces a directed Hamilton cycle with all products -1.
PSCS's three-color no-singleton-port theorem then excludes that topology.
This removes completeness from the earlier complete-biclique consumer.

The remaining necessary system is now explicit. For an internal cycle C,
remove its rows from its one resource, leaving actual exterior rows U.
Every proper column subset T of C has source (-1)^|T| times its incoming
cycle-product, while the source on all of C must vanish. Both orientations
and all three color pairs must obey this together with the unchanged FB
antecedent. These constraints are necessary, not an equivalence with FB.

An exact five-component control realizes this entire one-sided cycle face
over Q(sqrt(-3)), including the vanishing full-cycle source. Its outside
singleton word has amplitude one. The construction extends to every cycle
length r >= 3 on 2r-1 components: exterior row weights have elementary
symmetric functions s^m/(m!)^2, with (1+s)^r=s^r. Thus the top defect cannot
be discarded or disproved using that face alone, at any cycle length.
Targets changing the exterior and the third-color coupling are essential
for the next parent step.

The parallel bounded degree-one investigation found neither a proof nor an
exact full-target counterexample. Its Paley-11 calculations and circulant
numerical search remain scratch exploration, not promoted claims. Numerical
solutions approaching zero support entries do not certify feasibility or
exclusion. The subprocess exited successfully within its declared bound.

The proof-topology delta is a new full-cut reduction and a stronger
conditional consumer, together with a sharp no-go for a face-only closure.
It closes neither FB nor CSQ4 and supplies no arbitrary-witness normal form.

## Two-sided synthesis and exact obstruction

The next proposed synthesis added the opposite binary cycle face and every
binary singleton/same-color double target. The
[two-sided cycle control](../../claims/arbitrary-order/PROTECTED_SCAFFOLD_TWO_SIDED_BINARY_CYCLE_NO_GO.md)
refutes the assertion that these constraints are already inconsistent.
It uses nine components and 27 actual shared-factor gadgets. Both localized
three-cycle systems, both full cycle faces, and all 90 cuts of sizes
1,2,7,8 hold exactly over an explicitly nondegenerate algebraic tower.
Independent triangular reduction and literal 36-vertex matching recursion
agree with the permanent-product source.

The source for the exterior-changing word 001011111 is nevertheless
(r^2+1)/(12r), nonzero for every admitted parameter choice. There are no
color-2 gadgets, so the third-color singleton and distinct-color S2 supply
is absent. This is not a control for the full FB antecedent. It pinpoints
the next load-bearing step: use further full cuts that change exterior
components, or couple the internal defect to the third color. A proof
must retain unequal center/leaf matchings; preserving all gadget products
while changing their two factors can break the double targets.

This second delta is an exact no-go for a named parent synthesis, not an
additional sibling support census. Its independent audit is separate-agent
review in this session, not a separate-human or Lean proof. The original
PSCL theorem and reviewed files remain unchanged.

## Final synthesis and stopping point

The last analytic step derives the
[exterior-coupling identities](full-binary-exterior-coupling-obligation.md).
Changing an extra cycle neighbor adds a row, and may also remove a column
from the same resource. Exact Laplace expansion exposes a nonzero cycle
term and the actual mixed-permanent remainder that would have to cancel it.
The distinct-color S2 equation substitutes a third-color contraction for
the edge product, but no proved identity links that contraction to the
whole binary remainder. Shortest-cycle minimality does not remove those
exterior entries. The proposed paired exterior-coupling lemma is sufficient
for FB and explicitly open.

This is the impasse reached by the current bounded parent attempt. No
three-color countercontrol to the new lemma or proof of its incompatibility
was found. The accepted localization and the two exact mechanism no-go
results form a completed, reviewable chunk; the repository owner's request
is to publish that chunk and then stop. No additional lane is authorized
by the persistent goal after that stop. The
[review handoff](full-binary-cycle-review-2026-09-20.md) states exactly
what another model should audit and which implication remains unproved.

## Bounded external-source inspection

Searches on 2026-09-20 used "GHZ weighted graphs dimension two perfect
matchings characterization cancellation", "permanent complementary principal
cuts binary GHZ graph matching degree", and the two paper-title/weighted-
dimension follow-ups. Search snippets were only leads.

The [Chandran--Gajjala abstract](https://arxiv.org/abs/2202.05562)
describes PMValid colorings in which every perfect matching is monochromatic.
Only its metadata and abstract were inspected for this use. That does not
supply a theorem about the destructive cancellations in the present source.
The [Chandran--Gajjala--Illickan paper](https://doi.org/10.4230/LIPIcs.MFCS.2024.41)
was inspected at Section 1.3, Theorems 9--12, and the Theorem 15 statement
in Section 2. These concern connectivity and sparse-graph reductions, not
the required full binary source implication. No result from either source
is imported into the new proof. This bounded inspection is not an exhaustive
novelty assessment. Existing source records retain their earlier usage scopes.
