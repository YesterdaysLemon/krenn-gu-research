# Astra research laboratory continuation, 2026-09-04

Status: active coordinated research. Global Krenn--Gu status **UNRESOLVED**.
Initial integration base: `b905f38038e8087c2fb0c67841af90c16b899218`.
The low-rank completion starts from merged
`73955ca0673d1c0ee41e016d7c9b0020704d3b49`.
The owner explicitly authorized continued research until resolution,
Astra-only low/medium/high agents, and reviewed PR creation and merges.

## Active global-facing parent

For every complex ternary witness W at even n >= 6, let r(W) be the
maximum cardinality of a fully supported pairwise zero-coupled root
configuration, maximizing over vertex sets and vectors. Attempt to prove

    r(W) >= n/2 - 1,

equivalently maximal-root surplus s=n-2r is at most two. The alternative
success is an exact counterexample to the original witness equations,
independently validated; countermodels to weaker premises remain controls.
This is pointwise, with no degree, genericity, or coefficient restrictions.

The accepted maximum-root saturation theorem supplies r>=1 and its
r=1 matrix-unit / r>=2 complete physical-layer split. Its full source
identity and the uncontracted companion expansion are the upstream
mathematical supply. The just-reviewed scaffold obstruction proves that
pure amplitudes, annihilation tests, the complete majority ideal hierarchy,
and maximal-root blockers alone cannot establish this parent. Every new
attack must retain actual mixed source equations.

Named downstream consumers: the s=0 weighted-permanent restriction and
the s=2 complete-deck/same-pair supply theorems (GLS2--GLS4), followed by
the explicitly still-open source-to-detector attachment interface. Proving
this parent would remove all higher-surplus witness branches, including
the r=1 branch at n>=6. At n=6 the numerical inequality permits r=2,
but the accepted five-outside-mode bound excludes it; the parent would
therefore itself reprove the six-vertex case.
It would NOT itself finish permanent exclusion or detector/gluing.

## Work allocation and reading requirement

Workers reconstruct the owning hypotheses, proofs, failures, and consumers
for their method before proposing new mathematics. The coordinator reads
the central source proofs and synthesizes the workers' dependency reports.
Methods: overlapping full source identities; nonlinear response/cofactor
reconstruction; torus-root incidence geometry; matrix-unit r=1 cancellation;
exact exceptional scaffold source equations; and adversarial proof review.
These are attacks on this one parent or a necessary branch of its exhaustive
root-order split, not independent theorem-collection projects.

Use only gpt-6-astra at low, medium, or high reasoning effort. Research
workers write assigned ignored scratch files, own their processes, and
use bounded runners for work exceeding sixty seconds. Initially permit
one computation per worker, at most 2 GB and 180 seconds without a revised
coordinator budget. The coordinator is the sole integration and merge owner.
Preserve all other worktrees and the main checkout's inherited files.

Any exact witness candidate triggers a dedicated validation task. Any
candidate proof triggers independent adversarial consolidation. A local
no-go is a checkpoint, not termination of the persistent resolution goal.

## Reviewed advance and current allocation

The initial scaffold obstruction was merged as
[PR 339](https://github.com/YesterdaysLemon/krenn-gu-research/pull/339)
after local and CI validation. It is not a global exclusion.

The next accepted mathematical result, merged as
[PR 340](https://github.com/YesterdaysLemon/krenn-gu-research/pull/340), is the
[eight-vertex common-coordinate source exclusion](../../claims/finite/n08/TWO_ROOT_COMMON_COORDINATE_EXCLUSION_THEOREM.md),
with [fresh independent consolidation](../audits/TWO_ROOT_COMMON_COORDINATE_EXCLUSION_REVIEW_2026-09-04.md).
The source supplies three separately labelled pure four-cofactors, allowing
partner stresses and nonlinear six-hafnian compatibility to give an actual
contradiction. The combined proof closes every same-pair rank-one/rank-two
pattern and all zero individual legs within the stated child. It covers
every root block of rank at least two, including coordinate-kernel
boundaries and dependent-channel four-cycles. No generic
parameter assumption or computational UNSAT assertion is used.

The [completion theorem](../../claims/finite/n08/TWO_ROOT_COMMON_COORDINATE_COMPLETION_THEOREM.md)
then removes the entire root-rank boundary within that same child. Both
nonmonomial rank-one factor types have analytic exclusions. For Q=0, the
proof retains the initially unobserved inactive hafnian and gives an
exhaustive orientation cover. Its last pure-(3,3) branch re-roots through
the already accepted high-rank theorem, then maps to a finite necessary
support instance. Independent reconstruction and a separate exact checker
accept the supplied certificate's 6,860 binary branches and 6,861 conflict
leaves. No native UNSAT result or failed runtime is treated as a proof.

This is load-bearing for the declared parent because n=8, maximum r=2
is one of its higher-surplus branches. The complete common-coordinate
child is now excluded, with no root-rank hypothesis. It does not establish
that the incidence hypothesis is exhaustive. The active next implications are:

- On the n=8 maximum-r=2 branch, use the full source to supply a pair with
  common-coordinate physical incidences, allowing re-rooting, or directly
  exclude the remaining incidence types. There is no need to force a
  particular root rank inside the now-closed common-coordinate child.
- The r=1 complete matrix-unit branch and arbitrary-order high-surplus
  branches remain part of the parent, not consequences of this finite leaf.

Read-set reports and failed mechanisms are retained in task-owned ignored
scratch. In particular, a full-source Laurent identity excludes the tested
four-group monochromatic support family at every group size at least two;
a separate exact K64 support control refutes a universal fixed two-deletion
repair cover. Neither is an arbitrary-support r=1 exclusion. Source
criticality, a single scalar contraction, transversal acyclicity, and
low-deviation shells were each tested and found insufficient in their
proposed weaker forms. They are not being promoted as substitutes for the
active full-source obligations.
