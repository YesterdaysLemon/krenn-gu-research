# Maximum-root surplus-two supply/target node: internal proof-DAG plan

Date: 2026-08-20
Programme branch: `codex/kg-maxroot-attachment-resume-20260822`
Original determinant-tranche base: `b578d599931942b97300be9d936ae14103538806`
Current normal-channel-tranche base: `origin/main` at
`4c10c45fc7a155441c8c0a1c12d662b466a34c8d`
Global Krenn--Gu status: **UNRESOLVED**

This is an internal research plan, not a theorem, reduction, or claim of case
coverage.  It fixes the source and target interfaces for the strategic node and
prevents later fixed-
\(Q\) calculations from being mistaken for an extraction edge.

## 1. Exact source and target

### Source locus

The physical source is an **actual hypothetical complex witness** in the scope
of M1/GLS2/GLS3:

- a ternary graph satisfying the complete GHZ coefficient equations;
- a maximum-cardinality fully supported torus root \(R\), where the maximum
  ranges over both vertex sets and torus vectors, with root size
  \(r\geq 3\);
- surplus two, so the outside set \(B\) has size \(r+2\);
- the resulting maximum-root incidence, blocker, local-concision, pure
  normalization, and complete uncontracted mixed deck data.

The presently committed physical reduction is over \(\mathbb C\).  GLS2 and
several later linear-algebraic implications are valid over characteristic-zero
fields after their incidence identities and nonzero hypotheses are supplied.
No package may silently upgrade the physical source quantifier from
\(\mathbb C\) to every characteristic-zero field.

### Required output interface

For every point of the source locus, on the same physical graph, prove one of:

1. **Useful-pair/attachment output.**  Produce a residual pair \(Q\) whose
   relevant order-two companion column survives modulo **all** relevant
   higher-order columns; a raw nonzero companion coefficient is not enough.
   If the named downstream theorem requires collective pair-observability or
   observability of the full fixed-\(Q\) coordinate family, prove that stronger
   condition as well.  On that same \(Q\), supply a
   legal nonzero target/response package in the full nuisance quotient.  The
   package must include exactly the hypotheses used by the downstream theorem
   being entered: constant-selector legality, nonzero response, augmented
   weights, synchronization, nuisance survival, target-pure anchoring, and,
   when the four-root detector is invoked, a legal \(l\) with
   \(l^{\mathsf T}Jp\neq0\) and
   \(l-\kappa p\in\ker U\).  All selectors and contractions must coexist on
   one graph and one \(Q\).

2. **Excluded failure output.**  Place the point in a proved exhaustive bad
   branch and derive a contradiction from complete mixed GHZ coefficients of
   that same graph.  Every declared nonzero coordinate, incidence weight,
   minor, amplitude, response, detector factor, or chart denominator must be
   handled by exact characteristic-zero saturation or by a separate divisor
   branch.

The node ends at this interface.  Permanent nonrestriction, downstream
extraction/gluing, and global-conjecture resolution are not part of it.

## 2. Failure-mode ledger

| Source | Exact contribution | Failure still requiring coverage at this node |
|---|---|---|
| GLS2 | Reconstructs the complete order-two sensor once the relevant quotient projection is injective. | Does not force pair-observability or any fixed-\(Q\) observability. |
| GLS3 | Forces some raw \(p(A,Q)\neq0\).  Gives physical sharpness families for the coarse source constraints. | By itself it neither aligns that raw incidence with a surviving pair column nor supplies a target selector. |
| GLS4 | The complete contracted mixed target forces one same \(Q\) with a nonzero physical pair block, individual order-two quotient survival, and nonzero raw \(p(A,Q)\). | It proves neither collective pair-observability nor full fixed-\(Q\) observability, and it supplies no legal GLD response/target selector. |
| Reviewed failure-module tranche | Universal pointwise target failure is exactly equality of every geometric radical Fitting profile; response-gated failure and common attachment have exact containment/incidence formulations. Function-field sensor failure has a separate finite rank-stratum formula, and a decomposable-retraction class measures constant-selector legality. | An exact maximal-rank abstract module shows that unrestricted observability, pure diagonality, and nonzero response do not force a legal selector. A physical companion/mixed identity is still required. |
| Reviewed common-contraction/alignment tranche | The GLS4 pair has one fully supported contraction with \(h\ne0\) and raw \(p\ne0\) simultaneously. Ambient GLD2 alignment failure is exactly \(p^{\mathsf T}Jp=0\) and \(Jp\in\operatorname{im}U^*\); the legal-subspace version is an exact annihilator incidence. | It does not create the legal-weight subspace, response, synchronization, nuisance, or anchor hypotheses. |
| Reviewed four-root source trichotomy | For \(r=4\), every source point lies in \(\{O,C\}\times\{R,E,A\}\): observable pair block or quotient circuit, crossed with response zero, common seven-target escape, or function-field pure absorption. Both E leaves reach the required individual-supply plus GLD5/7 interface; O adds stronger pair observability. | The four R/A leaves are the open branches for the stronger all-seven GLD3 package, not the exact obstruction to an isolated one-row escape. |
| Reviewed promoted two-probe one-row reduction | For every \(r\ge3\), re-rooting at the GLS4 probe pair leaves only deck orders \(2r-2,2r\).  One top-minus-two desired coefficient survives the unique top column.  Complete nuisance slicing gives a legal constant-selector criterion, while pure radical--Fitting profiles exactly encode one-row failure at every residual fibre.  For standard \(r=4\), failure must hold for every target and every eligible \(Q\). | It does not force a nonzero response or full-nuisance survival.  One-row escape is not the simultaneous GLD3 package, and the promoted top layers do not match a committed downstream detector for \(r\ge4\). |
| Reviewed full-rank literal-response-zero localization | At root order four, `det H_Q!=0` plus all six pair responses zero forces every direct port block to zero and a common one- or two-port shore.  Its residual factors are coordinate; the singleton shore is fully monomial, while in the two-port shore at least one local factor is coordinate.  The four-port response then vanishes automatically and the complete contracted target forces opposite residual colours with pure third-colour \(\Pi_Q\). | Its pure-\(\Pi_Q\) chart is passed to the next exact complete-fibre exclusion.  The determinant divisor survives.  This remains narrower than GLS7's branch where merely one response is zero. |
| Reviewed full-rank literal-response-zero survivor exclusion | On the localized opposite-colour pure-\(\Pi_Q\) chart, the complete `(i,i)` fibre excludes the singleton shore because one insertion line cannot carry both pure colours.  In the two-port normal form the same two alpha-lines occur on both residual shores, but the `(i,i)` and `(j,j)` fibres require incompatible \(\{i,k\}\) and \(\{j,k\}\) covers. | This excludes the whole `det H_Q!=0` literal all-seven-zero branch.  Focused replay, independent no-import audit, and hostile review pass.  The determinant divisor, weaker response-zero patterns, nonzero-response absorption, and the named downstream package remain open. |
| Reviewed determinant-divisor six-response reduction | On the same GLS4 pair, six full pair-response zeros exclude rank zero and reduce rank two to an exactly-two-active-port conformal core with fixed coordinate blockers.  Rank one has an exhaustive double-contained/one-sided/two-sided quotient trichotomy; its escaping branches have exact support-at-most-two normal forms and complete-target refinements. | The reduction itself leaves the rank-two and rank-one cores.  The linked reviewed follow-up excludes rank two and the singleton triangle.  The seventh four-port response is an independent quartic on the divisor, so literal all-seven zero needs that equation separately.  Focused primary, independent no-import replay, and hostile review pass. |
| Reviewed rank-two and singleton-triangle exclusion | Six active-pair quotients of the complete target exclude all four rank-two active-dimension profiles.  The rank-one singleton triangle is incompatible with one common incidence family because column splicing its three pure companions would give `P_4 -> Delta_3`, contrary to exact subrank two. | Rank-one double-contained, one-sided, and two-port two-sided branches survive; the seventh quartic and every broader response/absorption branch remain.  Focused primary, independent no-import replay, and hostile review pass. |
| Reviewed rank-one two-port `P_5` extraction | The complete target forces the two local factors onto the two residual colours.  Twelve common-tail identities split exactly `8+4`; one Latin bottom-row splice yields a weighted `P_5 -> Delta_3` restriction, and the physical seventh response is termwise zero. | This routes the two-port two-sided branch to the downstream `P_5` node; it is not a permanent nonrestriction or selector theorem.  Branches I/II pass to the sibling continuation below; weaker response patterns, absorption/exceptional fibres, and named downstream gates remain.  Primary, no-import replay, and hostile review pass. |
| Reviewed rank-one contained/one-sided reduction | Branch I is exhausted by pure decomposable `P_5` pullbacks or a same-coordinate balanced core; `Phi!=0` makes the seventh response visible, while `Phi=0` aligns the partitions and leaves one augmented-`P_6` face defect `Psi`.  Branch II and its transpose give pure decomposable `P_4/P_5` compression interfaces. | None of the pure-compression outputs is a downstream exclusion.  The visible-response branch still lacks a legal selector, the aligned branch still has `Psi`, and weaker response/absorption/exceptional branches remain.  Primary, no-import replay, and hostile review pass. |
| GLD7 | On a fixed target quotient, rank \(q\ge2\) contradicts GHZ and rank \(q=1\) forces attachment. | Rank zero is swallowed pure; response-zero leaves the target class undecided. |
| GLD9 | Synchronizes a common contraction on the maximal-rank survival stratum. | Does not create survival, and does not cover lower-rank/exceptional contractions. |
| GLD11 | Gives exact physical simultaneous swallowed-pure/nonzero-response controls; particular higher mixed coefficients exclude those controls. | It is not a classification of all selector failures or all supports. |
| GLD13 | For prechosen \(Q\) and seven nonzero response polynomials, gives the function-field split: common generic legal survival or desired-plus-three-pure absorption. | Response-identically-zero and every exceptional nuisance-rank-drop fibre remain; generic absorption is not pointwise absorption. |
| GLD14 | Closes the affine fibre and all-depth mixed shape after legal attached rows are supplied. | It cannot supply those rows or choose \(Q\). |
| GLD15 | Gives the exact fixed-target joint \(M/Z\) ranks \(k=0,1,2\). | Nothing forces \(k>0\); rank-one cancellation and response rank are separate. |
| GLS15 | For every root order and pair target in the original `r`-root, `r`-port fixed-`Q` chart, identifies the two GLD15 desired columns as one physical transform `Psi_C` applied to `K^Q` and `R`.  Rank-one absorption is `Psi_C(delta R-eta K^Q)` in the complete joint nuisance, and a foreign absorbed direction gives exactly the projective determinant times the local quotient generator and active pure target class. | The transport defect is not proved nuisance.  Joint rank zero, response-zero/missing activity, the four-port line, and an arbitrary-order downstream package remain.  The distinct two-probe promoted GLS8 interface is not integrated. |
| GLS16 | Maximum-root evaluation sends the original fixed-`Q` pair `M` column to the complementary permanent and kills the pair `Z` column; the complete joint nuisance maps to all other order-two coefficient slices.  Base survival excludes `k=0` and forces rank one to the common pure-`M` line.  Every foreign labelled pair map also has zero cross-contraction against a legal selector for another target. | Source-level base survival is not forced.  Every oblique/pure-`Z` or `k=0` target remains on an explicit swallowed-base circuit, and cross-annihilation is not foreign nuisance membership.  Four-port synchronization, activity, exceptional fibres, and GLS8 remain open. |
| GLS17 | For any original fixed-`Q` even target `|S|=2t`, leaving `t-1` roots open kills the residual-present grade and every higher grade.  The residual-absent column has an explicit injection/permanent leading tensor; survival modulo every lower-or-equal-grade nuisance slice forces a pure-`M` operator row.  A finite surviving family has one common pure-`M` direction, and at `r=4` the explicit four-port first-root covectors complete the seven-row synchronization gate. | No leading class is forced to survive and physical output/activity remain separate.  At `r=4`, at least one swallowed pair shadow, all four swallowed four-port shadows, or activity/rank/response failure remains.  Foreign transport and GLS8 are not integrated. |
| GLS18 | Applying every GLS17 shadow to the complete target equation makes the pure leading quotient rank at most one and useful exactly when both its desired leading class and physical `M` response are nonzero.  Universal failure is the exact all-rank geometric radical--Fitting profile, and finite target families are encoded at one shared residual point. | The simultaneous profile is not proved empty.  At `r=4`, four-port failure means four full first-root nuisance shadows, while pair failure forces only a diagonal three-space inside a nine-space.  Activity, foreign transport, source coverage, and GLS8 remain open. |
| GLS19 | Leaving `t` roots open for a `2t` target and adjoining the generally surviving `M` column to the exact individual-`Z` nuisance exposes an injection/permanent residual-present top class.  Survival supplies pure `Z`; the complete target and all-rank Fitting profile couple it exactly to nonzero `Z_S` response at one shared residual point. | No top class or response is forced.  At `r=4`, the pure top columns span only diagonal three-spaces, and seven useful rows still require GLD16 activity.  Simultaneous GLS18/GLS19 failure, oblique lines, transport, source coverage, and GLS8 remain open. |
| GLS20 | In the promoted GLS8 chart, maximum-root contraction of the two probe slots maps each source Laplace target's complete `81`-row nuisance to a `9`-row base quotient.  Base survival is exactly a legal factor-through selector.  The source Laplace identity forces a raw nonzero base coefficient and turns universal base absorption into a nonzero `Pi_Q` circuit; the complete target makes usefulness exactly base survival plus nonzero promoted response, with an all-rank Fitting profile. | GLS21 subsequently proves the nine-row quotient is always zero on the required `p!=0` gate.  The conditional GLS20 identities remain valid, but this base-survival route is a no-go. |
| GLS21 | The active `D=Q` residual-absent all-port label is retained nuisance for every promoted source target.  Maximum-root contraction makes it exactly `p_(A,Q)id`; its coefficient slices give `pI_9`, so all source base nuisances are full on `D(p)` and every GLS20 Fitting failure profile is automatic. | This proves only the factor-through base route impossible.  It does not absorb the full `81`-row desired class, exclude upstairs selectors or responses, supply a downstream package, or close the node. |
| GLS22 | On `p!=0`, `P_Q=pI-G_Q^A(z_Q) tensor epsilon_A` kills exactly the retained all-port root line and identifies the full GLS8 quotient with an exact transverse quotient (`81 -> 72` rows for top-minus-two, `9 -> 8` for top).  Full legal survival, complete-target pure rank, and all-rank failure are equivalent after projection.  Source terms satisfy `T_Q=pF_Q-G_Q^A(z_Q) tensor Pi_Q`. | No transverse class or response is forced.  Aggregate synchronization is not termwise synchronization, and the `r=3` seven individual quotients still lack common selection/activity.  No `r>=4` downstream package or node closure follows. |
| GLS23 | Every unwanted promoted complement pair `D` contributes exactly `Slice_(D_0-C)(a_D) tensor V_(C-D_0)^*` to a target's complete transverse nuisance.  The common top tensor `omega=W_(a_0,a_1)` is nuisance for every pair target and desired for the top target.  Disjoint root-slice fullness absorbs a pair target; `omega=0` kills the top target, while `omega!=0` gives exact `63`-row pair quotients and an explicit eight-row top survival test. | The disjoint criterion is sufficient, not necessary.  Proper disjoint spans, overlap slices, top-anchor absorption, pair/top response zero, common normalization/activity, and every `r>=4` downstream package remain.  At `r=3` the anchor split is exhaustive, but it is not seven-row supply or node closure. |
| GLS24 | Each actual probe-root contraction maps the `GLS23` transverse root space `8 -> 2`.  A nonzero top-anchor marginal defines the denominator-free exterior map `v -> u_i wedge rho_i(v)`, giving one exact common `9`-row physical nuisance for every pair target after the `63`-row anchor quotient.  Zero, nonzero-marginal, and nonzero double-transverse anchors are exhaustive.  Complete-target rank, all-rank failure, and an aggregate synchronization fork descend. | Nine-row failure is not full `63/72`-row failure.  No common marginal, pair survival/response, top survival, or activity is forced.  At `r=3` only the all-six-useful plus top-useful plus three-active leaf enters `GLD3`; every failed-row/low-activity branch and every `r>=4` downstream package remain open. |
| GLS25 | On the nonzero double-transverse anchor branch, `q,p` define the denominator-free scaled projector `Xi_Q` with exact `4/4` image/kernel and `Xi_Q^2=pXi_Q`.  Wedging its core image by `omega` gives exact `27`-row pair modules; the top target has a separate four-row core test with desired `p^2 omega`.  Target rank, Fitting failures, and source synchronization descend. | Reduced failure is not full failure.  No pair/top survival, response, or activity is forced.  At `r=3` only the all-six-core-useful plus top-core-useful plus three-active leaf enters `GLD3`; the zero anchor, every failed-row/low-activity fibre, and every `r>=4` downstream package remain. |
| GLS26 | On the zero-anchor branch, the full top-target GHZ quotient reconstructs the projected diagonal root space, of rank two or three, from the remaining physical pair labels.  All labels meeting `Q` once lie in a canonical projected residual-shore tangent of dimension at most seven.  Modulo it, either some genuine promoted pair label has an essential nonzero transverse slice or the two residual shore spans cover all three coordinate lines. | The essential slice is raw supply, not survival in its own complete nuisance or response supply.  The coordinate-shore cover is not excluded.  Neither branch gives common synchronization, activity, an `r>=4` detector, or node closure. |
| GLS27 | Retaining the GLS26 residual pair as a Laurent family gives an exact generic-escape/fixed-cover dichotomy.  Generic escape supplies a nonempty principal open of essential promoted-pair contractions.  Generic fixed cover has only the C12/C21/C22 shore normal forms, with one fixed colour-to-shore assignment on a nonempty principal open.  The GLD11 graph realizes C21 identically with `h,p,Pi_Q!=0` but is killed by a later mixed coefficient. | The open is used only to choose a source contraction; it is not a universal exceptional-fibre theorem.  Essential raw supply still lacks its own complete-nuisance survival and response, while none of C12/C21/C22 is excluded on the complete witness locus.  Synchronization, activity, a named downstream package, and node closure remain open. |
| GLS28 | On the zero-anchor branch, the complete nuisance of a promoted pair target lies in the root envelope generated by `P_Q(T_Q)` and all other promoted pair suppliers.  A supplier coloop gives a legal full selector; a projected-diagonal coloop gives the named nonzero-response row.  Simultaneous useful-row failure leaves a deletion-stable diagonal cover with at most four labels per certificate, while full absorption gives relations on at most five labels. | Envelope containment is one-way.  A supplier coloop can have zero response, and useful failure does not make every supplier redundant.  The redundant cover, all exceptional fibres, C12/C21/C22, synchronization, activity, a named arbitrary-root detector, and node closure remain open. |
| GLS29 | On the GLS28 `(d_0,d_1)=(2,2)` branch, the shore-normal product is the exact dual of the one-dimensional tangent quotient.  Every supplier factors through two local channels; every target nuisance has an exact normal-cylinder image; and the complete physical equation gives an arbitrary-root mixed-response identity.  Active colours force coefficient/response activity and two-port channel occurrence.  Full activity excludes intersecting support at arbitrary `r` and is pointwise impossible at `r=3`, including every local-rank/response fibre. | The divisor where one or two normal colour products vanish remains.  At `r>=4`, the at-least-three-edge disjoint-supplier branch remains.  Normal-image fullness is not full nuisance absorption, and an exact rational same-graph control with full absorption and six responses fails original mixed coefficients.  Other shore ranks, C12/C21/C22, synchronization, downstream entry, and node closure remain open. |
| GLS30 | Complement-kernel contraction isolates one normal supplier at arbitrary root order without dividing by any response, kernel coordinate, or minor.  At `r=3` it gives exact one-active and two-active projected-kernel profiles, including every silent zero-star fibre.  Both profiles have exact same-graph scalar-normal controls with six nonzero responses and full normal nuisance images; a separate one-active graph has a maximum torus root, defect four within the bound six, pure coefficients one, and the exact normal tensor. | The maximum-root control does not have simultaneous full normal absorption, fails mixed GHZ coefficients, and does not certify the full GLS26 top reconstruction.  Thus the six-response/full-normal implication and the separate maximum-root/pure upgrade are refuted, not their coupling.  Coupled maximum-root absorption, tangent/other-root equations, full target nuisance survival, all divisor fibres, higher-root support, other shores, synchronization, downstream entry, and node closure remain open. |
| GLS31 | The residual-shore normals give exact denominator-free evaluation-pencil coefficient equations.  Their bidegree `(1,1)` part is GLS29, while both first-polarized parts retain all one-`Q` labels.  A sparse exact graph simultaneously has a maximum root, defect six, pure normalization, six nonzero responses, full normal images, the scalar normal identity, the GLS26 diagonal inclusion, and absorption of all six desired pair tensors in their complete GLS23 nuisances. | The graph fails exactly `313` mixed GHZ words and is not a witness.  Hence the combined static coupling is a no-go route, not divisor exclusion.  The retained-root quotient is only the old product-normal channel.  Complete first-polarized mixed/root-deck equations, all divisor and rank fibres, higher-root support, other shores, synchronization, downstream entry, and node closure remain open. |
| GLD16 | For arbitrary physical residual scalar \(h\), a common projective line plus the stated activity gives a legal shifted detector through \(a=\delta+h\eta\) and the denominator-free identity \(aT'=C(D)-C(\eta K)\).  Both \(a=0\) and \(a\ne0\) are excluded under three-colour activity. | Zero target spaces, unequal slopes, and missing activity remain; the `h=0` restriction no longer remains inside the common-line branch. |
| GLD17 | Excludes the stated \(M\)-active slope/cancellation alternatives with a three-full complement. | Pure-\(Z\), other slopes, missing three-full activity, and support drops remain. |
| GLD18 | Proves \(C_S\subseteq\ker R_S\) and treats the stated edge-dependent cancellation locus. | The converse fails; invisible/visible rank one, rank zero, noncancellation, pure-\(Z\), and sparse support remain. |
| GLD19 | Under literal all-seven response-map zero, classifies twelve-row complementary support and supplies conditional detectors. | The source does not force response-map zero, and the localized support locus is not excluded. |
| GLD20 | Exhaustively classifies response-window support masks inside that fixed-\(Q\), response-zero stratum. | Mask exhaustiveness is not witness exhaustiveness; \(F=\varnothing\), star/triangle absorption, coefficients, and same-graph integration remain. |
| GLD21 | In the two-corrected-colour, one-\(K_4\), response-zero cell, excludes \(h=0\) and identifies the \(h\neq0\) integrability residue. | It neither forces the cell nor excludes \(h\neq0\); its absorbed scalar rows are not legal GLD15 operators. |
| GLD22--GLD62 | Exclude private/single-slice and exactly one-, two-, or three-reciprocal-pair dense subcharts, including recorded exceptional divisors. | These are conditional subcharts, not source coverage; four-or-more-pair, proper-secondary, broader/root-colour-changing arrays, and other response-zero cells remain. |
| BO1 | Proves that a uniformly bounded ambient Wick/response atlas cannot characterize the arbitrary-order target. | Any finite atlas needs a new target-coupled structural-degree/generative coverage theorem. |

Additional cross-cutting failure modes are: choice of \(Q\); collective versus
coordinate observability; quotient survival versus raw nonvanishing; response
visible versus response invisible; target spaces of rank zero, one, and two;
different versus common slopes; generic versus exceptional nuisance rank;
support divisors; and coexistence of all required selectors on the same graph.

## 3. Branches already closed (conditional scopes only)

- M1 supplies the maximum-root surplus-two physical source restrictions.  The
  GLS4 same-pair theorem strengthens GLS3: every source point has one pair
  \(Q\) whose physical pair block and complementary permanent are nonzero,
  whose order-two column survives individually modulo every higher column,
  and whose raw \(p(A,Q)\) is nonzero on a fully supported contraction.
- GLS2 reconstructs the order-two sensor on an assumed observable block.
- GLD7 closes \(q\ge2\) and gives the desired attachment when \(q=1\).
- GLD9 and the survival side of GLD13 provide common-contraction attachment on
  their declared fixed-\(Q\) principal opens.
- GLD11 proves that two coarse support-preserving selector-failure controls are
  killed by particular complete mixed coefficients.
- GLD14 closes the paired affine/all-depth shape after attachment.
- GLD16--GLD18 close their named common-line, activity, slope, cancellation,
  and three-full subbranches.
- GLS16 confines pair rank zero and every non-pure-`M` rank-one direction to
  an explicit swallowed base pair-column quotient.  When all required base
  classes survive, every rank-one pair line is synchronized to pure `M`.
  It also proves exact foreign-label cross-annihilations, but neither source
  base survival nor foreign transport membership.
- GLS17 extends the maximum-root shadow to every even target.  Leaving `t-1`
  roots open for `|S|=2t` gives an explicit leading tensor and kills the
  adjacent residual-present grade.  Leading survival forces the pure-`M`
  row, including the four-port row at `r=4`; a surviving finite family has a
  common pure-`M` direction even across rank-two target spaces.  Survival and
  activity are not forced.
- GLS18 couples every such shadow back to the complete target equation.  Pure
  leading quotient rank is at most one and rises exactly when the desired
  leading class and physical `M` response are both nonzero.  Universal failure
  is now an exact all-rank radical--Fitting profile.  At `r=4`, the bad
  four-port branch is four full first-root shadows; the theorem does not
  exclude that branch or supply activity.
- GLS19 supplies the complementary top-grade axis.  Leaving `t` roots open
  exposes the residual-present injection/permanent tensor after placing the
  surviving `M` column in the exact individual-`Z` nuisance.  Top survival
  gives a legal pure-`Z` row, and complete-target usefulness is exactly top
  survival plus nonzero `Z_S`.  Universal failure is an all-rank Fitting
  profile; neither that profile nor activity is excluded.
- GLS20 integrates the GLS8 source Laplace terms with the target quotient.
  Contracting both probe slots at their maximum roots gives a complete nine-row
  base nuisance.  A surviving base class is a legal factor-through selector;
  complete-target usefulness is exactly base survival plus nonzero promoted
  response.  The source forces raw nonvanishing only, so simultaneous base
  absorption and response-zero branches remain exact open alternatives.
- GLS21 proves the GLS20 base-survival alternative is absent on the actual
  `p!=0` source gate.  The retained `D=Q` all-port nuisance contracts to
  `pI_9`, filling every base quotient.  This is a route no-go; full upstairs
  GLS8 selectors remain possible and must be treated without deleting that
  nuisance label.
- GLS22 quotients the uncontracted all-port root line exactly instead of
  deleting it.  The resulting `72/8`-row transverse modules are equivalent to
  the full legal GLS8 quotients on `D(p)`.  Their complete-target and Fitting
  profiles retain every response-zero and rank-drop fibre; survival and
  downstream common-package gates remain open.
- GLS23 expands that projected nuisance label by label.  The exact contribution
  is the projected companion sliced over the target-side fixed factors and
  tensored with the complete missing left-port space.  Disjoint root-slice
  fullness is one sufficient absorption mechanism.  The top tensor is a common
  pair-target nuisance anchor and the top-target desired coefficient, giving an
  exhaustive zero/nonzero anchor split.  No branch is excluded, and every
  proper slice-span fibre remains open.
- GLS24 contracts the transverse root factor at an actual probe-root vector.
  A nonzero partial top-anchor contraction then gives a canonical exterior
  quotient with one root row and nine pair-complement rows.  The exact physical
  nuisance, target rank, Fitting failures, and source aggregate descend without
  division.  The zero/double-transverse anchor fibres and every failed-row or
  low-activity branch remain open; marginal failure is not full failure.
- GLS25 treats the nonzero double-transverse anchor fibre.  The all-port tensor
  supplies a denominator-free scaled projector onto the four-dimensional core;
  exterior multiplication by the anchor gives three root rows and exact
  `27`-row pair modules, while the top target uses a four-row core module.
  Reduced survival and activity are not forced, and reduced absorption is not
  full absorption.
- GLS26 treats the zero-anchor fibre by the complete top-target equation.  The
  projected three-colour diagonal root space has dimension two or three and
  must be reconstructed from the remaining pair-label slices.  Labels meeting
  `Q` once lie in the at-most-seven-dimensional projected tangent of the two
  residual incidence shores.  Outside the exact coordinate-shore cover, a
  genuine promoted pair label is therefore essential.  This is raw transverse
  supply only; its own complete nuisance, response, synchronization, and
  downstream detector gates remain open.
- GLS27 varies the fully supported residual pair instead of freezing one
  contraction.  Either a principal open escapes the coordinate-shore cover,
  or one fixed cover persists generically and has exactly the C12, C21, or
  C22 shore-space normal form.  GLD11 sharply realizes C21 with every listed
  source gate but violates a later mixed equation.  The result chooses a
  source point; it neither discards exceptional fibres from a universal claim
  nor proves target survival, response, synchronization, or exclusion of the
  three normal forms on the witness locus.
- GLD19--GLD21 and GLD22--GLD62 close only their explicitly declared
  response-zero/support/dense subcells.
- The reviewed failure-module theorem compresses all pointwise nuisance-rank
  fibres into exact radical--Fitting profiles, and the common-attachment
  question into one shared-residual incidence ideal.  This is an exhaustive
  algebraic encoding, not a physical exclusion.
- The reviewed common-contraction theorem synchronizes the GLS4 \(h\) and raw
  \(p\) gates and exactly classifies ambient and legal-subspace augmented
  alignment.  It does not force the missing legal subspace.
- The reviewed four-root source theorem proves the exhaustive
  \(\{O,C\}\times\{R,E,A\}\) cover.  Both E leaves are good for the declared
  all-seven GLD5/GLD7 interface; none of the four R/A leaves is claimed closed
  for that package.  C is only an obstruction for a stronger downstream
  observability requirement.
- The promoted two-probe theorem puts every root order into one exact promoted
  legal-target module and gives the pointwise all-rank one-row failure criterion.
  It also proves that the standard four-root isolated-one-row bad locus is
  simultaneous failure of every target for every GLS4-eligible \(Q\), not one
  R or A target.  No physical exclusion of that locus or entry to a named
  downstream detector is claimed.
- On the root-order-four literal all-seven response-zero branch, the full-rank
  residual edge chart is localized to an opposite-colour pure-\(\Pi_Q\) locus;
  equal blocker colours are excluded by the first complete-target projection.
  The reviewed two-fibre quotient theorem excludes the remaining pure locus
  because the first fibre kills the singleton and the two-port normal form has
  the same two alpha-lines in incompatible `(i,i)` and `(j,j)` covers.  The
  divisor `det H_Q=0` is not excluded, and one-zero-response
  patterns are broader than this pair of theorems.
- On that determinant divisor, the reviewed six-response theorem excludes
  rank zero and every rank-two quotient escape.  Its rank-two survivor has
  exactly two active combined ports and fixed coordinate blockers.  Rank one
  is covered by explicit double-contained, one-sided, and two-sided normal
  forms.  The theorem also proves that the four-port response is a separate
  quartic obligation.  The reduction itself leaves those rank-drop survivors;
  the next bullet records the follow-up exclusions.
- The reviewed follow-up excludes the whole rank-two conformal survivor by a
  six-quotient support cover and excludes the rank-one singleton triangle by
  common-incidence column splicing into the fourth-order permanent subrank
  obstruction.
- The reviewed two-port continuation forces the Branch-III local factors
  onto the residual colours and uses twelve common-tail identities to build
  one exact weighted \(P_5\to\Delta_3\) restriction.  This routes that branch
  to a downstream permanent node; it does not exclude \(P_5\) or attach a
  GLD target package.
- The reviewed contained/one-sided continuation exhaustively routes the sibling
  Branch-I/II leaves.  Branch I gives a nonzero decomposable \(P_5\) pullback
  or a same-coordinate balanced core.  The latter splits into \(\Phi\ne0\),
  where the seventh response is visible but a selector is not supplied, and
  \(\Phi=0\), where an augmented \(P_6\) retains the explicit face defect
  \(\Psi\).  Branch II and its transpose give pure decomposable \(P_4/P_5\)
  compression interfaces.  These are reductions to open downstream or legal
  selector obligations, not exclusions.

None of these bullets is an exhaustive path from the source witness to the
target interface.

## 4. Minimal remaining branches

The all-pairs individual-supply failure branch is closed: the contracted
complete mixed target always produces at least one same pair with individual
order-two quotient survival and raw incidence.  The first missing edge is to
attach a complete legal response/target package to at least one such supplied
pair, or to exclude every attachment failure on the same graph.  If the
chosen downstream entry needs collective pair-observability or the full
fixed-\(Q\) coordinate family, those stronger ranks remain part of that edge.
A support-free proof must now cover:

1. for root order four, simultaneous pointwise failure of every target for
   every eligible \(Q\), expressed by the response-gated/pure-profile radical
   containments; the coarser R/A branches remain relevant only to the stronger
   all-seven GLD3 package, while C is separate only if a different downstream
   entry requires stronger pair-block observability;
2. all nuisance-rank-drop and observability-minor divisors, now expressible
   without rank-stratum enumeration by the radical--Fitting/incidence
   criteria;
3. response-identically-zero branches outside the already named conditional
   GLD subcells; at root order four the reviewed complete-fibre theorem
   excludes the literal all-seven/full-rank chart, while the reviewed
   determinant-divisor theorem reduces simultaneous six-pair zero to exact
   rank-two/rank-one cores and keeps the seventh quartic separate.  The
    reviewed follow-up excludes rank two and the singleton.  The reviewed
    two-port theorem routes Branch III to \(P_5\), while the reviewed
    contained/one-sided continuation routes Branches I/II to explicit pure
    compression, response-visible-selector, or augmented-face-defect
    interfaces.  Every weaker response-zero pattern remains;
4. joint selector ranks \(k=0,1,2\), common and unequal slopes, and all response
   visibility alternatives; GLS16 now sends every `k=0`, pure-`Z`, oblique,
   or unequal-line residue to an explicit swallowed base pair circuit, while
   GLS17 sends absence of pure `M` at any even target to all of its swallowed
   partial-root leading classes.  GLS18 couples those classes to the physical
   `M` response.  GLS19 adds the complementary residual-present top classes,
   each coupled to `Z_S`.  Simultaneous failure of both axes is now encoded
   across all nuisance-rank fibres by exact radical--Fitting containments at
   one shared residual point, but the containments are not yet contradicted.
   The common-line detector is denominator-free at arbitrary \(h\), so no
   residual-scalar divisor remains inside that conditional branch;
5. the detector's augmented-weight/alignment/anchor failures when that detector
   is the downstream entry;
6. at every root order, exclusion of the promoted response-zero/full-nuisance
   failure profiles, followed by the missing common-package/downstream-shape
   theorem satisfying every synchronization, alignment, activity, nuisance,
   and anchor hypothesis of a named committed downstream detector, especially
   for root order at least five.

The desired structural form is a companion-exchange/hafnian/Pluecker syzygy or
module identity which, after only legitimate saturation, places a nonzero pure
target coefficient in the module generated by complete mixed coefficients.
An exact countermodel instead revises this branch decomposition; it is not to
be hidden by adding support masks.

GLS15 now gives the exact class such a syzygy must control.  If target `T` has
absorbed physical direction `A_T=delta_T R-eta_T K^Q`, then in another
rank-one target `S`

```text
[Psi_(C_S)(A_T)]
 =(delta_T eta_S-eta_T delta_S)bar g_S.
```

Multiplying by an active selected diagonal value identifies this class with
the same projective determinant times the active pure GHZ class.  The next
step must prove foreign transport into `N_(C_S)^J` or contradict the resulting
nonzero pure class; generic raw injectivity of `Psi_C` does neither.

GLS16 adds the maximum-root base shadow.  If the base pair class of `S`
survives, then `k_S` is nonzero and a rank-one line is necessarily pure `M`.
Thus every rank-zero target and every oblique, pure-`Z`, or unequal-line
residue now carries a concrete order-two base-swallowing circuit.  A selector
for `T` also cross-annihilates both physical columns of every `S!=T`, but this
map has source `S-T` and target `T-S`; it is not the `S`-quotient membership
required by GLS15.  The next proof must use these exact circuits and cross
maps in the complete mixed equations, or force enough base classes to survive.

GLS17 supplies the adjacent-grade extension.  For `|S|=2t`, every partial-root
shadow leaving `t-1` roots open kills `g_S^Z` and sends `g_S^M` to an explicit
injection/permanent tensor.  Survival of any such class puts `(1,0)` in
`C_S`; absence of pure `M` forces all `binom(r,t-1)` shadows to be swallowed.
At `r=4`, this reduces the four-port synchronization failure to four exact
first-root absorptions, while the six pair failures remain the GLS16 base
circuits.  Complete mixed equations must now force one of these leading
classes or exclude their simultaneous absorption; activity is still separate.

GLS18 applies those shadows to the complete GLD15 target equation.  For every
`A,S`, the pure quotient columns satisfy

```text
sum_c alpha_c [d_(A,S,c)] tensor w_(S,c)
  =b_(A,S) tensor M_S.
```

Thus the quotient rank rises exactly when `b_(A,S)` and `M_S` are both
nonzero, and failure on every residual/rank-drop fibre is precisely the
geometric radical--Fitting containment family.  At `r=4`, absence of a pure
four-port `M` row makes all four first-root shadows full.  This is the exact
bad-locus profile to contradict, not a proof that it is empty.

GLS19 gives the complementary residual-present chart.  For `|S|=2t`, leave
`t` roots open and quotient by the exact individual-`Z` nuisance
`N_S^J+K g_S^M`.  The grade-`t` column becomes an injection/permanent top
tensor; survival gives pure `Z`.  On a complete witness,

```text
sum_c alpha_c [d_(A,S,c)^Z] tensor w_(S,c)
  =c_(A,S) tensor Z_S.
```

Thus usefulness is exactly top survival plus nonzero `Z_S`, and universal
failure has its own all-rank radical--Fitting profile.  Seven useful rows at
`r=4` enter GLD16 with `a=h`; both `h` branches still require the separate
three-colour activity gate.  The next proof must contradict simultaneous
failure of the pure-`M` and pure-`Z` profiles or control the remaining
projective lines.

GLS20 returns to the distinct promoted GLS8 chart and contracts its two probe
slots at the actual maximum-root vectors.  For each source pair `C subset U`,

```text
N_C^base=epsilon_A(N_C),
Pi_Q=sum_C b_C tensor Per_(K_0,U-C).
```

Base survival is equivalent to a legal selector factoring through
`epsilon_A`.  If every base class is swallowed, the nonzero `Pi_Q` lies in the
explicit Laplace nuisance sum; this is a circuit, not a contradiction.  On the
complete target the three diagonal base columns have rank at most one, and the
rank rises exactly when `[b_C]` and the promoted physical response are both
nonzero.  Simultaneous failure is now a nine-row radical--Fitting profile on
every exceptional fibre.  The next proof must exclude that profile or use
upstairs-only GLS8 escapes, then assemble a complete named detector package.

GLS21 corrects that proposed next step.  The complete GLS8 nuisance contains
the distinct same-grade complement `D=Q`:

```text
G_Q^A(z_Q) tensor id_(W_Uhat).
```

After maximum-root contraction this is `p_(A,Q)id`, and slicing over the target
factor supplies the whole submodule `pV_C^*`.  Since the source gate has
`p!=0`, every nine-row base quotient is zero and every GLS20 Fitting
containment is forced.  The next promoted proof must remain in the full
`81`-row quotient, use a legal joint construction retaining the all-port
label, or contradict full upstairs simultaneous failure.  Forcing GLS20 base
survival is no longer an open route.

GLS22 gives the exact upstairs replacement.  With

```text
q=G_Q^A(z_Q),
P_Q=pI-q tensor epsilon_A,
t_C=(P_Q tensor id)g_C,
```

one has `ker P_Q=Kq`, `im P_Q=ker epsilon_A`, and `P_Q^2=pP_Q` on `D(p)`.
Because `q tensor V_C^*` is already complete nuisance, full GLS8 survival is
equivalent to survival of `t_C` in the projected complete nuisance.  This gives
`72` rows for all top-minus-two targets and `8` for the top target, with exact
target coupling and all-rank Fitting profiles.  The source fork
`T_Q=pF_Q-q tensor Pi_Q` distinguishes raw transverse nonvanishing from an
aggregate projective synchronization identity, but neither branch proves
quotient survival or activity.

GLS23 replaces the remaining opaque projected nuisance by an exact physical
slice sum.  For an active complement pair `D`, put `D_0=D intersect Uhat` and
let `a_D` be its transversely projected companion.  At target complement `C`,
its exact contribution is

```text
Slice_(D_0-C)(a_D) tensor V_(C-D_0)^*.
```

The sum over every unwanted label, plus the projected top term exactly when it
is nuisance, is the complete transverse nuisance.  Thus disjoint root slices
spanning `ker epsilon_A` absorb a pair target.  The common top tensor
`omega=W_(a_0,a_1)` is nuisance for every pair target and desired for the top
target: `omega=0` makes the top desired coefficient zero, while `omega!=0`
reduces every pair problem exactly to `63` rows and leaves an explicit
eight-row top survival test.  At `r=3` this exhausts the anchor alternatives
for the six pair targets and the four-port target; it does not force any
survival, response, common normalization, or activity.

GLS24 refines the nonzero-anchor branch without selecting a support chart.
For probe index `i`, contraction at `x_(a_i)` maps the eight transverse root
rows onto the two-dimensional annihilator of the other probe root.  If
`u_i=rho_i(omega)` is nonzero, the denominator-free map

```text
v -> u_i wedge rho_i(v)
```

has one-dimensional image and kills the common anchor.  Applying it to the
complete GLS23 slice sum gives an exact nine-row physical nuisance for every
pair target.  Its survival is precisely the existence of a legal selector
factoring through this common marginal route; every target and Fitting fibre
descends.  The anchor split is now zero, nonzero with a nonzero marginal, or
nonzero in the double-transverse `2 x 2` core.  At `r=3`, usefulness of all six
rows through one common marginal, usefulness of the top row, and three-colour
activity enter `GLD3` and are excluded.  The theorem forces none of those
conditions and does not identify marginal absorption with full absorption.

GLS25 handles the remaining nonzero double-transverse anchor fibre.  Write
`s_0=q(-,x_(a_1))` and `s_1=q(x_(a_0),-)`.  The denominator-free operator

```text
Xi_Q(v)=p v-s_0 tensor rho_0(v)-rho_1(v) tensor s_1
```

has the four-dimensional double core as image, a four-dimensional marginal
kernel, and `Xi_Q^2=pXi_Q`.  On `0!=omega` in that core, the map
`v -> omega wedge Xi_Q(v)` has three root rows, giving exact `27`-row pair
modules.  The top desired anchor cannot be wedged away and instead has a
four-row core test with desired `p^2 omega`.  Target rank, every Fitting fibre,
and the source aggregate descend.  At `r=3`, all six useful pair modules plus
useful top and activity enter `GLD3`; no usefulness or activity is forced, and
reduced failure does not imply full failure.

GLS26 returns to `omega=0`.  Put

```text
X_i=span{W_(a_i,q_0)(-,z_0),W_(a_i,q_1)(-,z_1)},
T_Q=X_0 tensor V_(a_1)^*+V_(a_0)^* tensor X_1.
```

The all-port tensor `q` lies in `T_Q`, so `P_Q(T_Q)` has dimension at most
seven.  Every top-target nuisance label meeting `Q` once lies in that space.
The complete GHZ top-target quotient and `omega=0` force all projected pure
diagonal root tensors into the exact nuisance.  Hence either a promoted pair
slice survives modulo `P_Q(T_Q)`, or every coordinate line is contained in
`X_0` on the first shore or `X_1` on the second.  The former is not survival
modulo the pair target's own nuisance, and the latter shore cover is still a
live exceptional branch.

GLS27 retains the residual vectors as Laurent variables.  Over their function
field, rank minors and augmented membership minors give an exact dichotomy:

```text
E: some colour is absent from both generic shores;
C: every colour lies on at least one generic shore.
```

In E, clearing one rank minor and one augmented minor per shore produces a
nonempty principal open on which GLS26 supplies an essential promoted-pair
slice.  In C, clearing basis minors and membership denominators produces one
fixed colour-to-shore assignment on a nonempty principal open.  The only
generic rank profiles and shore-space normal forms are C12, C21, and C22.
This is a source-choice theorem, not a claim about every point on every
rank-drop divisor.  The exact GLD11 control realizes C21 identically with
`h,p,Pi_Q!=0`; its nonzero mixed word shows that complete mixed equations are
load-bearing.  Thus the next alternatives are exact: promote the E raw slice
through its own complete nuisance and response gates, or contradict C12/C21/
C22 with the complete physical equations.

GLS28 uses the exact GLS23 label types to compare the GLS27 essential supplier
with its receiving target.  With `omega=0`, all one-residual nuisance lies in
`P_Q(T_Q)`, every foreign promoted pair lies in its own root-support slice, and
the top nuisance vanishes.  Root separation therefore supplies a legal full
selector when a desired supplier leaves this envelope.  Separation of a
projected pure diagonal is stronger: GLS22 target coupling forces the named
nonzero physical response.  If this never occurs, the diagonal defect remains
spanned after deletion of any supplier label in a quotient of dimension at
most four.  Full absorption, but not merely useful-row failure, promotes this
to bounded supplier relations.

GLS29 resolves the physical scalar normal channel when both residual shore
ranks are two.  The product shore normal identifies the one-dimensional
quotient `E/P_Q(T_Q)` and sends each pair supplier to
`x_u tensor y_v+y_u tensor x_v`.  Applying it to the complete GLS23 nuisance
gives exact target cylinders; applying it to the complete physical equation
gives an arbitrary-root mixed-response identity.  Every active normal colour
has a supplier with nonzero pure coefficient and response and occurs in two
local channel spans.  Intersecting full-activity supplier support is excluded
at arbitrary root order.  At `r=3`, complementary-kernel contractions give
diagonal-versus-two-channel rank identities; the full local-rank cover,
including rank zero and zero star products, forces
`gamma_0 gamma_1 gamma_2=0`.  The surviving normal-product divisor, the
`r>=4` disjoint-supplier branch, other shore ranks, and C12/C21/C22 remain.
An exact rational graph-side control has all six nuisances full and responses
nonzero but fails mixed GHZ coefficients, so module/response gates alone do
not close the branch.

GLS30 profiles the one- and two-active normal-product divisor without losing
zero-star fibres.  Contracting every complement port by its exact local
channel kernel leaves one supplier and yields a diagonal-versus-supplier
identity at arbitrary root order.  At four promoted ports this gives the
one-active visibility fork and the complete two-active projected-kernel rank
rules.  Exact rational response decks realize both scalar identities with all
six responses nonzero and every normal nuisance image full.  A separate
one-active graph has a maximum torus root, defect four within the incidence
bound six, pure normalization, and the exact scalar normal tensor, but fails
Hamming-one coefficients.  This proves that the six-response/full-normal
implication is insufficient and separately that maximum-root/pure data do not
upgrade the scalar identity to a witness.  It does not refute a coupling of
maximum-root incidence with simultaneous scalar absorption and does not
produce a complete zero-anchor point because full top reconstruction, other
root contractions, and full target nuisance gates are not supplied.

GLS31 closes that proposed static coupling as an insufficiency route.  A
single exact graph combines maximum-root saturation, pure normalization, six
nonzero responses, full normal images, the scalar normal identity, the
projected diagonal inclusion, and simultaneous complete GLS23 absorption, but
fails `313` mixed coefficients.  The exact shore-normal evaluation pencils
show what remains: two first-polarized equations whose one-`Q` labelled terms
cannot be dropped.  Their retained-root quotient recovers only the old normal
product.  The next divisor obligation must therefore use those complete
mixed/root-deck equations pointwise, not another static incidence/module
coupling.

## 5. Dependency order for theorem packages

1. **Same-pair source package (merged as PR #235).**  Use the complete
   contracted mixed target to align individual order-two quotient survival
   and a raw two-root companion on one physical pair; record why scalar
   complementary permanents alone have no universal elimination identity.
2. **Failure-module/alignment package (reviewed current tranche).**  Give coordinate-free definitions of
   simultaneous observability failure and selector failure; prove equivalence
   to exact kernel-witness/determinantal conditions on the physical deck,
   including a divisor ledger.
   This tranche now supplies the radical--Fitting, projected-kernel,
   decomposable-retraction, common-contraction, and alignment criteria, plus
   exact abstract sharpness models.
3. **Four-root source-integration package (reviewed current tranche).**  Prove
   an exhaustive source split before any coefficient elimination.  This is now
   \(\{O,C\}\times\{R,E,A\}\), with both E leaves good and the four R/A
   leaves open for the stronger all-seven package.
4. **Promoted arbitrary-root one-row package (reviewed merged tranche).**  Re-root
   at the GLS4 probe pair, prove the two-grade top-depth module and exact
   pointwise failure criterion, and correct the all-seven versus one-row
   four-root topology.
5. **Full-rank literal-response-zero localization (merged as PR #238).**  Use
   the uncontracted rank-three residual block and complete contracted target to
   localize the all-seven zero branch, retaining the determinant ledger exactly.
6. **Full-rank pure-survivor exchange package (reviewed current tranche).**
   Compare two complete diagonal target fibres in the active-line quotient and
   exclude both the singleton and two-port GLS9 survivor.  The bounded exact
   729-coefficient probe is discovery provenance, not the arbitrary-point proof.
7. **Determinant-divisor six-response package (reviewed current tranche).**
   Exclude rank-zero and every rank-two quotient escape, derive the
   exactly-two-port conformal core and fixed blockers, give the exhaustive
   rank-one trichotomy, and keep the seventh-response quartic independent.
8. **Rank-two/singleton physical exclusion package (reviewed current tranche).**
   Use the complete target quotients to exclude the rank-two conformal core,
   and use common-incidence column splicing plus `subrank(P_4)=2` to exclude
   the rank-one singleton triangle.
9. **Rank-one two-port extraction package (reviewed current tranche).**
   Force the residual-colour coordinate pairing, derive the twelve common-tail
   identities, and splice them into one weighted \(P_5\to\Delta_3\)
   restriction.  This routes the branch to a downstream permanent node and
   does not claim \(P_5\) nonrestriction.
10. **Arbitrary-`h` common-line detector package (current tranche).**  Starting
   from the exact `GLD15` coefficient plane, prove
   \(aT'=C(D)-C(\eta K)\) for \(a=\delta+h\eta\) without division, audit the
   \(a=0\) divisor separately, and retain common-line existence and
   three-colour activity as explicit inputs rather than conclusions.
11. **Physical pair-transform package (current tranche).**  In the original
    fixed-`Q` chart and for arbitrary root order, identify the two pair desired
    columns as `Psi_C(K^Q),Psi_C(R)`,
    orient every rank-one quotient by the absorbed pencil member
    `delta R-eta K^Q`, and derive the denominator-free cross-target determinant
    and pure-target identities.  These identities expose, but do not yet kill,
    the projective transport defect.  This is not the GLS8 two-probe promoted
    target, whose target size is `2r-4`.
12. **Base-grade and cross-annihilation package (merged as PR #246).**  Evaluate
    the complete joint pair quotient at the maximum root, retain every other
    order-two slice, and prove that base survival orients rank one to pure
    `M` while rank zero and every other slope force a swallowed base circuit.
    Separately compare foreign labelled coefficients of simultaneous legal
    selectors and preserve the type distinction from transport membership.
13. **Partial-root all-even-target package (merged as PR #247).**  Leave exactly
    `t-1` roots open at a `2t`-target, identify the leading injection/permanent
    tensor and complete lower-grade nuisance, and force a common pure-`M` row
    whenever the leading classes survive.  Record the four-root four-port
     first-root gate and retain activity as a separate hypothesis.
14. **Leading-shadow target/Fitting package (merged as PR #248).**  Apply the
    partial-root shadows to the complete target equation, prove the exact
    response-gated rank-one identity, encode universal and simultaneous failure
    on all nuisance-rank fibres, and record the four-root full-shadow branch
    without claiming its exclusion.
15. **Residual-present top-shadow package (merged as PR #249).**  Leave `t`
    roots open, use the exact individual-`Z` nuisance, prove the explicit top
    tensor and common pure-`Z` route, and couple it to the complete target and
    all-rank failure profile without claiming survival or activity.
16. **Promoted transverse nuisance package (merged as PR #253).**  Expand every
    projected active label into its exact target coefficient-slice space,
    isolate disjoint-label fullness, and prove the common top-anchor
    zero/nonzero dichotomy without claiming either branch is excluded.
17. **Promoted one-probe anchor-marginal package (merged as PR #254).**  Derive
    the exact `8 -> 2 -> 1` root filtration, its nine-row physical pair
    modules, the double-transverse divisor, and the conditional `r=3` detector
    edge without equating marginal failure with full selector failure.
18. **Promoted double-transverse core package (merged as PR #255).**  Use the
    all-port tensor to project denominator-free onto the four-dimensional core,
    derive exact `27/4`-row physical modules, and retain every reduced/full
    failure and activity gate.
19. **Promoted zero-anchor reconstruction package (merged as PR #256).**  Use
    the complete top-target equation to reconstruct the projected diagonal
    space, isolate the exact residual-shore tangent, and split the branch into
    essential raw pair supply versus the coordinate-shore cover without
    claiming target survival.
20. **Zero-anchor residual-family and shore-normal-form package (merged as PR
    #257).**  Vary the residual pair, prove generic escape or one fixed
    coordinate-shore cover, classify the exact C12/C21/C22 normal forms, and
    retain the distinction between choosing one source contraction and a
    universal statement on every exceptional fibre.
21. **Zero-anchor target-envelope package (merged as PR #258).**  Prove the exact
    foreign-supplier envelope, the supplier- and diagonal-coloop selector
    criteria, and the bounded deletion-cover consequences without identifying
    useful failure with full absorption.
22. **Rank-two-shore normal-channel package (merged as PR #259).**  Identify the
    one-dimensional quotient normal, derive the exact nuisance cylinders and
    complete mixed-response identity, exclude intersecting full-activity
    support at arbitrary root order, and exclude the complete `r=3`
    full-activity locus without discarding rank or response fibres.
23. **Normal-product divisor profile package (current tranche).**  Isolate one
    supplier by complement-kernel contraction without division, derive the
    exact one-/two-active four-port profiles, and record same-graph controls
    proving the exact six-response/full-normal-image insufficiency boundary.
    Retain the coupling to maximum-root incidence, full top reconstruction,
    complete nuisance survival, and other root contractions as explicit open
    gates.
24. **Target exchange-syzygy package.**  Prove the cross-target transport defect
    is nuisance from the remaining physical failure conditions, use its nonzero
    pure-target identity to obtain a complete mixed contradiction, or exhibit
    and fully validate an exact countermodel that disproves the proposed
    implication.
25. **Remaining mixed and exceptional-fibre package.**  Exclude the surviving
    GLS29 normal-product divisor, the higher-root disjoint redundant cover, and
    the C12/C21/C22 witness loci, and prove
    any principal-open identity extends by a denominator-free argument, or
    exhaust every rank-drop/response-zero divisor by a finite **proved** cover
    and complete mixed contradictions.
26. **Closure audit package.**  Independent no-import replay, hostile
   proof-topology and case-cover audit, provenance/dependency records,
   `docs/current-frontier.md` update, full candidate-tree QA, exact-head hosted
   CI, and merged-main replay.

Packages may be combined when one proof supplies several edges.  They may not
be split into a micro-PR campaign over unproved support strata.

## 6. Bounded experiment and checkpoint disposition

Commit `39933ee8c7f09ec81d93e331734b89f5cbaea803` remains protected local
exploratory history and is not a first standalone PR.  It tests only two
support-preserving nonzero-weight controls: the GLS3 \(r=3\) control and the
GLD11 \(r=4\) control.  It retains the complete coefficient map for those
families, but it does **not** encode universal pair/Q-observability failure,
selector-kernel witnesses, determinantal ideals, GLD quotient legality,
alignment, or a saturated universal failure ideal.  If the computation later
becomes load-bearing, transplant it with that corrected scope and with the
schematic unimplemented ideal clearly labelled.

The new \(r=3,4\) formulation audit is recorded separately.  It encodes the
complete physical GHZ coefficient formula, but stops before locus elimination:
GLS2 failure is a function-field rank condition, while target-selector failure
has a fibrewise-for-all-torus-points quantifier, and maximum-root maximality is
itself a family of Laurent unit-ideal conditions.  Replacing those statements
by constant kernels, one generic contraction, or blocker quotas would define a
different locus.  The compact exact invariant for raw pointwise selector
failure is equality of every radical Fitting-rank ideal of the nuisance matrix
and its desired-column augmentation.  A physical mixed-coefficient consequence
of that invariant remains open.  In accordance with the stop rule, no rank or
support atlas was launched.

The later
[four-root pure-survivor exact probe](R4_PURE_PI_SURVIVOR_EXACT_PROBE_2026-08-20.md)
materializes all 729 coefficients of the relaxed GLS9 contracted target.  It
excludes 216 singleton charts by one exact saturated minor and 648 covering
two-port charts by the final pure coefficient.  It found no target point or
counterexample.  This finite computation exposed the compact two-fibre
quotient identity used in the reviewed theorem; it neither proves physical
integrability nor replaces the arbitrary-point written proof.

An apparent exact global counterexample, contradiction with a live theorem, or
nonexhaustive claimed cover triggers immediate dedicated adversarial escalation.

## 7. Node-closure checklist

The node is closed **iff** every box below is discharged:

- [x] The same-pair source theorem quantifies over every actual complex maximum-root
      surplus-two witness and every allowed root size \(r\ge3\).
- [ ] Every source point is mapped pointwise either to one useful-\(Q\) output
      or to a complete-mixed-coefficient contradiction on the same graph.
- [x] At least one same \(Q\) has a nonzero physical pair block, quotient-column
      survival modulo every relevant higher column, and raw \(p(A,Q)\neq0\).
- [x] Every root order has a promoted fixed-\(Q\) legal-target module and an
      exact pointwise failure profile; this is a reduction, not target supply.
- [x] The reviewed two-complete-fibre theorem excludes the root-order-four
      literal all-seven response-zero branch on `det H_Q!=0`.  Focused replay,
      the independent no-import audit, and hostile review pass.  The determinant
      divisor remains open.
- [x] The reviewed determinant-divisor theorem has reduced the simultaneous
      six-pair-zero divisor to exact rank-two/rank-one cores and isolated the
      seventh-response quartic.  The reduction itself leaves those cores;
      their status is refined by the follow-up below.  Focused primary,
      independent no-import replay, and hostile review pass.
- [x] The reviewed follow-up excludes the rank-two core and the rank-one
      singleton triangle.  Focused primary, independent no-import replay, and
      hostile review pass.
- [x] The reviewed continuation routes the rank-one two-port Branch III
      pointwise to one weighted \(P_5\to\Delta_3\) restriction and proves its
      seventh response termwise zero.  Primary, no-import replay, and hostile
      review pass.  The downstream \(P_5\) theorem is outside this node.
- [x] The reviewed contained/one-sided continuation exhaustively routes the
      remaining six-response rank-one Branches I and II to exact pure
      \(P_4/P_5\) compression interfaces, a \(\Phi\ne0\) response-visible
      selector obligation, or the aligned \(\Phi=0\) augmented-\(P_6\) face
      defect \(\Psi\).  Focused primary, independent no-import replay, and
      hostile review pass.  None of those residual interfaces is closed.
- [x] The common-projective `GLD16` identity is denominator-free for arbitrary
      physical residual scalar \(h\): with \(a=\delta+h\eta\), the selected
      package obeys \(aT'=C(D)-C(\eta K)\), and three-colour activity excludes
      both \(a=0\) and \(a\ne0\).  This closes only the conditional common-line
      detector's old `h=0` restriction; it does not force the common line or
      activity.
- [x] The arbitrary-root physical pair-companion columns have one common
      transform: `g_M=Psi_C(K^Q)` and `g_Z=Psi_C(R)`.  Rank-one absorption and
      cross-target slope disagreement are now the exact denominator-free
      transport classes of GLS15, including pure axes and incidence-rank-drop
      fibres.  The theorem does not prove transport into a foreign nuisance,
      cover joint rank zero, synchronize the four-port line, force activity,
      or integrate the distinct GLS8 promoted interface.
- [x] The maximum-root base shadow maps each original fixed-`Q` pair `M`
      column to its complementary permanent, kills its `Z` column, and maps
      the complete joint nuisance to every other order-two slice.  Hence base
      survival excludes `k=0` and forces rank one to pure `M`; every remaining
      pair slope or rank-zero target lies on an explicit base-swallowing
      circuit.  Foreign labelled cross-annihilations are proved, but are not
      foreign nuisance membership.  Primary, no-import audit, and hostile
      review pass.
- [x] The partial-root grade shadow now covers every original fixed-`Q` even
      target: for `|S|=2t`, leaving `t-1` roots open kills `g_S^Z`, exposes an
      explicit leading tensor for `g_S^M`, and retains every lower-or-equal-
      grade nuisance label.  Leading survival forces a legal pure-`M` row;
      finite-family survival gives one common direction, including the
      explicit `r=4` four-port first-root gate.  Survival and GLD16 activity
       remain open.  Primary, no-import audit, and hostile review pass.
- [x] Every partial-root leading shadow is coupled to the complete target
      equation: its pure quotient rank is at most one and rises exactly when
      the desired leading class and physical `M` response are both nonzero.
      Universal and finite-family failure have exact all-rank geometric
      radical--Fitting profiles.  At `r=4`, four-port failure is four full
      first-root nuisance shadows.  The profiles are not proved empty and
      activity remains open.  Primary, independent no-import audit, and
      hostile review pass.
- [x] Every original fixed-`Q` even target also has an exact residual-present
      top shadow: leaving `t` roots open kills higher grades, while the
      individual-`Z` quotient exposes an explicit top tensor.  Survival gives
      pure `Z`, and the complete target makes usefulness equivalent to top
      survival plus nonzero `Z_S` on every rank fibre.  At `r=4`, seven useful
      top rows give the common line with `a=h`, but activity and survival remain
      open.  Primary, independent no-import audit, and hostile review pass.
- [x] Every promoted GLS8 source Laplace pair has an exact nine-row base
      quotient.  Base survival gives a legal factor-through selector, the
      complete target couples it exactly to response nonvanishing, and all-rank
      failure is an exact Fitting profile.  The source forces a raw nonzero base
      term but not quotient survival.  Primary, independent no-import audit,
      and hostile review pass.
- [x] The nine-row factor-through route has been audited against the complete
      nuisance: the retained all-port label contributes `pI_9`, so the quotient
      is identically zero on `p!=0`.  This is a proved no-go, not full GLS8
      absorption.  Primary, independent no-import audit, and hostile review
      pass.
- [x] The full promoted quotient has an exact legal transverse reduction on
      `D(p)`: `81 -> 72` rows for top-minus-two targets and `9 -> 8` for the
      top target, with selector equivalence, target coupling, source aggregate,
      and all exceptional Fitting fibres preserved.  No transverse survival or
      response is forced.  Primary, independent no-import audit, and hostile
      review pass.
- [x] The complete promoted transverse nuisance has an exact physical
      label-slice decomposition.  Disjoint root-slice fullness is a sufficient
      pair-target absorption mechanism, and the common top anchor gives an
      exhaustive zero/nonzero split with exact `63/8`-row residual tests.
      Neither branch is excluded, and proper/overlap slice spans remain open.
      Primary, independent no-import audit, and hostile review pass.
- [x] A nonzero actual-root top-anchor marginal gives an exact common
      denominator-free `9`-row factor-through module for every pair target.
      The zero/nonzero-marginal/double-transverse anchor split, complete-target
      rank, Fitting failures, and aggregate synchronization are exact.  At
      `r=3`, only the all-six-useful plus top-useful plus three-active leaf is
      excluded by `GLD3`; all other branches remain.  Primary, independent
      no-import audit, and hostile review pass.
- [x] The nonzero double-transverse anchor branch has an exact
      denominator-free rank-four core projector, `27`-row pair modules, and a
      four-row top module.  Target rank, all-rank failure, and core
      synchronization descend.  At `r=3`, only the all-six-core-useful plus
      top-core-useful plus three-active leaf is excluded; reduced failure is
      not full failure.  Primary, independent no-import audit, and hostile
      review pass.
- [ ] The same \(Q\) carries the complete legal response/target package needed
      by the named downstream attachment theorem.
- [ ] Constant-selector, full-nuisance, synchronization, augmented-weight,
      alignment, nonzero-response, and target-pure-anchor hypotheses are each
      proved or are explicitly inapplicable to the chosen downstream entry.
- [ ] Generic, exceptional rank-drop, response-zero, support-divisor, and all
      \(k=0,1,2\)/slope/visibility branches are covered without silent division.
- [ ] Any finite atlas has a proved source-level coverage theorem; otherwise it
      is not used as the extraction edge.
- [ ] Every bad leaf is excluded by complete mixed GHZ coefficients, not by a
      formal deck, sampled evidence, or a response fixture.
- [ ] Exact statement/proof, focused verifier, genuinely independent no-import
      audit, hostile scope/case-cover review, and provenance are committed.
- [ ] Every load-bearing PR is merged; exact merged-main candidate-tree QA and
      hosted CI are green.
- [ ] `docs/current-frontier.md` records an explicit closed source-to-interface
      edge and still states that the global conjecture is **UNRESOLVED**.
- [ ] An independent adversarial audit agrees that no generic, exceptional, or
      failure branch was omitted.

Only then may this long-running goal be marked complete.  The next permanent,
extraction/gluing, or resolution node must not be started under this mandate.
