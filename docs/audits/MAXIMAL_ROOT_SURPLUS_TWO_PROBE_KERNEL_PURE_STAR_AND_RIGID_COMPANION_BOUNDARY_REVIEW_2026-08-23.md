# Hostile review: probe-kernel pure star and rigid-companion boundary

## Verdict

**ACCEPT as a scoped structural theorem and exact receiver no-go, conditional
on the required repository replay and exact-head hosted CI below.**

`GLS56` proves that every nonrigid auxiliary label in the complete `GLS8`
two-probe chart has a three-colour coordinate-pure escape on its full joint
probe kernel.  The pointwise escape has a fixed-neighbour generic refinement,
a denominator-free flag covering every exceptional torus section, and a
simultaneous fully supported kernel point activating one fixed shore in each
colour.

The same package proves two sharp attachment boundaries.  Rigid coordinate
readouts can cancel pairwise through a pure-axis or exact projective
anti-synchronization alternative.  More strongly, re-anchoring the nonrigid
label and one old probe as the residual pair of the natural `GLD3` window
puts `h=0`; if the triangle of star-neighbour pair responses is
target-diagonal, all three responses are zero.  Thus this transport cannot
meet `GLD3`'s three-colour pair-depth activity gate.

This is not a response, selector, target attachment, source cover, or
receiver entry.  The maximum-root surplus-two strategic node and the global
Krenn--Gu conjecture remain **UNRESOLVED**.

## Reviewed artifacts and owners

- [`GLS56 theorem`](../../claims/arbitrary-order/MAXIMAL_ROOT_SURPLUS_TWO_NONRIGID_PROBE_KERNEL_THREE_COLOUR_PURE_STAR_ESCAPE_THEOREM.md)
- [`focused exact primary verifier`](../../claims/arbitrary-order/verify_maximal_root_surplus_two_nonrigid_probe_kernel_three_colour_pure_star_escape.py)
- [`independent no-project-import audit`](../../claims/arbitrary-order/audit_maximal_root_surplus_two_nonrigid_probe_kernel_three_colour_pure_star_escape.py)
- complete promoted matching identity and physical deck typing: `GLS8`
- five-rigid-label floor used only for the root-order-three zero-anchor
  corollary: `GLS55`
- historical pointwise antecedent:
  [`THREE_COLOUR_HYPERPLANE_ANNIHILATION_THEOREM.md`](../../claims/arbitrary-order/THREE_COLOUR_HYPERPLANE_ANNIHILATION_THEOREM.md)
- downstream interface checked against:
  [`GLD3`](../../claims/arbitrary-order/TWO_RESIDUAL_PAIR_FOUR_PORT_DIAGONAL_INTERFERENCE_AND_CAMOUFLAGE_BOUNDARY_THEOREM.md)

The older hyperplane-annihilation theorem already proves the unrestricted
pointwise coordinate-killer mechanism and a generic fixed whole-domain
edge.  `GLS56` does not relabel that fact as new.  Its new kernel-restricted
content is nonvanishing on `ker J_n`, complete exceptional-section coverage,
simultaneous three-colour activation, the rigid-companion boundary, the
root-order-three bifurcation, and the exact `GLD3` no-go.

## Quantifier and matching audit

Fix any complete promoted identity over a characteristic-zero field, any
auxiliary label `n` with

```text
K_n=ker(W_(a_0,n),W_(a_1,n)),
K_n intersect (K^*)^3 != empty,
```

any fully supported `k_n in K_n`, and any target colour `c`.  The algebra is
field-general in this conditional sense.  The repository's live maximum-root
physical-source application remains over `K=C`; the theorem does not
silently assert source existence over every characteristic-zero field.

If no edge
`W_(n,t)(k_n,-)` is a nonzero `c`-coordinate covector, the elementary
covector alternative supplies, independently for every `t!=n`, a vector
`k_t` killing that edge while retaining its `c` coordinate.

Every pair term in the complete `GLS8` identity then dies in exactly one of
two ways:

1. if the selected pair contains `n`, both probe shores at `n` vanish on
   `k_n`;
2. otherwise every perfect matching of the complementary deck pairs `n`
   with another contracted label, and that edge was killed.

The same matching argument kills the top deck even when the probe anchor is
nonzero.  The pure target coefficient is the product of the retained
`c` coordinates and is nonzero, a contradiction.  This uses the complete
physical identity, not a support atlas or a selected subdeck.

The primary verifier recursively enumerates every complementary-deck and top
matching through root order seven; pairs containing `n` are discharged
symbolically by `J_n(k_n)=0`.  The independent audit rebuilds the deck/top
cover with integer bit masks and reversed recursion.  The written argument,
not the bounded census, carries arbitrary root order.

## Uniformity and exceptional fibres

For fixed `c`, each pure-shore condition on `K_n` is a closed linear
subspace.  The pointwise theorem gives a finite cover of the dense torus
inside the irreducible space `K_n`.  Therefore one neighbour is pure on all
of `K_n`, and the corresponding restricted map is nonzero.  The three
colours require distinct neighbours because a nonzero image cannot lie in
two coordinate lines.

The three nonzero scalar functionals and the coordinate functions cut only
finitely many proper hyperplanes, so one torus point activates all three.
This is a simultaneous-existence statement, not pointwise nonvanishing of
one fixed shore.

On an exceptional kernel section where a chosen scalar vanishes, repeat the
same finite-cover argument inside that linear section.  Each step cuts the
dimension strictly, so the flag stops after at most `dim K_n` steps.  No
response, incidence minor, shore scalar, or divisor equation is inverted.

The exact rank-one example

```text
K={(x,y,x+y)},
first shore=(x-y)e_0,
successor shore=y e_0+(x-y)e_1
```

shows why this flag is necessary: the first fixed shore dies on the torus
divisor `x=y`, while the successor is still nonzero there.  The primary
checker audits exact rational points; the independent checker uses an
exhaustive `F_5` torus census.  These computations audit the section logic;
the written irreducibility proof carries characteristic zero.

## Rigid-companion and synchronization audit

For two labels using a common rigid coordinate `c`, the same-coordinate
pair coefficient is

```text
Gamma_(s,t,c)=x_(s,c) tensor y_(t,c)
              +x_(t,c) tensor y_(s,c).
```

Equality of two nonzero decomposable tensors gives the exact trichotomy:
common pure first-probe axis, common pure second-probe axis, or

```text
x_t=lambda x_s,
y_t=-lambda y_s.
```

For three labels, the last alternative would give three pairwise slope
relations `rho_s+rho_t=0`; characteristic different from two makes this
impossible unless all labels are on one common pure axis.  The independent
checker uses projectively normalized `F_5` rows rather than the primary
checker’s exact integral row census.

The homogeneous common-probe identity was expanded coefficientwise.  Its
projective determinant is a real synchronization defect, but `GLS55` does
not provide the common contractions or annihilate the two square-term
nuisances.  The exact divisor `Gamma=(z+1)p tensor q` retains both probe axes
at `z=-1`; it forbids any generic-only synchronization claim.

## Exact `GLD3` interface audit

At the simultaneous star point, write

```text
a_i=lambda_i e_i^*,
b_i=W_(a_sigma,t_i)(z,-).
```

Re-anchor at residual pair `{n,a_sigma}`.  Because the chosen vector at `n` lies
in the full joint probe kernel,

```text
h=W_(n,a_sigma)(k,z)=0,
D_ij=a_i tensor b_j+b_i tensor a_j.
```

This is exactly `GLD3`'s residual-present pair response `D_ij=K_ij` on its
`h=0` fibre.  If all three star-triangle responses are target-diagonal, the
off-diagonal equations first force `b_i=mu_i e_i^*`.  They then give

```text
lambda_i mu_j+mu_i lambda_j=0
```

for all three pairs.  Division uses only the declared nonzero star scalars;
the normalized three-by-three system has integer determinant `-2`, so every
`mu_i` vanishes in characteristic zero.

Every perfect matching on the three star neighbours plus a fourth port uses
one star-triangle edge.  Hence every complementary pair-depth product is
zero.  The result does not enter `GLD3`; it proves that this natural transport
lands on the opposite side of its load-bearing activity gate.

## Receiver and gate table

| Candidate route | Exact supplied data | Missing or failed gates | Verdict |
|---|---|---|---|
| `GLD3` with residuals `{n,a_sigma}` | three pure shores, `h=0`, exact physical pair formula | legal constant selectors, target attachment, nuisance survival, and the four-port row required by the currently recorded full attachment package; if the triangle is diagonal, pair-depth activity is identically zero even though GLD3's `h=0` rank contradiction itself would not need `T` | **NO-GO for the natural re-anchor** |
| `GLD5` / `GLD7` / `GLD9` | direct auxiliary edge shores only | named same-`Q` quotient classes, constant normalized selectors, one common contraction, all seven responses, activity | **type mismatch** |
| `GLD15` / `GLD16` | no physical joint quotient operator is supplied | common projective line, synchronized pair and four-port rows, selected-response activity, complete nuisance annihilation, target anchor | **type mismatch** |
| `GLS8` promoted target module | one nonrigid-label structural escape | nonzero promoted response, quotient survival, complete labelled nuisance annihilation, legal selector | **not an attachment** |
| root-order-three all-six-rigid branch | six coordinate readouts | nonzero diagonal companion, response, synchronization, nuisance survival, anchor, complete mixed/deck coupling | **open** |
| root-order-three unique-nonrigid branch | one simultaneous three-colour star into rigid labels | a different legally transported receiver or complete-equation exclusion of the zero-triangle/low-activity cell | **open after the natural-route no-go** |

No named committed receiver accepts only one coordinate-pure direct edge
family or a rigid coordinate readout.  A one-row quotient criterion would
not cure these missing gates.

## Sharp controls and counterexample audit

The sparse rational eight-vertex graph in the theorem has one nonrigid label,
five rank-one rigid labels, and the complete three-colour star.  It also has
nonzero `H_Q`, raw pair companion, and complementary permanent, but all six
promoted pair responses and the top response vanish.  Its full tensor has
exactly one nonzero mixed word, of coefficient `-2`, and no pure target word.
It is therefore an off-target physical control, not a Krenn--Gu witness or an
exact counterexample.

Two all-six-rigid controls separately show:

- rank-one coordinate readouts can cancel every same-colour pair through
  opposite signs;
- injective rank-three joint maps with both probes active can have pair
  companions supported only off the target diagonal.

These are incidence/companion interfaces, not complete witness tensors.  The
primary checker uses exact rationals and a matrix graph implementation.  The
independent checker uses modular rows and a separately written sparse
integer graph evaluator.  Both reproduce the response zeros and the unique
off-target full word.

## Required replay

```text
python claims/arbitrary-order/verify_maximal_root_surplus_two_nonrigid_probe_kernel_three_colour_pure_star_escape.py
python -I claims/arbitrary-order/audit_maximal_root_surplus_two_nonrigid_probe_kernel_three_colour_pure_star_escape.py
python claims/arbitrary-order/verify_two_residual_pair_four_port_diagonal_interference_and_camouflage_boundary.py
python -I claims/arbitrary-order/audit_two_residual_pair_four_port_diagonal_interference_and_camouflage_boundary.py
python claims/arbitrary-order/verify_maximal_root_surplus_two_zero_anchor_torus_kernel_contraction_and_five_rigid_label_floor.py
python -I claims/arbitrary-order/audit_maximal_root_surplus_two_zero_anchor_torus_kernel_contraction_and_five_rigid_label_floor.py
python claims/arbitrary-order/verify_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
python -I claims/arbitrary-order/audit_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
```

The package must also pass full candidate-tree validation, exact-head hosted
CI, safe merge, and fresh merged-main replay.

```text
GLS56 base HEAD: 6afc0e2e0b453629c58b77b48d99905e1f5dfdfd
```

The protected authority checkout and protected exploratory worktrees remain
read-only and are not dependency inputs.

## Exact unresolved boundary

At root order three on the zero-anchor branch, `GLS55` and `GLS56` give the
exhaustive structural split:

1. all six auxiliary labels are rigid;
2. exactly one is nonrigid and has a simultaneous three-colour pure star to
   three rigid neighbours.

This is not an attachment case cover.  The first branch still needs complete
mixed/deck coupling beyond the pure-axis, anti-synchronization, and
full-rank off-diagonal controls.  In the second branch, the smallest
load-bearing obligation is to use the complete mixed GHZ equations either to
exclude the target-diagonal zero-triangle/low-activity cell or to construct a
different same-graph receiver with every selector, response, synchronization,
nuisance-survival, activity, and anchor gate proved.

For `r>=4`, `GLS55` does not force the pure-star neighbours to be rigid and
no named detector accepts this structure alone.  Nonzero-anchor branches,
raw-escape/source coverage, arbitrary-root attachment, strategic-node
closure, permanent restriction, and global resolution all remain open.
