# Protected full-source parent review

Date: 2026-09-22.

Status: **PASS** after independent mathematical and promoted-file review.
All ten advertised replay commands passed from the repository root and from
a neutral working directory. The accepted scope is exact identities and route obstructions,
not exclusion of the full protected family or the global conjecture.

The reviewed owners are
[component elimination and Ward boundary](../../claims/arbitrary-order/PROTECTED_COMPONENT_ELIMINATION_AND_WARD_BOUNDARY.md)
and [paired-minority source and repair boundary](../../claims/arbitrary-order/PROTECTED_PAIRED_MINORITY_SOURCE_AND_REPAIR_BOUNDARY.md).
An independently assigned research agent rederived the load-bearing
arguments and replayed the finite controls separately from their authors.
This is an agent audit, with no claimed human peer review or Lean proof.

## Elimination and Ward transport

The contraction uses arbitrary hollow oriented crossing blocks, with only
the reverse orientation constrained to be the transpose. The boundary
polynomial, the 81 local rows, and the legal quadratic correction are
defined with the same actual exterior quadratic and incidence pencils.
The relative-kernel equation is equivalent to the stated legal elimination
step. Its supply from a full source remains open.

The reviewed controls distinguish literal normalization from a scalar
surrogate: the canonical `K_2` and `K_2/3` updates give respectively zero
and `(4/9)G` on the displayed controls. These k=2 proper-subsystem arrays do
not refute the k>=3 full-source existential parent. A single K4 remains a
valid source, and its empty-exterior contraction has scalar factor three.

Ward transport retains the derivative of the boundary and every transformed
target. The Casimir eigenvalue is used only on the symmetric target
subrepresentation. Fixed-vertex pencil intertwining is distinguished from
more general operations mixing removed vertices.

For unrestricted local `gl_3`, physical-support Euler projectors and local
matrix units give the written module decomposition. Conjugation by
`exp(Q)` transports it to the Ward module. The absent transformed top
support is exactly the desired elimination condition for the proposed
correction. Raw top degree and top response are different. This proves
the method boundary: pointwise module membership does not independently
supply elimination. A uniform polynomial syzygy is sufficient; a fully
covered rational stratification could also work. Neither is provided.

## Paired-minority identities and controls

For one complete minority pair, counting minorities and exposed majority
partners gives `h+t=1`, where `h` counts direct minority edges and `t`
opened protected resources. This proves the exact permanent-minor/seam
formula at every order. Its finite symbolic replay covers all 6,000
applicable eight-vertex word/color pairs and 60 twelve-vertex samples.

For two complete pairs, the count gives `h+t=2`. The only nonphysical
terms in ordinary hafnian completion reuse one resource, so subtracting
its resource hafnian gives the exact correction. The correction is
generally nonzero, not necessarily nonzero at every specialization.
All 7,200 applicable eight-vertex cases replay; 96 display the error made
by omitting the correction.

The protected-seam factorization follows from literal internal zero rows.
A nonzero supplier means its two-term permanent is nonzero, not merely
that one supporting entry or monomial exists. The 192 finite symbolic
instances pass. Degree-two macro circuits alone do not supply this
nonvanishing premise.

The independent literal enumerator reconstructs the 22-entry k=3 support
and finds 605 terms on 401 words, all 24 mixed macro fibres covered, no
nonempty independent-minority term, and 260 other uniquely supported
mixed words. The crucial P1 failure is established by its 56 one-pair
singletons, including the displayed `0020|0000|1100` witness with
`q=(1,4,5)`. A witness with all three pair depths greater than one would
establish only UPM failure and is insufficient for that claim.

Deletion checks establish inclusion-minimality of this particular macro
support, not minimum cardinality over all supports. Its three whole port
graphs are DAGs. This refutes the proposed SES implication from macro
coverage plus PSMR, not P1 itself or a weighted-source statement.

The PSFD check concerns the 248-vertex base only: 744 total scalar support
entries, no independent directed cycle, and a twelve-minority q=1 word
with exactly one 124-edge matching. The original fixed-depth owner supplies
its macro-failure control. No persistence of this short word in every
high-girth lift is asserted.

## Rooted circuits and stationary repairs

In a supported q=1 word, every nontrivial component of `M union P_c` must
contain the complete minority pair; otherwise it supplies a forbidden
q=0 word. There is exactly one such component and it contains all
minorities. If a same-word repair component is protected-pair saturated,
it is closed under `M union P_c`, and meeting any minority means containing
all of them. This obstructs proper same-word minority-set descent without
any minimal-depth assumption. It does not rule out cross-word operations
or valid assemblies of unsaturated boundaries.

The stationary v3 control has two opposite Laurent terms on
`0012|2212`, with the identical path `2 -> 6 -> 0`. Changing only the
closure square makes the actual source coefficient zero. This refutes
automatic path-shortening from a vanishing q=1 equation. Neither that
control nor the k=3 support satisfies every P1 premise.

## Residual scope

All-order status belongs to the written algebraic and combinatorial
arguments. Finite scripts do not prove an arbitrary-order exclusion.
There is no independent certificate of a new k=3 SAT exclusion, no P1
support countermodel, no full weighted source, no proved elimination
supply, and no arbitrary-witness-to-scaffold reduction. Those boundaries
are retained in both owners and the current frontier.
