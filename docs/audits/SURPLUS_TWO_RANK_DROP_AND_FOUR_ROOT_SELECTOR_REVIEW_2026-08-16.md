# Surplus-two rank-drop and four-root selector review

## Review status

**Repaired candidate package; final frozen replay pending below.**  This review
covers two exact characteristic-zero theorem packages:

1. the maximum-root surplus-two nonzero raw pair companion and physical
   rank-drop sharpness theorem; and
2. the four-root adjacent-grade constant-target-selector and single-shore
   cleanness boundary.

The first theorem's universal witness application is over `C`, matching its
owning maximum-root incidence theorem.  The second theorem is an algebraic
characteristic-zero statement.  Neither theorem supplies every paired GLQ2
window, extracts a weighted permanent restriction, or resolves the global
conjecture.  The Krenn--Gu conjecture remains **UNRESOLVED**.

## 1. Exact claims accepted for review

The surplus-two theorem proves:

- order-two sensor coordinates are identifiable exactly when their columns
  are independent modulo all order-four-and-higher nuisance columns;
- every blocker-saturated maximum-root surplus-two hypothetical complex
  witness cell has some nonzero raw residual-assignment companion
  `p_(A,Q)=per H_(A,Q)`;
- this raw `p_A` is not the augmented detector weight `l^T Jp`;
- a rational sensor kernel is not automatically a physical graph fibre; the
  exact matching-secant deck decides physical integrability; and
- one exact maximum-root, quota-saturated, locally concise `r=3` family has
  normalized pure coefficients, zero Hamming-one shell, rank-seven sensor,
  failure of every fixed-`Q` criterion, and a three-torus same-state physical
  pair fibre.  The mixed coefficient `02120111=1` excludes the family, so it
  is not a witness.

The four-root theorem proves:

- the exact zero-root-edge and one-root-edge decompositions (9)--(10);
- for `h!=0`, one zero-grade equation removes the direct response nuisance
  exactly when `l-kappa p` lies in `ker mathcal U`;
- augmented nonvanishing `l^T Jp!=0` does not force that alignment;
- function-field inversion need not preserve the diagonal target space;
- conditional on constant target-derived `Y_0,Y_1`, and on either constant
  target supply of all correction tensors or an exact target-diagonal
  correction identity, a nonzero mixed corrected response is an explicit
  nonzero constant combination of mixed GHZ coefficients;
- the six-column open-residual assignment fan can have rank six;
- one single-shore coefficientwise-clean ansatz with nonzero augmented weight
  has zero corrected response; this is not a global selector-count lower
  bound; and
- a maximum-root, six-triple-blocker route control has augmented weight one,
  nonzero corrected response, and both adjacent matching grades zero by
  nested cancellation.  Its pure coefficients are zero and another mixed
  coefficient is four, so it is not a witness.

## 2. Dependency and circularity audit

The surplus-two result depends on:

- the maximum-cardinality torus-root saturation and corank-six incidence
  theorem over `C`; and
- the uncontracted surplus-two complete-deck companion sensor.

The four-root result depends on the same companion-depth organization and on
the same-graph target-selector boundary, but its displayed identities are
proved directly by perfect-matching partitions.  Neither package imports a
permanent/co-two theorem.  Such an import would be circular because those
theorems assume the permanent restriction that Universal Supply is intended
to extract.

The raw-companion theorem supplies some `p_A!=0`; it does **not** supply a
legal constant root-edge selector `l` with `l^T Jp!=0`.  That augmented
nonvanishing, cross-depth alignment, constant target purity, and nuisance
control remain distinct obligations.

## 3. Primary exact verification

The surplus-two primary verifier:

- constructs the rank-seven injection sensor and every fixed-`Q` failure;
- enumerates the complete eight-vertex state symbolically;
- checks the maximum-root incidence data, local-concision flattening minors,
  pure coefficients, Hamming-one shell, and mixed detector; and
- proves no supported matching uses any of the three variable invisible
  edges, establishing the full algebraic three-torus fibre.

The four-root primary verifier:

- expands the generic ten-vertex hafnian with SymPy and separates all `360`,
  `540`, and `45` matchings in root-edge grades zero, one, and two;
- checks the two displayed decompositions as polynomial identities;
- derives the six assignment columns from the four vectors and checks
  determinant absolute value eight;
- constructs and replays the physical nonalignment control with
  `l=e_12`, `p=e_34`, and independent `U_12,U_34`;
- checks the rational target-purity countercontrol and both scoped
  single-shore controls;
- replays the nested cancellation; and
- verifies the six triple-blocker incidence ranks and decisive non-witness
  coefficients of the written maximum-root upgrade.  Maximum-rootness itself
  is discharged by the theorem's support argument.

## 4. Independent no-import audits

The surplus-two audit imports neither SymPy nor the primary.  It uses
root-word incidence permanents, an independent `Fraction` row reduction, a
bit-mask target-word recurrence, and a separate structural perfect-matching
enumeration proving that no supported matching contains an invisible edge.
This last check establishes parameter-independent constancy, not merely
agreement at two sample points.

The four-root audit imports neither SymPy nor the primary.  It generates all
`945` perfect matchings, partitions them by a separate root-assignment ledger,
and verifies disjoint category counts.  It derives the assignment fan from
the four vectors using `Fraction` outer products and independent elimination,
constructs the physical alignment records, audits target-purity in Laurent
exponent form, and replays both cancellation controls with rational matching
weights.  For the written maximum-root upgrade, its bit-mask coefficient
recurrence independently checks the selected, pure, and mixed coefficients;
the maximum-root support proof remains mathematical rather than computational.

These bounded programs replay the displayed identities and controls.  They
are not the proofs of the arbitrary-order incidence theorem and do not prove
that every witness supplies the required target selectors.

## 5. Hostile review and repairs

Three read-only hostile lanes attacked the package.  The frozen pre-repair
snapshot was rejected for the following material reasons, all repaired here:

1. raw `p_A` had been conflated with augmented `l^T Jp`;
2. the conditional detector did not require `Y_0,Y_1` to be target-derived in
   every branch;
3. a scoped single-shore no-go had been overstated as a global three-selector
   lower bound;
4. the displayed cross-clean control did not attain the theorem's
   coefficientwise bound;
5. the physical nonalignment control was not fully specified or replayed;
6. the assignment functional's necessity lacked its formal coefficientwise
   quantifier;
7. the complex field scope and maximum-cardinality root quantifier were not
   explicit;
8. the independent assignment-fan audit had hard-coded the primary matrix;
   and
9. the independent fibre audit had compared only two parameter values.

The frontier diagram was also repaired to retain the general non-four-root
`GLD1 -> GL` branch.  The four-root node is a strict refinement, not a
replacement for every detector route.

External Qwen and Claude OMP reports were treated only as conjecture and
adversarial steering sources.  They are not counted as verification evidence.
Their pair-depth cancellation controls were not witnesses because they failed
the four-port target equations.

## 6. Exact route boundary after review

What is proved:

```text
some raw p_A!=0 on every maximum-root s=2 complex witness cell;
pure + Hamming-one + maximum-root incidence do not force pair supply;
one exact higher mixed coefficient can exclude a full physical fibre;
four-root adjacent matching grades have the displayed decomposition;
one coefficientwise-clean shore has trivial corrected response;
augmented nonvanishing can coexist with complete nested cancellation.
```

What remains open:

```text
force a useful pair-observable or Q-observable physical quotient;
legally supply a constant l with l^T Jp!=0;
force cross-depth alignment and constant target-pure selectors;
kill or target-supply every correction tensor;
find a smaller multi-shore/aggregate selector package or use h=0;
attach a synchronized corrected channel to a weighted permanent restriction;
handle every higher-surplus cell.
```

The proposed next bounded calculation is the exact full two-port plus
four-port mixed target ideal, stratified by `h`, response rank, and minimal
support.  Pair-depth-only controls are insufficient.  A separate serious
breadth lane is cross-cut diagonal interference between two uncontracted
same-graph sensors; contracted determinant ratios are not valid in the
truncation range.

## 7. Frozen hashes and final replay

The repaired theorem and replay surfaces were frozen at these SHA-256 hashes:

```text
4D8A69609AF34F06B3D326C9003655BA5212AB3A7239F5BC7895E3D009E4E776  MAXIMAL_ROOT_SURPLUS_TWO_NONZERO_PAIR_COMPANION_AND_PHYSICAL_RANK_DROP_SHARPNESS_THEOREM.md
083937C177CE626A2D69401E46F4F1808D180D57F45C6A59814155F74DFEEE4B  FOUR_ROOT_PAIRED_GRADE_CONSTANT_TARGET_SELECTOR_AND_SINGLE_SHORE_CLEANNESS_BOUNDARY_THEOREM.md
9ADCEB11826D17D61F0CAD5495DBE5AC8CED342DF51470D1C6EBC42E05574EC4  verify_maximal_root_surplus_two_nonzero_pair_companion_and_physical_rank_drop_sharpness.py
B2F556264E9983EE824D307478545FD448B5D26F855EA588DE13A8F1DE440A46  audit_maximal_root_surplus_two_nonzero_pair_companion_and_physical_rank_drop_sharpness.py
C22614F46BC85D0C040449835E6783125979A7DCC1CA4D1B080E635E14602B3A  verify_four_root_paired_grade_constant_target_selector_and_single_shore_cleanness_boundary.py
7E603A470C54BEF8483E534A6F7E062D512A96DF372E336821B5D1058DBD9852  audit_four_root_paired_grade_constant_target_selector_and_single_shore_cleanness_boundary.py
```

On the staged candidate tree, all four focused replays passed, Ruff check and
format check passed, `check_hygiene.py` passed, the migration suite passed
`191/191`, the cycle-cover lattice suite passed `14/14`, and the link rewriter
reported zero changes.  The final cached diff passed `git diff --check` and
the worktree had no unstaged change.  Three read-only hostile reviews accepted
the repaired mathematical scope, proof topology, and evidence independence.

These results certify this scoped package only.  They do not supply the open
constant target selectors, do not extract a weighted permanent restriction,
and do not resolve the Krenn--Gu conjecture.
