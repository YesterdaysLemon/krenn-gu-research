# Hostile review: Universal Supply and target-coupled detection

## Verdict

**PASS for both repaired characteristic-zero theorem packages at their stated
scopes.**  Two independent hostile reviews found no remaining mathematical
countermodel on the frozen surfaces.  No exact Krenn--Gu graph witness or
counterexample was found, and the global conjecture remains **UNRESOLVED**.

The accepted advances are:

1. an exact surplus-graded expansion of the full uncontracted maximal-root
   tensor, with conditional same-graph complete-deck or fixed-pair supply at
   surplus two and a linear depth floor at every surplus at least four; and
2. an exact correction of the proposed extraction-or-detection arm: literal
   same-graph response defects and holonomy vanish, while a corrected mixed
   component reaches the target ideal only through a separately legal
   coefficient selector with every nuisance term retained.

These results do not force companion-sensor observability on the witness
locus, supply the higher-surplus low response grades, force a three-group
identifying overlap, diagonalize the corrected permanent channel, or produce
a universal nuisance-free target selector.

## 1. Reviewed surfaces and frozen hashes

The final SHA-256 values are:

```text
supply theorem: f2fdf783aac6fff2ff7010ba8bdc154c231b088cf0600ee608cac08a10cbd93e
supply primary: 8204773f1a5436cd8812710187f94a5edace61860ac9be78ab1772766ca38bf0
supply audit:   7d08b646935fb9ac1c8b6479a25ce4288f3d27adf2943bd63cb72324ea6745a9

detect theorem: 05e4901d5792d33513d156ac9f3349c00ee40f6d26e5f3f1a951a3b12c789324
detect primary: 50701df16b98cf325b52ab9831cd93386d8ebea842b1c9ea6ac17641581ee8fc
detect audit:   3e2b62595b263845d6d1d81b455cc25bc47eb56911d9ed41c2de99b0751f6d95
```

The owning documents are:

- [maximal-root surplus-two complete-deck sensor](../../claims/arbitrary-order/MAXIMAL_ROOT_SURPLUS_TWO_COMPLETE_DECK_SENSOR_AND_HIGHER_SURPLUS_DEPTH_BOUNDARY_THEOREM.md); and
- [same-graph response-defect and target-coupled selector boundary](../../claims/arbitrary-order/SAME_GRAPH_RESPONSE_DEFECT_VANISHING_AND_TARGET_COUPLED_SELECTOR_BOUNDARY_THEOREM.md).

## 2. Supply theorem audit

Restricting a perfect matching of `R union B` to the roots gives a bijection
between one root partial matching, one injection from the remaining roots to
outside vertices, and one perfect matching of the remaining outside set.  If
the root partial matching has `p` edges, its outside principal-hafnian label
has order `s+2p`.  This verifies the grade rule without importing the
contracted fixed-surplus theorem as a tower.

At surplus two, every nonempty even outside label occurs.  Over the outside
function field, full column rank of the companion map therefore identifies
the entire deck.  For a fixed residual pair, zero projection of the rational
kernel to the desired labels is necessary and sufficient only for **linear
sensor identifiability**.  It is sufficient for recovery of the actual
physical paired family because the graph supplies one physical solution.
Failure does not manufacture a second regular or physical hafnian deck.  The
final theorem states this quantifier wall explicitly.

The clean companion columns split into `2^r` no-`c` basis words and
`2^r-1` one-`c` sums with disjoint pivot supports.  Hostile review separately
checked the maximum-root repair: all clean contractions are realized first;
two interpolation perturbations per outside vertex supply the missing
coordinate rows; the full-rank minor has a nonzero constant term; and assigned
coordinate edges plus coordinate-monomial outside edges keep the displayed
root set maximum.  This proves a full-rank point in the maximum-root,
triple-blocker ambient stratum for `r>=3`.  It does not prove that this open
meets the target/witness variety.

The coordinate-absorption interface was checked against its owning theorem.
The old blocker shore `C` is disjoint from the original residual pair.  The
two promoted outside sets are exactly `C union {q_1}` and
`C union {q_0}`; after choosing a fixed residual pair inside `C`, their port
overlap is `C-Q_*`.  Thus no residual is duplicated or silently deleted.

At surplus at least four, all order-two labels are absent from every root
companion column.  This is a linear root-word depth boundary.  It does not
exclude nonlinear target coupling, cross-window recovery, or the independent
balanced full-sensor lane.

## 3. Detection theorem audit

For a physical `q=2` response, inserting one corrected residual edge into a
perfect matching gives `Z=M(h+Q_K)`.  Pointing one edge of every matching in
the residual-absent deck proves the displayed insertion defect is zero at
every even support.  This is a tensor-valued matching identity, not a scalar
genericity argument.

Literal restrictions of one graph also carry one global residual frame.
Every unique identifying overlap transition is therefore a chart-gauge
coboundary, and every cycle product telescopes to the identity.  A nontrivial
atlas holonomy can diagnose an abstract incompatible reconstruction, but it
cannot occur after honest same-graph supply and is not itself a GHZ
coefficient.

The rational block control

```text
h=1,   B=I+E_01,   K=-E_01,   hB+K=I
```

was replayed independently.  It proves that diagonal uncorrected top data do
not diagonalize the corrected channel and that the direct `hB` sector cannot
be discarded.

The surviving target bridge is the exact algebraic identity

```text
Omega T=Omega Lambda_Q-hY+hN.
```

The nuisance tensor `N` contains every other selected cofactor column.  A
nonzero corrected mixed component becomes a displayed mixed target
coefficient only for `h=0`, or after proving a nonzero coefficient-pure
selector and killing or independently subtracting its nuisance.  The
augmented-hafnian ledger confirms that odd root counts have zero pair-selector
weight and maximal-root base words have zero weight for every `r>2`.  The
four-root/P6 polarization is the first possible conditional selector, not a
universal one.

## 4. Independence and validation

The supply primary constructs companion columns with a direct matching
recurrence and computes exact SymPy ranks through `r=7`.  Its no-import audit
uses residual-to-root injections, a disjoint-pivot ledger, and separate grade
and capacity counts.  It does not claim a second symbolic kernel computation;
the quotient-rank criterion is the written linear-algebra proof.

The detector primary uses symbolic matching and matrix arithmetic.  Its
no-import audit uses `Fraction`, a bitmask matching recurrence, and separately
written matrix operations.  It checks the selector identity at exact
substitutions rather than claiming an independent symbolic derivation; the
displayed algebra is the proof.

The following focused surfaces were also replayed at the reviewed checkout:

- maximal torus-root saturation and coordinate absorption;
- lower mixed-root deletion-label tomography;
- residual-relative dual-Wick response;
- response-jet principal-deletion and root-parity legality;
- two-residual response-atlas descent; and
- top two-port synchronization/observability boundary.

All primary and no-import replays passed.  Both Ruff check and format check,
Python compilation, local-link/trailing-whitespace checks, and
`git diff --check` passed on the frozen candidate.

## 5. Accepted boundary

```text
uncontracted maximal-root companion grade rule:          PROVED;
surplus-two full-rank sensor -> complete physical deck:  PROVED CONDITIONAL;
fixed-Q kernel criterion for linear identifiability:     NECESSARY AND SUFFICIENT;
fixed-Q criterion -> physical paired response supply:    PROVED SUFFICIENT;
full-rank maximum-root/triple-blocker ambient point:      PROVED;
surplus >=4 order-two labels in linear root words:        ABSENT;

same-graph response insertion defect:                     IDENTICALLY ZERO;
same-graph identifying-atlas holonomy:                    IDENTICALLY TRIVIAL;
top diagonal -> corrected channel diagonal:               FALSE;
target-coupled selector identity with nuisance:            PROVED;
nuisance-free nonzero selector -> mixed detector:          PROVED CONDITIONAL;

observability forced on every surplus-two witness:         OPEN;
higher-surplus nonlinear/cross-window supply:              OPEN;
three-group identifying overlap forced:                    OPEN;
weighted-diagonal permanent attachment:                    OPEN;
universal coefficient-pure selector/nuisance control:      OPEN;
global Krenn--Gu conjecture:                               UNRESOLVED.
```
