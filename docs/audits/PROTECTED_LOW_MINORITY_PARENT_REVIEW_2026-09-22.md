# Protected low-minority parent review

Date: 2026-09-22.

Status: **PASS** for the mathematical scopes below. The reviewed
[owner](../../claims/arbitrary-order/PROTECTED_LOW_MINORITY_SUPPORT_COUNTERMODELS.md)
refutes every fixed paired-minority support exclusion, including P1 and
DAG-P1, and records a separate exact weighted-macro control. Global
Krenn--Gu remains **UNRESOLVED**.

Independent here means separately assigned research agents and independently
implemented checks within the same session. It does not mean independent
human refereeing, a Lean proof, or a global resolution audit.

## Explicit P1 construction and physical source proof

One agent independently enumerated the complete local common-star states:
three uniform, nine AB, three BB, and three four-cross states. For each
background color their q histogram is `{0:4,1:8,2:6}`. A separate adversarial
review rederived the global no-q<=1 argument and the macro construction.

The review explicitly checked the easily missed BB root whose protected
color differs from the background. Its crossed background-color leaf
would require a non-background exterior leaf, which is impossible. The
same argument excludes every four-cross root. The remaining AB case
requires two different labels on one component pair; the remaining BB
case requires a component triangle. Thus the exhaustion does not silently
omit a local matching state.

The 726-component permutation construction has a direct proof: any
nonconstant six-left assignment contains two occurrences of one color and
an occurrence of another; three specified distinct labels block all three
right colors. Constant-left assignments force every right component to
the same color.

The compact certificate uses twelve literal columns. A separate checker
reads the JSON alone, reconstructs its 72 component edges and 144 physical
crossings, checks every enabled gadget's physical matching, and exhausts
all 729 left assignments. All 726 nonconstant left assignments have a
fully blocked right column. The conditional independence of right
components makes this a proved exhaustive cover of the macro obligation.
It is not an enumeration of all physical words.

Both physical port orientations follow the exact protected partner map.
Every D_c respects `non-c leaves < centers < c-leaves`; the primary
reconstructs 96 arcs and longest path two for each color. The compact
fixture SHA-256 is
`9a7a6eb0ce56d2ad9c585c686e2ae7485b3619b588520c0f52ce70b59fa3c396`.

## Every fixed cutoff: physical and probability audits

The q-versus-girth proof was independently rederived. A q-zero active
component consumes a distinct non-background leaf in a positive-q
component; there are at most 2q such leaf incidences and at most q positive
active components. The 3q bound therefore counts actual physical resources.
In a forest projection all used edges are doubled by even-degree parity.
Two doubled edges cannot meet because each uses the unique center. Hence
the projection is a matching of gadgets and the word is component-constant.
Four-cross states and repeated component projections are covered.

Two additional audits checked the random-layer existence proof. The four
families used in the fixed-coloring avoidance bound are independent. The
zero-side cases, majority count over both bipartite sides, entropy estimate,
strict margin delta>2, and complete summation over distance types all hold.

The short-cycle audit checked canonical cycles modulo rotation/reversal,
with matching-layer identities retained. In particular, its 1,128 limiting
parallel two-cycle events comprise 168 same-color-label layer pairs and
960 different-label layer pairs. Omitting the first class would not prove
simplicity. The exact mean for length 2l is `(47^(2l)+47)/(2l)`.

For any compatible cycle union, its probability is exactly the product of
inverse falling factorials for the individual matching layers. Incompatible
reuse has probability zero. Two distinct canonical cycles in an overlapping
connected union have cyclomatic rank at least two, so e>v and their
contribution vanishes. Vertex-disjoint patterns give the stated factorial
moments, and the odd/even Bonferroni bounds have the correct orientations.
Their two successive limits give positive no-short-cycle probability.

The rigidity failure probability tends to zero, so its intersection with
the positive-probability girth event is nonempty at some finite order.
Excluding two-cycles makes the graph simple and one-label. Choosing girth
greater than 3r completes the P_r construction. This is a written
probabilistic proof of existence, not empirical evidence from random draws.
The support can depend on r; no growing-r, size bound, or unrestricted UPM
claim follows.

## Weighted boundary and exact rational contrast

The existing PSCS identity `A_uv B_uv=F_uv-F_u F_v` applies to every edge of
the triangle-free support. It rules out all nonzero weights satisfying
the macro targets. The support countermodels therefore cannot be promoted
to weighted sources. On general one-label common-star arrays, the q1
classification and the existing exhaustive U4 classification also give
WP1 exactly the same antecedent as CSQ4; the new terminology does not
create a new common-star proof obligation.

A complementary 72-entry rational fixture was checked by two genuinely
different finite engines. The primary recursively enumerates word-compatible
edges; the independent checker first enumerates all 10,395 unlabelled
twelve-vertex perfect matchings, then evaluates each word with fractions.
All 27 macro coefficients agree with their targets, and the reconstructed
D_c arc counts are 33,38,37, with independent topological/DFS checks.

The exact non-macro word `1210|1111|1221` has q_1=1 and one matching of
weight -1, so the fixture fails WP1 and P1. Its SHA-256 is
`bbc7ef7abd60c97169d274ccc1bf66537ff94ad6fff5f1382c35fa39c26abb1a`.
The four promoted construction/weighted scripts also passed by absolute
path from a neutral working directory. Fixture bytes were preserved, and
promotion changed only paths/bootstrap, explanatory wording, and removal
of an automatic scratch receipt write.

## Limits

The all-order statements rest on the written proofs, not finite replay.
No external random-graph theorem, solver certificate, numerical optimum,
or unchecked SAT outcome is a premise. An exploratory k=3 native UNSAT
result has no checked proof certificate and is not used here. The accepted
18-component example is consistent with any valid exclusion at k=3.
The full weighted WP1/SFULL and unrestricted all-word UPM obligations
remain open, as does arbitrary-witness supply of the protected scaffold.
