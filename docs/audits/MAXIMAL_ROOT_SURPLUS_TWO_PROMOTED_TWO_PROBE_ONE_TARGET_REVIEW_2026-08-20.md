# Maximal-root surplus-two promoted two-probe one-target hostile review — 2026-08-20

## Verdict

**Accepted at the frozen hashes below. No P0, P1, or P2 defect remains.**

The accepted package is an exact characteristic-zero arbitrary-root
**promoted-module reduction** and an exact pointwise criterion for the isolated
one-row failure locus. It is not a theorem that a useful row occurs on every
witness, and it is not entry into any named committed downstream detector.
The complete supply-and-target-attachment strategic node remains **OPEN**.
The global Krenn--Gu conjecture remains **UNRESOLVED**.

The hostile review required the following repairs before acceptance.

1. The promoted desired tensor and the unique top companion were first compared
   in different coefficient spaces. The theorem now defines the common left
   coefficient space

   ```text
   L^*_(S_C)=(tensor_(a in A)V_a^*) tensor V_C^*
   ```

   and the exact top coefficient-slice space

   ```text
   N^top_(S_C)=G^A_empty tensor V_C^* subset L^*_(S_C).
   ```

   The proved statement is the typed quotient survival
   `[g_(S_C)] != 0` in `L^*_(S_C)/N^top_(S_C)`. The functional
   `epsilon_A tensor id_(V_C^*)` kills the entire top slice space and not the
   desired tensor.
2. The geometric radical notation now extends every displayed ideal to
   `bar Lambda=bar K tensor_K Lambda` before taking its radical. Every variety
   and radical containment is therefore explicitly geometric over the
   algebraic closure rather than an ambiguous containment between rings.
3. Source-level failure now has the required biconditional quantifier: it occurs
   exactly when every eligible `(Q,A)` and every target in that promoted family
   satisfies the pointwise failure containments. Failure for one arbitrarily
   chosen pair is not promoted to a source theorem.
4. All language that could lower the owner's strategic-node interface to one
   legal row was removed or qualified. The theorem, README, live frontier, and
   proof-DAG call this an isolated one-row reduction. They retain as open every
   common-package, synchronization, augmented-weight/alignment, detector
   activity, nuisance, and additional anchor gate required by a named
   downstream theorem.
5. The four-root `R/E/A` split remains the exact split for the stronger
   all-seven GLD3 package. A single response-zero or absorbed target is not a
   bad isolated-one-row leaf when another target is useful, but this refinement
   does not replace the seven-row package.
6. Verification provenance now accurately says that the independent audit uses
   standard-library `Fraction` arithmetic and exact characteristic-zero
   reduced-point/rank tables. It does not claim a finite-field lift that the
   script does not perform.

## Frozen artifacts

Base HEAD before the candidate package:

```text
2bcd2172240ec2e23692b45a6fdee0d172086db1
```

```text
promoted theorem   52592BA0BAEF28F301B90D08826717D288919C8E8ED08596348D00A9B3691729
primary verifier   ABD51FB2D5DDD9E94CA68CD196AC9A786280B909ED59802963D78A62CBCBED25
no-import audit    409B943D814514DBC10AFA07042DAFB144E0BA589CB9007BDEE7CFD741AA8D91
package README     D73E0894D90D61885FC1F0B8ACD74D9FDAD732275FF52FAA8545505C91EC5E9B
current frontier   C0924E0679C81966085B7548DA954523B1ECFE67C210E13E1AF858C09FED5572
internal DAG       BA5439C4EC98B52454198C8C7CAB7F73A98AC3ABCEB1F753C3172F6AFC4AB179
```

## Source hypotheses and two-grade companion census

The theorem begins with an actual hypothetical complex witness in the
maximum-root surplus-two scope, with root order `r>=3`, and imports exactly the
GLS4 conclusion: one residual pair `Q`, one probe-root pair `A`, nonzero
`H_Q`, nonzero complementary permanent `Pi_Q`, original individual order-two
survival modulo every higher companion, and nonzero raw polynomial
`p_(A,Q)`. Reclassifying the other old roots as open ports changes neither the
physical graph nor the complete tensor equality.

There are two probe roots and `2r` outside vertices in the promoted partition.
A root partial matching therefore either uses no probe--probe edge and sends
both probes outside, or uses the unique probe--probe edge. Consequently the
only active deck orders are `2r-2` and `2r`. The displayed dimensions follow
from direct even-subset sums. After residual evaluation, sorting the missing
pair by its intersection with `Q` gives

```text
e_m=binom(m,2)3^(m-2)+2m3^(m-1)+2*3^m,
m=2r-2.
```

The primary and independent audit derive this census by different routes. The
former enumerates labels; the latter classifies complete-graph perfect
matchings by bitmask and recomputes all subset sums.

## Laplace supply and the exact top-slice boundary

Expanding `Pi_Q` by the two ports assigned to `A` gives the exact permanent
Laplace identity

```text
Pi_Q=sum_(C in binom(U,2))
  epsilon_A(G^A_C) tensor Per_(K_0,U-C).
```

Since `Pi_Q!=0`, at least one `epsilon_A(G^A_C)` is nonzero. The root condition
kills `epsilon_A(G^A_empty)`. In the correctly padded target coefficient
space, every slice of the unique top column lies in
`G^A_empty tensor V_C^*`; hence the selected `g_(S_C)=G^A_C` survives that
whole slice space. This proof uses no denominator.

This is deliberately weaker than full nuisance survival. It proves neither
that `H_(Q union S_C)` is nonzero nor that `g_(S_C)` survives the other
top-minus-two labels. Those are precisely the response and complete-nuisance
gates treated by the later quotient criterion.

## Formal selector and physical target quotient

For every promoted target, `mathsf P_S` is the nonzero **formal** labelled-deck
projection. The realized physical tensor is separately denoted
`P_S(H;z_Q)=mathsf P_S(H)`. This distinction remains valid when the physical
response is zero.

Writing

```text
Gammahat_Q=g_S tensor mathsf P_S+Theta_S
```

and taking every coefficient slice of `Theta_S` gives the complete nuisance
space `N_S`. Direct-summand independence and finite-dimensional separation
then prove

```text
(lambda_S tensor id)Gammahat_Q=mathsf P_S
iff lambda_S in N_S^perp and lambda_S(g_S)=1
iff [g_S]!=0 in L_S^*/N_S.
```

No same-grade, top-grade, residual-absent, or unwanted coordinate slice is
discarded.

On the complete GHZ equation, quotienting by `N_S` gives

```text
sum_c alpha_c[d_(S,c)] tensor w_(S,c)
 =[g_S] tensor P_S(H;z_Q).
```

The residual factors `alpha_c` are Laurent units and the three output words
`w_(S,c)` are independent. Thus the pure quotient has rank at most one, and
rank one is equivalent to simultaneous desired survival and nonzero physical
response. This supplies the normalized constant selector, full nuisance
annihilation, and the nonzero target-pure class for that isolated row. It does
not supply the additional gates of a downstream multirow detector.

## Pointwise radical--Fitting criterion

All pointwise statements are made after extending to
`bar Lambda=bar K[z_(q,c)^(+/-1)]`. For a fixed target, the useful locus is

```text
D(hp) intersect {rank[B_S|D_S] > rank B_S}.
```

At a fibre of nuisance rank `j-1`, adjoining the pure columns raises rank
exactly when every `j`-minor of `B_S` vanishes and some `j`-minor of the
augmentation does not. Therefore emptiness is equivalent, for every `j`, to

```text
(hp) I_j([B_S|D_S]) subset sqrt_geom(I_j(B_S)).
```

The containment direction is correct: it is the Nullstellensatz translation of

```text
V(I_j(B_S))
 subset V(hp) union V(I_j([B_S|D_S])).
```

This formulation includes all nuisance-rank drops, response-zero fibres, and
exceptional selector escapes without division. The equivalent ambient formula
uses the response **ideal**, not the product of all response coordinates.

For a fixed eligible `(Q,A)`, no target is useful anywhere exactly when every
target satisfies these containments. Source-level failure is their conjunction
over every eligible `(Q,A)`. In the standard four-root module, the raw gate is
correctly encoded by the ideal generated by all `p_(A,Q)`, and pointwise
failure is further conjoined over every eligible `Q`.

## Generic split and exceptional support

The function-field split is exhaustive only at the generic point:

- one target has nonzero response and a generic desired rank rise; or
- every target has zero response or generic desired absorption.

The theorem does not promote the second branch to pointwise absorption. Its
annihilator ideal records simultaneous denominator-cleared identities, while
the support of the torsion quotient remains governed by the full geometric
Fitting containments. The exact one-parameter controls separately exhibit
generic escape with exceptional swallowing, generic absorption with
exceptional escape, permanent absorption, and a legal zero-response row.

## Root-order and downstream scope

At `r=3`, promotion gives four open ports and exactly the six pair targets plus
the four-port target shapes occurring in GLD3. One useful row does not supply
the common seven-row package required to enter GLD3.

At `r=4`, the already committed standard four-root/four-port module retains its
all-seven `R/E/A` cover. The promoted construction instead has six ports and
only the fifteen four-port targets plus the six-port target; GLD6 additionally
requires all fifteen pair targets. For `r>=5`, no committed detector accepts
only the promoted top-minus-two and top layers.

Accordingly, this theorem is not a closed source-to-detector edge at any root
order. It cannot be used to omit common-package, synchronization,
augmented-weight/alignment, activity, nuisance, or anchor hypotheses required
by the chosen downstream theorem. The proof-DAG and live frontier preserve
those obligations explicitly.

## Physical sharpness control

The sparse same-graph construction has exact nonzero `H_Q`, `Pi_Q`, and
`p_(A,Q)`, and a unique nonzero Laplace-selected promoted coefficient. Its
corresponding physical response has exactly two perfect matchings with opposite
weights, so the whole response tensor is zero. This proves that the displayed
supply data alone do not clear the response gate.

The construction is explicitly not a hypothetical witness: its all-one and
all-two pure GHZ coefficients vanish. It is also not asserted to satisfy the
maximum-root quota package. It therefore supplies a sharp graph-side boundary,
not a physical or global countermodel.

## Independent exact checks and bounded gaps

The focused commands pass at the frozen hashes:

```powershell
uv run --with sympy python claims/arbitrary-order/verify_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
python -I claims/arbitrary-order/audit_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
python -m py_compile claims/arbitrary-order/verify_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py claims/arbitrary-order/audit_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
uv run --with ruff ruff check claims/arbitrary-order/verify_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py claims/arbitrary-order/audit_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
uv run --with ruff ruff format --check claims/arbitrary-order/verify_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py claims/arbitrary-order/audit_maximal_root_surplus_two_promoted_two_probe_one_target_attachment_and_pointwise_failure_reduction.py
```

The primary reports PASS for root orders three through six, seven univariate
Laurent-radical controls, `4096` finite scalar controls, `512` point-rank
controls, all four boundary modules, and the response-zero graph control at
each tested order.

The no-import audit is genuinely independent at the implementation level. It
uses only the Python standard library and does not read or import the primary.
It reports PASS for `146580` classified perfect matchings, `870` symbolic
Laplace monomials, five independently summed root orders, `21297` selector
matrices, `1215` pure quotient tensors, `133380` pointwise rank matrices,
`180` reduced two-point profiles, eleven exceptional states, and eight
cancelling response matchings.

The bounded gaps are explicit:

- the scripts replay finite orders and finite exact controls; the arbitrary-
  root theorem is the written combinatorial proof;
- the multivariate geometric radical criterion is the written
  Nullstellensatz argument; the scripts check its orientation on exact
  univariate and reduced-point models;
- neither script proves that the physical witness locus avoids the simultaneous
  all-target containments;
- the response-zero control is not a witness or a quota-saturated source point;
- the package supplies no common detector package and audits no downstream
  detector activity or extraction consequence.

## Remaining obligation

For this isolated reduction, the smallest physical obligation is to prove that
some containment fails for some eligible `(Q,A)` at every actual source point,
or to derive a complete mixed GHZ contradiction from simultaneous validity of
all containments. The strategic node additionally requires a proved edge from
the surviving rows to every hypothesis of one named committed downstream
detector. Neither obligation is discharged here.

There is no claimed permanent restriction, extraction/gluing completion,
global proof, or exact counterexample. The supply-and-target-attachment
strategic node remains **OPEN**, and the global Krenn--Gu conjecture remains
**UNRESOLVED**.
