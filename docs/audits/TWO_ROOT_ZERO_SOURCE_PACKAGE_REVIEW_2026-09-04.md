# Final packaged zero-Q checker review

Final [package](../../claims/finite/n08/two-root-zero-source-certificate/README.md)
review: **PASS**. The checker, manifest, README scope, and corrected replay
command pass. This reviewer did not repeat the full traversal in the
packaging audit; the coordinator subsequently ran the documented packaged
replay and received encoding PASS and certificate PASS, with all 6,860
branches and 6,861 leaves checked and process exit zero.

The reviewed integration package is claims/finite/n08/two-root-zero-source-certificate/ in the astra-r2-lowrank-20260904 worktree. No tracked files were edited by this reviewer.

## Exact identity and guard checks

- Accepted canonical checker SHA256: 3fad19ae0bba42e40d9e77c30377da3263943dbdb14f8857f021b6ee0c10d6af.
- Accepted portable checker SHA256: 87e48e8f613e736dd0dde5b19020a4c19e552c38c9e19245e3d72b8a90dcfd7b.
- Packaged formatted check_certificate.py SHA256: ab565a6ee2a207687b56611b90871412115b9f052e31f0c016f99b25b85fc825.
- Packaged formatted generate_instance.py SHA256: 5ad567f96cc99fe01bdc0578934b2bb62ad3aa74446944180e3e34479eb5c30d.

AST equality, excluding location attributes, passed for all five non-main functions against both canonical and portable checkers: read_cnf, reconstruct, conflicts_by_up, verify_tree, selftest. The entire formatted package checker also has exactly the same AST as the portable checker, including main and its module guard. Therefore formatting changes no Python semantics.

The optimization guard is an actual `if not __debug__: raise RuntimeError(...)` statement, not an assert that would disappear. Executing the packaged checker under python -O produced the intended RuntimeError before loading artifacts. The module's raw-CNF pin is 4415ea3d243603910729098d104240ca2d6fd2fa1d2843098e3131b4088ac1ac, and raw-certificate pin is d73b746cbf5bafdcb1ac6e2af9bcac65475e5d7d1595f82cabca25bc8556c1fd. The certificate hash is checked before JSON traversal; its assertion cannot be disabled because of the optimization guard.

The packaged --self-test-only run passed all nine controls. The packaged --encoding-only run passed on the already regenerated byte-identical CNF, checking all 11394 clauses.

## Manifest and scope

The instance's 2394 variables and 11394 clauses agree with the accepted frozen CNF. README clause-family counts 222,6561,2181,1458,972 sum correctly and match independent reconstruction. Both raw hashes in manifest agree with their pins and previously checked artifacts. A separate structural traversal of the packaged certificate, without unit propagation or proof replay, confirms 6860 binary branches,6861 leaves,maximum decision depth48.

README and manifest correctly restrict the certificate to the pure (3,3), n=8, maximum-two-root, common-coordinate zero-root-block source. They distinguish necessary support constraints from complex-coefficient feasibility, identify the accepted rank-at-least-two theorem, leave the broader branch cover to the owning mathematical completion, do not claim Lean formalization, and preserve global UNRESOLVED. Native crashed/empty-trace UNSAT supplies no proof. The package-local certificate.json -text attribute was already confirmed by git check-attr as text unset, protecting exact certificate bytes.

## Replay command correction

The initial README command generates tmp/two-root-zero-source.cnf then invokes the checker with only --cnf. The checker's specified default searches for certificate.json adjacent to the CNF, so this would incorrectly read tmp/certificate.json. The integration owner was notified to add:

    --certificate claims/finite/n08/two-root-zero-source-certificate/certificate.json

to the documented and final bounded replay commands. This is a CLI documentation fix, not a proof-checking change. Full package replay remains owned by the coordinator.

The integration owner corrected README line65 to include the explicit package certificate path. This reviewer re-read the corrected command and confirmed the path now resolves to the checked durable certificate. The replay-command HOLD is therefore cleared.
