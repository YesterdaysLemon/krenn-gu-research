# Review: survivor invariant-quadratic principal-open nonextension

Date: 2026-08-26

## Verdict

Accept `GLD82` as an **exact explicit polynomial-circuit principal-open
exclusion** on the declared scale-fixed equal-leaf survivor chart over
`K=Q(i)`, hence over `C`.

On

```text
D(Delta_82),
Delta_82=Omega gamma_num det(M_ff),
```

every concise GHZ tensor in the fixed rank-`44` nuisance image and every one
of its raw coefficient preimages fails the complete legal first-response
condition.  The `GLD81` bridge transfers this exclusion to the named
root-order-four, surplus-two, fully-supported, rank-three, nonisotropic
maximal-star source branch.

This is not coverage of `V(Delta_82)`, other survivor components or gauges,
rank/support boundaries, triangles, other root profiles, or the global
conjecture.  The global Krenn--Gu status remains **UNRESOLVED**.

## Exact construction audited

The accepted route has four load-bearing parts.

1. The fixed `GLD70` rank-`44` nuisance pivot has nonzero determinant
   `theta_0=510015580149921683079168`.  It gives a polynomial raw section on
   the survivor scheme.  The leaf-`S_3` invariant part of `ker b` has the
   fixed Gaussian-calibrated basis `V_0` of rank eight; its displayed basis
   minor is `-6912+6912i`.
2. A universal polynomial-port calculation proves, for all six leaf
   permutations and for each root map separately,

   ```text
   b rho_raw=rho_ten b,
   H_r rho_raw=rho_ten H_r.
   ```

   The constant `Q`/eta-residual block is stable.  Reynolds averaging in the
   intrinsic quotient therefore sends every full affine necessary rank-one
   solution to an invariant one, including zero-column and response-rank-drop
   cases.  The fixed thirteen-row quotient chart is not assumed equivariant.
3. With `F=(A,G,G,G)`, adjugate transport

   ```text
   U_num=adj(A) tensor adj(G) tensor adj(G) tensor adj(G)
   ```

   satisfies `U_num R=d Diag`, up to the allowed common invertible monomial
   change of response columns, where `d=det(A)det(G)^3`.  A named thirteen-row
   pivot of the transported constant block has determinant `gamma_num`.
   The adjugate quotient formula `Q_num` is polynomial and agrees with
   `gamma_num` times the ordinary quotient on `D(gamma_num)`.
4. The three homogeneous invariant response columns in `P^8` give forty-five
   named intrinsic `2 x 2` minors.  Their coefficients in the forty-five
   quadratic monomials form the ambient polynomial matrix `M_ff`.  On
   `D(det(M_ff))` these quadrics span every quadratic, so their projective
   common zero set is empty.  The entries and determinant are restricted to
   the declared survivor scheme only at the theorem statement.

No response minor, nonzero response column, or moving nuisance/kernel pivot
is selected.  The scale equation `x_8=0` is enforced by the portable builder.

## Gaussian specialization and certificate

At the exact `GLD72` frame the builder obtains

```text
d=24-24i,
gamma_num=-692533995824480256(1+i).
```

After dividing by the proved common scalar, it matches all `2,025` entries of
the independently reconstructed `GLD74` normalized `45 x 45` coefficient
matrix.  If that matrix is `M_0`, then

```text
M_ff(F_0)=(d gamma_num)^2 M_0,
det M_ff(F_0)=(d gamma_num)^90 det M_0 !=0.
```

The exact normalized determinant is

```text
-378089878893442723106646837537745718758189247729870198909680050358976512
 /205891132094649
+25931419533924809154531852205198475327334321064667678574124775287933632512
 /1853020188851841 i.
```

The certificate SHA-256 is
`4cdaf08a5f5dc40abc845d4dc1e6046ce3b259b2c751dfd3ec2955e5b94e65e0`, and
the independently rebuilt `GLD74` quotient fingerprint is
`17c10d8e04a4e29b073914919beb0a99ff77735be12cc16f095e07ef7549452e`.

## Hostile-review history and repairs

The first hostile pass rejected an earlier draft because it certified only
the Gaussian coefficient matrix and had not yet supplied a moving polynomial
construction.  That would have been another pointwise `GLD74` corollary, not
the requested parent-level survivor-open advance.

The accepted revision adds the moving fraction-free builder, the universal
individual-root covariance audit, the fixed invariant-kernel calibration,
the adjugate response transport, and the named quotient pivot.  A basis
normalization mismatch between the builder's Reynolds average and the stored
unnormalized Gaussian basis was found and repaired; the final specialization
now agrees entry for entry.  Repeated quotient determinants were also
factored out without changing the arithmetic circuit.

Three independent hostile reviewers then accepted the repaired package with
no priority-zero or priority-one mathematical defect.  Their remaining
editorial requests were incorporated: the evaluator is total on
`gamma_num=0` in denominator-free mode, `x_8=0` is guarded, quotient averaging
is separated from the coordinate chart, `U_num R=d Diag` is stated, and the
survivor-neighborhood and ambient-polynomial scopes are explicit.

## Verification evidence and independence

The moving builder constructs the fixed raw section, invariant kernel,
fraction-free quotient, response quadrics, and `45 x 45` specialization.  It
checks entrywise equality with the stored normalized matrix.

The primary verifier independently rebuilds the physical `GLD74` quotient,
the invariant response block, the forty-five quadrics, their exact
determinant, the certificate bytes, and the quotient fingerprint.

The no-import covariance audit works over a universal polynomial port ring
and checks all six leaf permutations and four root maps.  It does not import
the builder.  The separate no-import determinant audit parses `Q(i)` and
recomputes the stored `45 x 45` determinant by fraction Gaussian elimination.
It independently checks the certificate and scope manifest, but does not
rederive the physical quotient.

Thus the evidence is deliberately split among moving-circuit construction,
physical specialization, universal covariance, and independent determinant
replay.  The written algebraic argument carries the geometric-point
principal-open theorem.

## Residual cover and next obligation

The exact residual cover exposed by this package is:

1. `det(M_ff)=0` inside the frame and quotient open;
2. `gamma_num=0`;
3. `Omega=0`, including frame or gauge degeneration;
4. other survivor components and gauges; and
5. the remaining source-tree branches: rank/support boundaries, triangles,
   residual-coordinate and isotropic cases, smaller survivor families,
   other root/surplus profiles, and non-star atlases.

The highest-value immediate successor is no longer another survivor point.
It is exact component/divisor analysis of the three named factors, beginning
with `det(M_ff)=0` in the survivor coordinate ring, while preserving every
quotient, gauge, and rank-drop branch separately.
