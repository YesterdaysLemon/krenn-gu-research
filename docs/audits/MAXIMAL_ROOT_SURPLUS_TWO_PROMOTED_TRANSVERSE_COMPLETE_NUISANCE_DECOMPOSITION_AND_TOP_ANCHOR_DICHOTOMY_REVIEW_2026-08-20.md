# Maximum-root surplus-two promoted transverse complete-nuisance hostile review -- 2026-08-20

## Verdict

**PASS for the exact label-by-label decomposition and top-anchor dichotomy.
Strategic node closure remains OPEN.**

The theorem correctly expands the abstract projected nuisance of `GLS22` into
the coefficient-slice space of every retained active label.  For a complement
pair `D`, its projected coefficient slots on the target side are sliced, its
coefficient slots on the left are retained, and the missing left port slots
come from the full identity action of the formal deck input.  The resulting
typed subspaces sum exactly to the complete nuisance because the formal domain
is a labelled direct sum.

The disjoint-label fullness criterion is sufficient, not necessary.  The
top-anchor dichotomy is exhaustive, but neither branch is excluded.  The
global conjecture remains **UNRESOLVED**.

## Scope audited

The review checked

- [`GLS23` owning theorem](../../claims/arbitrary-order/MAXIMAL_ROOT_SURPLUS_TWO_PROMOTED_TRANSVERSE_COMPLETE_NUISANCE_DECOMPOSITION_AND_TOP_ANCHOR_DICHOTOMY_THEOREM.md);
- [`GLS8` complete promoted nuisance](../../claims/arbitrary-order/MAXIMAL_ROOT_SURPLUS_TWO_PROMOTED_TWO_PROBE_ONE_TARGET_ATTACHMENT_AND_POINTWISE_FAILURE_REDUCTION_THEOREM.md);
- [`GLS22` transverse quotient](../../claims/arbitrary-order/MAXIMAL_ROOT_SURPLUS_TWO_PROMOTED_ALL_PORT_TRANSVERSE_QUOTIENT_AND_PROJECTIVE_SYNCHRONIZATION_FAILURE_THEOREM.md);
- the focused primary verifier; and
- the independent standard-library no-import audit.

No external literature claim is used.

## Load-bearing formula

For an active complement pair `D`, let `D_0=D intersect Uhat` and let `a_D` be
the transversely projected companion after evaluating those residual `Q` slots
which occur in `D`.  For target complement `C`, split

```text
X=C intersect D_0,
Y=D_0-C,
Z=C-D_0.
```

The labelled operator is `a_D tensor id_(Uhat-D_0)`.  Slicing its target-side
output contracts the fixed `Y` factors of `a_D`.  The input identity on `Z`
supplies the entire `V_Z^*`.  Hence its exact left coefficient space is

```text
Slice_Y(a_D) tensor V_Z^*.
```

The `Q-D` residual evaluation is surjective because every residual vector is
nonzero.  No image direction is lost there.  Summing labelled spaces is exact
because inputs from distinct deck labels are independent formal summands.

The projected top-grade term is `p omega tensor id_(Uhat)`, with
`omega=W_(a_0,a_1)` and `epsilon_A(omega)=0`.  It is nuisance for every pair
target and desired for the top target.  This produces the common anchor line
and the zero/nonzero dichotomy.

## Hostile assertions

1. **The formula infers nuisance ownership from filenames.**  False.  It is
   derived from the labelled direct-summand operator and target factorization.

2. **Residual evaluation may shrink the formal port input unpredictably.**
   False.  Evaluation by each nonzero residual vector is a nonzero functional
   onto the ground field; its tensor product is surjective onto the remaining
   formal port tensor space.

3. **Only a contained nuisance subspace is proved.**  False.  Every active
   unwanted label is enumerated and the top label is included exactly when it
   is unwanted.  Structurally zero lower labels add zero.

4. **The `D=Q` all-port label was omitted.**  False.  It is included and its
   projected tensor is zero because `P_Q(q)=0`, matching GLS21--GLS22.

5. **The desired pair label is accidentally counted as nuisance.**  False.
   `D=C` is explicitly excluded for a top-minus-two target.

6. **The top-grade term is always nuisance.**  False.  It is nuisance for pair
   targets and the desired coefficient for `C=empty`; the two formulas differ
   accordingly.

7. **An overlap-one label supplies the full missing port pair.**  False.  The
   formula retains its common fixed coefficient factor, slices the outside
   factor, and tensors only the one genuinely missing left port.

8. **A disjoint fixed coefficient is left unsliced.**  False.  All its port
   factors lie on the right and are contracted; only its root slice remains,
   tensored with the full `V_C^*` from the input identity.

9. **Disjoint root-slice fullness is necessary for absorption.**  False and
   explicitly disclaimed.  Overlap spaces and the top anchor can complete a
   proper disjoint span.

10. **Disjoint root-slice fullness does not imply full target nuisance.**
    False.  Tensoring the full eight-dimensional root space with the entire
    nine-dimensional `V_C^*` is the full `72`-dimensional ambient space.

11. **`omega` may fail to be transverse.**  False.  The maximum-root equation
    is exactly `epsilon_A(omega)=0`.

12. **`omega=0` merely makes the top response zero.**  False.  It makes the top
    desired coefficient itself zero, so no legal normalized selector exists
    regardless of response.

13. **`omega!=0` proves top-target survival.**  False.  It must still escape
    the explicit root-slice nuisance span.

14. **The `63`-row quotient deletes a desired pair coefficient.**  False.  The
    anchor line is already nuisance for every pair target, so quotienting it
    is selector-equivalent.

15. **The top target also has `63` rows.**  False.  Its transverse space has
    eight root rows and its desired tensor is `p omega`.

16. **At `r=3`, the dichotomy supplies seven useful rows.**  False.  It only
    gives exact individual modules and an exhaustive anchor split; pair/top
    survival, responses, common normalization, and activity remain open.

17. **Generic full slice rank closes exceptional fibres.**  False.  The theorem
    explicitly keeps every proper root-slice and overlap-rank branch.

18. **The theorem proves a witness contradiction or strategic closure.**
    False.  No bad branch is excluded by a complete mixed GHZ coefficient.

## Verification independence

The primary verifier builds exact labelled identity operators on four ports,
extracts their target coefficient slices, and compares all 77 target/label
spaces with the formula.  It separately checks top identity slices, disjoint
fullness, and anchor quotient ranks.  The audit imports no SymPy or repository
code; it independently enumerates 28 smaller target/label operators using
`Fraction` arithmetic and a hand-written rank routine.

Both paths pass.  Their finite checks audit the tensor bookkeeping; the
arbitrary-root result follows from the written labelled-operator proof.

## Open boundary

The promoted failure locus is now described by physical projected companion
slice spaces rather than an opaque nuisance matrix.  The next proof must use
complete mixed GHZ equations to exclude simultaneous fullness/overlap spans
and top-anchor absorption, or prove a useful shared family with all response
and activity gates.  A generic slice-rank computation would not cover the
required exceptional fibres.

Strategic node: **OPEN**.  Global conjecture: **UNRESOLVED**.
