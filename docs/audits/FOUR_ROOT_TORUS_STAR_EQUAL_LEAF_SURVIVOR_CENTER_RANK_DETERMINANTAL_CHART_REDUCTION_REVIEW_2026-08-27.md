# Review: equal-leaf survivor center-rank determinantal chart reduction

Date: 2026-08-27

## Verdict

Accept `GLD84` as an **exact finite determinantal parameter reduction** of the
scale-fixed equal-leaf survivor base over `Q(i)`, hence over `C`.

After `x_8=0`, the ten pinned survivor generators have the exact form

```text
g=A(z)c+q(z)
```

with eight center variables `c`, six leaf variables `z`, and a `10 x 8`
coefficient matrix `A(z)`.  Its determinantal ranks give an exhaustive cover
by `45` rank-eight Schur charts, `960` exact rank-seven charts, and the closed
rank-at-most-six branch `V(I_7(A))`.

This is not a response exclusion or a full survivor-component theorem.  It
does not compute the pullback of the `GLD83` ideal `I_Pl`, prove the
rank-at-most-six branch empty, cover other gauges/components or source
presentations, or resolve Krenn--Gu.  Global status remains **UNRESOLVED**.

## Exact algebra audited

The sparse `GLD75` certificate has SHA-256
`05a2540431023b7add3d3ae60189cac31ebd375609911cfdc66b8c4028346b57`.
After deleting monomials containing `x_8`, an exact exponent scan shows that
every surviving monomial has total degree at most one in
`x_0,...,x_7`.  Thus the displayed center-linear decomposition is a
polynomial identity, not an interpolation.

For an invertible eight-row block `A_R`, exact Schur elimination gives

```text
c=-adj(A_R)q_R/det(A_R)
```

and two denominator-free residual equations.  This identifies the localized
survivor chart with a quotient of the six-variable leaf ring.

For a seven-row/seven-column pivot `A_(R,C)`, leave the omitted center
coordinate `t` free and solve the other seven.  The three remaining equations
have the form

```text
p_k(z)t+q_k(z)=0.
```

The coefficients `p_k` are the Schur forms of the relevant `8 x 8` minors.
Therefore a genuine rank-seven chart must impose all of `I_8(A)=0`, or the
equivalent three `p_k=0` equations after localization.  Merely selecting a
nonzero seven-minor gives a rank-at-least-seven chart and still contains
rank-eight points.  The final theorem states the full rank condition and does
not make this mistake.

The exact locally closed partition is

```text
B^[8]   = B intersect D(I_8(A)),
B^[7]   = B intersect V(I_8(A)) intersect D(I_7(A)),
B^[<=6] = B intersect V(I_7(A)).
```

It is exhaustive because `A` has eight columns.  The chart counts are
`binomial(10,8)=45` and `binomial(10,7)binomial(8,7)=960`.  No claim is made
that every named chart or the low-rank branch is nonempty.

## Gaussian geometry and corrected derivative

At the shifted `GLD72` origin,

```text
rank A=7,
det A[rows 0..6, columns 0,1,2,3,4,5,7]=12.
```

The center kernel is the line generated by

```text
(1,-1,0,0,0,0,1,0).
```

For the eight-row minor

```text
mu_0=det A[rows 0..7, columns 0..7],
```

the correct exact statements are

```text
mu_0(F_0)=0,
partial_(x_14) mu_0(F_0)=0,
d mu_0(F_0)(tau_14)=48i.
```

An early shorthand called `48i` the `x_14` derivative without distinguishing
the intrinsic survivor coordinate from the ambient partial derivative.  The
primary verifier rejected that wording: along the normalized survivor
tangent `tau_14`, the dependent leaf coordinates satisfy

```text
dx_9=-1/2+i/2,
dx_10=-2+2i,
dx_11=0.
```

The ambient gradient of `mu_0` has `x_9` entry `48-48i`, giving the exact
directional value `48i`.  The theorem now makes this distinction explicit.

Because the scale-fixed `GLD75` germ is smooth of dimension four and this
cotangent class is nonzero, `mu_0` is not the zero function in its local
ring.  Hence rank-eight points occur on the same local component as `GLD72`.
This does not identify the full rank-seven locus with the single divisor
`mu_0=0`; all forty-five eight-minors must vanish.

## Hostile-review findings

Three independent bounded reviews attacked the rank cover, Schur formulas,
Gaussian normalization, and relevance to `GLD83`.

- The geometric review reconstructed all ten sparse equations and their
  center support, the Gaussian `10 x 8` matrix, the seven-minor `12`, the
  scale-fixed tangent, and the corrected `48i` directional derivative.
- The determinantal review confirmed the exact `45/960` cover and found the
  main scope trap: a seven-minor chart is not rank seven until the full
  `I_8(A)` condition is imposed.  The final theorem includes that condition
  and retains `V(I_7(A))` as a named branch.
- The proof-topology review accepted the cover as load-bearing only after the
  response pullbacks and saturated residual ideals were named explicitly.  It
  confirmed the six-variable rank-eight and seven-variable rank-seven target
  rings, while rejecting any claim that the atlas alone advances or excludes
  `V(I_Pl)`.  It also required the `C_F`-rank-drop branch to return to the raw
  response incidence rather than rely on bordered quadrics that vanish there.

No reviewer found an exact countermodel or a contradiction with `GLD75` or
`GLD83`.  The package is accepted only as a parameter reduction; no response
nonextension claim is inferred from it.  All three final static reviews
accepted the repaired package with no priority-zero or priority-one finding.

## Verification and independence

Run:

```powershell
python claims/arbitrary-order/verify_four_root_torus_star_equal_leaf_survivor_center_rank_determinantal_chart_reduction.py
python -I claims/arbitrary-order/audit_four_root_torus_star_equal_leaf_survivor_center_rank_determinantal_chart_reduction.py
```

The primary verifier uses SymPy and the established sparse parser to
reconstruct the complete ten-generator system.  It checks center-linearity,
the Gaussian center rank and named minors, the scale-fixed tangent, the
directional determinant, and the chart counts.

The no-import audit uses only the standard library.  It independently parses
the sparse certificate over rational Gaussian pairs, performs its own row
reduction and determinant arithmetic, and recomputes the same center-linear,
rank, tangent, and scope controls without importing repository modules.

The universal localized Schur isomorphisms and determinantal partition are
the written proof.  Exact Gaussian replay is not substituted for that proof.

## Proof-tree delta and remaining obligation

`GLD84` replaces the undifferentiated equal-leaf residual

```text
V(I_Pl) intersect D(Omega) subset B
```

by three exact families:

1. rank-eight pullbacks in six leaf variables with two survivor equations;
2. exact rank-seven pullbacks in six leaf variables plus one free center
   kernel coordinate, with three compatibility equations and `I_8(A)=0`;
3. `V(I_Pl,I_7(A)) intersect D(Omega)`.

In ambient notation these are governed by the exact saturated ideals

```text
(I_B+I_Pl):(Omega mu_R)^infinity,
(I_B+I_8(A)+I_Pl):(Omega nu_(R,C))^infinity,
(I_B+I_7(A)+I_Pl):Omega^infinity.
```

The first load-bearing successor is to compute or certify the pulled-back
Fitting ideal on the named rank-eight chart adjacent to `GLD72`, then audit
the Gaussian rank-seven chart independently.  A complete fixed-chart result
must still cover every surviving finite chart and the rank-at-most-six
branch.
