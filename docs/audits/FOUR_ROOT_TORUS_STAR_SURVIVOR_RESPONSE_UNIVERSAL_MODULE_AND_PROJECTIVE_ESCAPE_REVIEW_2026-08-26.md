# Review: survivor response universal module and projective escape

Date: 2026-08-26

## Verdict

Accept `GLD76` as an **exact local fixed-star incidence reduction and route
correction**.  Do not describe it as a first-response exclusion on a
survivor-open set, an exhaustive compactification, an affine lift, a source
theorem, or progress on the global status beyond the named fixed-star parent
obligation.  The global Krenn--Gu conjecture remains **UNRESOLVED**.

## What was checked

The primary computations reconstruct the fixed `81 x 79` nuisance map of rank
`44`, its `35`-dimensional kernel, the scale-fixed `GLD75` chart, and the
complete `q_0` legal response.  The thirteen coefficient-independent response
columns have full-tensor rank `13`.  Quotienting them gives an exact
`68 x 4` root-response matrix and an equivalent `4 x 3` lift system with
`204` equations.  This is an image-quotient equivalence, so it retains every
rank-drop fibre and uses no response minor.

An attempted three-root-column compression was correctly rejected.  The
signed combination `H_0+H_1+H_2-H_3` vanishes in the mixed quotient used by
`GLD74`, but not in the full `68`-dimensional quotient.  Keeping four columns
is necessary for an exact universal containment statement.

The leaf-`S_3` verifier checks not only `b`-equivariance but also covariance of
all four matching-derived root response maps and invariance of the thirteen
fixed mixed response columns.  The resulting character decompositions are
integral and dimension-consistent.  In particular,

```text
ker b = 8 trivial + 3 sign + 12 standard copies,
Q_GLD74 = 20 trivial + 3 sign + 21 standard copies.
```

Thus the symmetry calculation is a valid block reduction, not an argument
that non-invariant raw directions may be discarded.

The projective-boundary verifier rebuilds the exact `GLD74` affine quotient
and checks two hard-coded sparse vectors.  The corresponding `130 x 35`
proportionality matrices have rank `34`, each displayed vector spans the
one-dimensional kernel, the leading `65 x 3` quotient has rank one and a
nonzero first column, and the induced raw directions transform by the leaf
sign representation.

The independent standard-library audit reconstructs the transformed
permanent map, raw fibre, response maps, and quotient with reversed fibre
variable order.  It then independently checks the two sparse projective
witnesses, their raw-kernel property, proportional-column identities, and
rank-`34` matrices while replaying the original `GLD74` certificates.  It
does not import any primary project module.

## Load-bearing scope limits

1. The `15`-variable frame chart is a certified local survivor germ.  The four
   scale-fixed free coordinates are local parameters; no global rational
   parametrization or global component statement is claimed.
2. The `68 x 4` module is the exact local parent incidence.  Its emptiness has
   not been decided.
3. The two projective witnesses concern the homogenized **necessary mixed
   rank-one condition** inherited from `GLD74`.  They are not finite raw
   coefficients and do not satisfy the universal affine response incidence.
4. The witnesses prove the projective boundary is nonempty, but not that it
   consists of exactly two points.  A selected-minor refinement timed out and
   supplies no case-cover theorem.
5. Nonempty boundary blocks the simplest proper-projection argument.  It does
   not prove that affine solutions approach the boundary under a survivor
   deformation; that is the newly named strict-transform obligation.
6. The `GLD74` affine theorem is unchanged: every finite raw preimage of the
   exact Gaussian tensor still fails first response.
7. No interface/source covariance beyond the fixed torus-star model, no
   maximum-root or fifth-root claim, and no graph witness follows.

## Recommended successor

Blow up the homogenized response incidence along the two verified sign
directions and compute its strict transform against the four scale-fixed
survivor directions.  Either an exact finite branch appears and must be
escalated, or the exceptional charts can be excluded and a principal-open
certificate-lifting attempt can resume.  A comprehensive Groebner cover
should be applied only after this boundary branch is separated.
