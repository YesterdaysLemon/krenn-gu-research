# Fixed-Q joint M/Z quotient and paired-attachment hostile review -- 2026-08-17

## Verdict

**Accepted at the frozen theorem and script hashes recorded below.**  No P0
or P1 defect remains.  The result is an exact joint module-selector theorem,
a conditional paired-attachment interface, and a fixed-fibre block-cover
boundary.  It does not force joint rank two on a hypothetical witness,
integrate a formal deck fibre into a graph fibre, establish the required
activity, or imply a permanent restriction.  The global Krenn--Gu conjecture
remains **UNRESOLVED**.

## Joint quotient and selector rank

The two labelled targets are correctly owned by the same complete fixed-`Q`
companion equation:

```text
I=S          -> M_S,
I=Q union S  -> Z_S.
```

Removing exactly these summands and retaining coefficient slices of every
other label gives the complete joint nuisance quotient.  Its accessible
operator combinations are precisely the row space of the two desired
quotient classes.  Separate cross-normalized `M_S,Z_S` selectors exist
exactly when `k_S=2`; a rank-one quotient supplies only one projective
operator combination.

On a hypothetical witness, the quotient tensor factors through the
desired-class and response maps.  Consequently

```text
q_S <= min(k_S,r_S) <= 2,
q_S=2 iff k_S=r_S=2.
```

All rank-one and rank-zero branches, including nonzero realized cancellation,
are stated correctly.  The `M+aZ` result is properly limited to a fixed
compatible `Z` fibre with fixed `Gamma_Q`.  It has the same affine difference
kernel as `M`, but does not separately reconstruct `M`, transfer target
diagonality, or create a physical graph fibre.

## Block covers and localized detector

The block-cover results are exact:

- full ternary complete-bipartite `K_(3,3)`: `dim L=16`, `kappa(L)=4`;
- full ternary seven-port `K_(5,2)`: `dim L=24`, `kappa(L)=6`.

The written decompositions prove both sufficiency and minimality, and both
implementations independently verify the displayed bases and covers.

The localized detector is correct.  One attached four-port tensor and one
diagonal pair from each nuisance complementary matching isolate the desired
mixed coefficient.  The corrected sharpness control has matching
contributions `+0011,+0000,-0011`, hence a nonzero pure `M_4=0000` tensor
while one remaining nuisance matching cancels the desired mixed product.
Minimality is appropriately restricted to the coefficientwise
nuisance-killing scheme.

## Physical GLD11 sharpness ledger

The GLD11 physical ledger was exhaustively replayed.  After removing the
desired `M` column, the six joint nuisance slice counts are

```text
202, 202, 199, 174, 193, 193.
```

All five coordinate pivots survive.  For `S=12`, only the two claimed
companion sets meet the separator coordinates, and both are annihilated.
Thus all six pair targets have `k_S=1`, while the four-port target has
`k_U=0`.  No deck label is omitted because every lower companion grade
vanishes with the zero root-root blocks.  The physical paired response values
are nevertheless nonzero, and the already displayed mixed full-state
coefficient excludes this graph-side control from the witness locus.

## Independent checks and frozen hashes

The primary exact rational replay and the independent standard-library
`Fraction`/recursive-companion audit both pass.  Ruff check, Ruff format
check, whitespace/conflict scans, and `git diff --check` also pass.

Frozen at base HEAD `7098456780f13d09871b238e4aaa508576be0f3e`:

```text
theorem  4ca4818283b2e624ad3e414b90c59cc4819e0edd04d18b2f3157593ff3db570f
primary  f227b1ee917824b333135a3e85cf2e8cad3f8af7b7b05d92eeaabf4a0a01c3e0
audit    fdafb6de7933dc02daa2baee5b725e717d8a3575756d83f181acf8e51dd71acf
```

The scripts replay the finite rank, separator, cover, and detector controls.
The complete nuisance quotient, tensor-rank, deleted-vertex decomposition,
and fixed-fibre arguments remain the load-bearing mathematical proofs.

## Exact remainder

Still **UNKNOWN**: forcing joint rank two on any required witness target;
simultaneous pair-block-cover and four-port attachment; exclusion of the
rank-one and rank-zero branches by the full mixed equations; physical
integration of a formal fixed-module fibre; the complementary-colour activity
needed by the localized detector; and every weighted-permanent consequence.
