# Hostile review: four-root determinant-divisor rank-one contained and one-sided permanent reduction

Date: 2026-08-20

## Verdict

**PASS, at the theorem's exact pointwise characteristic-zero reduction
scope.  No P0, P1, or P2 defect remains in the frozen surface below.**

Start with an actual hypothetical ternary GHZ witness whose
maximum-cardinality torus root has order four and surplus two.  Fix the same
physical residual pair \(Q=\{q_0,q_1\}\) supplied by GLS4, assume all six
same-\(Q\) pair-response tensors vanish, impose the complete contracted
six-slot GHZ target, and enter rank-one Branch I or Branch II of the GLS11
trichotomy after the GLS12 exclusions.

The reviewed GLS14 package gives a pointwise exhaustive reduction of those
two remaining rank-one branches:

- Branch I either gives one of eight deleted-mode decomposable \(P_5\)
  pullbacks, gives a ninth decomposable \(P_5\) cross pullback, or enters the
  same-coordinate balanced core.  On that core, \(\Phi\ne0\) makes the
  physical seventh response nonzero without supplying a legal selector;
  \(\Phi=0\) aligns the two balanced partitions, forces the displayed
  \(e_2\)-equation, and leaves one augmented-\(P_6\) face defect
  \(\Psi\in J\).
- Branch-II singleton support supplies a nonzero decomposable \(P_4\)
  companion.  Its committed rank-drop theorem applies only on the separately
  declared all-four-ranks-at-least-two leaf.
- Branch-II two-port support, and its exact labelled transpose, supplies one
  of two nonzero decomposable \(P_5\) deletions.

These are source-to-interface reductions, not downstream exclusions.  The
\(P_5\) outputs are nonconcise pullbacks on their exact restricted domains and
do not enter GLS13's weighted \(P_5\to\Delta_3\) node.  The augmented
\(P_6\) tensor is weighted diagonal only when \(\Psi=0\).  A nonzero seventh
response alone is not a legal nuisance-annihilating GLD selector.  Nothing in
the package closes weaker response-zero patterns, nonzero-response
absorption, exceptional selector fibres, synchronization, activity,
nuisance-survival, target-pure-anchor hypotheses, the supply-and-target
strategic node, permanent nonrestriction, or extraction/gluing.  The global
Krenn--Gu conjecture remains **UNRESOLVED**.

No apparent exact hypothetical witness, global counterexample, contradiction
with a live theorem, nonexhaustive final case cover, or candidate global
resolution was found.

## Frozen review surfaces

The isolated worktree was based on

~~~text
branch: codex/kg-r4-rank1-remaining-20260820
HEAD:   169237f9aa58d8410a27e39a069e1ca26ddb65a7
~~~

The six reviewed pre-review inputs were frozen by SHA-256:

| artifact | SHA-256 |
| --- | --- |
| [theorem](../../claims/arbitrary-order/FOUR_ROOT_DETERMINANT_DIVISOR_RANK_ONE_CONTAINED_AND_ONE_SIDED_PERMANENT_REDUCTION_THEOREM.md) | 818c50306e8542016b0c322e8e24dda5170dfd364a396b9f861cc4810a7d4ee2 |
| [focused exact primary](../../claims/arbitrary-order/verify_four_root_determinant_divisor_rank_one_contained_and_one_sided_permanent_reduction.py) | 4d62e916ca6870b2a0b3a88a25ffe9768d40f5f19a2d5ecffccdd67bf5735c84 |
| [independent no-import audit](../../claims/arbitrary-order/audit_four_root_determinant_divisor_rank_one_contained_and_one_sided_permanent_reduction.py) | 55637cace144de66a5e6959ef5e262a7228973bc64d7058730f6a5191a10f149 |
| [arbitrary-order navigation](../../claims/arbitrary-order/README.md) | bd7ceb50404cb740c1a2e822ee91ccacead704ba5835488fd112bf548489d039 |
| [live frontier](../current-frontier.md) | 105ed33dbf545cc629edf62c98c8a7a9e88fea1c8574f2d63fc54142a7edd100 |
| [supply/target node DAG](../history/handoffs/MAXIMUM_ROOT_SURPLUS_TWO_SUPPLY_TARGET_NODE_DAG_2026-08-20.md) | 845994e86167e0dade8afe9276c2fb69813cf31cd5dd8f8949f85570bb60fe74 |

### Final promotion and hash freeze

After the hostile derivation and both exact replays passed, the package was
promoted without changing its statement, proof, numbered equations,
nonzero/divisor ledger, dependencies, or downstream mathematical boundary.
The promotion-only patch did exactly the following:

1. in the theorem, changed the hostile-review gate from pending to pass,
   shortened the verification-section title, and linked this review;
2. in the arbitrary-order navigation, linked this review and changed the
   review gate from pending to pass;
3. in the live frontier, changed the GLS14 evidence label from pending to
   reviewed/PASS and linked this review; and
4. in the supply/target DAG, changed the GLS14 row, bullet, and checklist
   evidence labels from proved/pending to reviewed/pass.

The final promoted inputs are frozen by SHA-256:

| artifact | final SHA-256 |
| --- | --- |
| [theorem](../../claims/arbitrary-order/FOUR_ROOT_DETERMINANT_DIVISOR_RANK_ONE_CONTAINED_AND_ONE_SIDED_PERMANENT_REDUCTION_THEOREM.md) | 5ea7d4847c26a932c36ef4edffd0e8d0893ce01e55186116a9610fe487797e91 |
| [focused exact primary](../../claims/arbitrary-order/verify_four_root_determinant_divisor_rank_one_contained_and_one_sided_permanent_reduction.py) | 4d62e916ca6870b2a0b3a88a25ffe9768d40f5f19a2d5ecffccdd67bf5735c84 |
| [independent no-import audit](../../claims/arbitrary-order/audit_four_root_determinant_divisor_rank_one_contained_and_one_sided_permanent_reduction.py) | 55637cace144de66a5e6959ef5e262a7228973bc64d7058730f6a5191a10f149 |
| [arbitrary-order navigation](../../claims/arbitrary-order/README.md) | c149a872c7679b35579e6d0540b2555b9e7c4eb7d37a3e5e2fc7c6ade6a56ef2 |
| [live frontier](../current-frontier.md) | 8a83d482953f4b036f81ff10b40b784ccb3494c5b7c21a2515c823af25d43b3f |
| [supply/target node DAG](../history/handoffs/MAXIMUM_ROOT_SURPLUS_TWO_SUPPLY_TARGET_NODE_DAG_2026-08-20.md) | a58f6fa5c69d1739ceb41de5488339f8a3f6581f1e0ab3cc79fb213fa37d0905 |

The final re-audit recomputed all six hashes, rechecked the complete current
theorem body against the hostile derivation below, and resolved all 722 local
Markdown links across the theorem, navigation, frontier, DAG, and this review.
There is no stale GLS14 candidate or pending marker.  The final surfaces say
exactly that GLS14 is proved/reviewed and its three output interfaces remain
open; the supply-and-target strategic node remains **OPEN** and the global
Krenn--Gu conjecture remains **UNRESOLVED**.

The pre-promotion theorem bytes were untracked and are not retained as a Git
blob in this worktree, so the old-to-new byte diff cannot be reconstructed
from Git alone.  The pre-promotion hashes above and the recorded promotion
patch establish the lineage; the final current-body hostile check found no
mathematical addition or strengthening.  This provenance limitation is not
silently presented as an independently reconstructed byte diff.

The owning GLS11, GLS12, GLS13, arbitrary-rank quartic kernel-type proof,
decomposable-\(P_4\) rank-drop theorem, and \(P_5/P_6\) frontier packages
were read only to audit inherited hypotheses, imported dependency boundaries,
and downstream stops.  This hostile review does not silently re-prove or
strengthen those owning packages.

## Source, field, and quantifier audit

The physical source is

~~~text
Omega=R disjoint-union B,       |R|=4,       |B|=6,
Q={q_0,q_1} subset B,           U=B-Q,       |U|=4.
~~~

The four vectors on \(R\) form a maximum-cardinality torus root.  The pair
\(Q\) is the same GLS4 physical pair, with \(H_Q\ne0\),
\(\Pi_Q\ne0\), higher-column survival, and raw incidence retained.  For all
six \(\{u,v\}\subset U\), the assumed equation is the full labelled tensor
identity

~~~text
H boxtimes B_uv+A_u boxtimes C_v+A_v boxtimes C_u=0.
~~~

The target is the full sum over all fifteen physical pair labels and all
\(3^6=729\) outside words, with \(\mu_0\mu_1\mu_2\ne0\).  Every companion
\(\Pi_P\) is the order-four permanent of the same physical incidence family.
No independently declared formal deck is substituted into the theorem.

The theorem is over a characteristic-zero field \(K\); the actual-witness
application is over \(\mathbb C\).  All tensor slots remain labelled.  Target
colour relabelling is simultaneous, and port relabelling preserves physical
labels.  No independent relabelling of the \(a\)- and \(c\)-families is used.

The separate seventh response is not inferred from the six pair responses.
It is imposed exactly when the literal all-seven-zero leaf is entered.  On
Branch I, GLS11 gives

~~~text
T_4=-2 x tensor y tensor Phi(a,c),
~~~

so characteristic zero and \(x,y\ne0\) give
\(T_4=0\Longleftrightarrow\Phi=0\).  The final case cover retains the whole
\(\Phi\ne0\) side, including both misaligned partitions and aligned
partitions with \(e_2\ne0\).

## Independent hostile derivation

The following derivation was completed independently of the no-import audit.
Agreement among agents or scripts was not treated as proof.

### 1. The arbitrary-rank quartic gate is exact

Let \(V=X\oplus K^2\), with \(X\) finite-dimensional, bottom covectors
\(a,b\), and an arbitrary symmetric form \(q\) on \(X\).  Suppose
\(\operatorname{Sym}(a b q)\) pulled back to a weighted \(\Delta_3\).  Target
concision makes all four local maps injective.  For each mode put

~~~text
K_i=ker(a composed with phi_i) intersect ker(b composed with phi_i).
~~~

Every \(K_i\) is nonzero.  Contracting at a nonzero \(z\in K_i\) gives
\(\operatorname{Sym}(a b q(\phi_i(z),-))\).  If the last covector is zero,
the source contraction is zero while the weighted diagonal target contraction
is nonzero.  Otherwise the three covectors are independent and the essential
source is \(P_3\).  A fully supported \(z\) would then give
\(P_3\to\Delta_3\), contradicting tensor ranks \(4\ne3\).

Thus each \(K_i\) lies in one coordinate hyperplane and is a one- or
two-support line or a coordinate plane.  Restriction on complementary pairs
gives the same coordinatewise-product matrix identity as the committed
kernel-type proof.  Direct inspection of that proof confirms that its five
line/plane cases use only this identity; nondegeneracy of \(q\) was used only
to make the contracted third covector nonzero, which the radical contradiction
now supplies.  Hence the exhaustion is valid for every rank of \(q\),
including zero.

For a general characteristic-zero field, all alleged restriction data lie in
a finitely generated subfield of \(K\), which embeds in \(\mathbb C\).
Polynomial equalities, ranks, and declared nonzeros survive the embedding.
No determinant of \(q\) is saturated.

In Branch I, if both residual factors \(x,y\) were noncoordinate, their
kernels would contain fully supported vectors.  The two-row augmentation

~~~text
Ltilde_q0=(L_q0,x,0),
Ltilde_q1=(L_q1,0,-y),
Ltilde_u =(L_u,-c_u,a_u)
~~~

has bottom minors

~~~text
beta_Q=-H,
beta_(q0,u)=A_u,
beta_(q1,u)=C_u,
beta_uv=B_uv.
~~~

Therefore

~~~text
P_6(Ltilde)=T_GHZ-2 H tensor Pi_Q.
~~~

Contracting the residual modes at the two torus kernel vectors kills the
correction term and leaves a three-term diagonal quartic.  The contracted
universal source is
\(+\operatorname{Sym}(e_4^*e_5^*q)\); the port bottom rows \((-c_u,a_u)\)
pull it back to \(-\operatorname{Sym}(c a q)\).  The overall sign is
irrelevant and the quadratic factor may have arbitrary rank, contradicting
the quartic gate.  Thus at least one of \(x,y\) is coordinate.

### 2. The eight-cover split and ninth pullback are exhaustive

For a deleted port \(t\), restriction at \(q_1\) to \(K(y)\) and at the
three other ports to \(K(a_v)\) gives

~~~text
a_t tensor Psi_t^a
 =sum_(d notin C_t^a) mu_d e_(t,d)^* tensor
   e_(q_0,d)^* tensor (e_(q_1,d)^*|K(y)) tensor
   product_(v!=t)(e_(v,d)^*|K(a_v)).
~~~

The symmetric \(c\)-identity is obtained by the exact labelled shore
exchange.  Every complementary factor for a surviving colour is nonzero.
Different surviving colours are independent on the complementary side
because the unrestricted \(q_0\)-slot, or \(q_1\)-slot after exchange,
carries distinct coordinate tags.  The left side has deleted-port flattening
rank at most one.  Consequently every cover has size at least two.

If a cover has size two, the sole missing target colour forces the deleted
factor onto that coordinate and makes the displayed \(P_5\) cofactor nonzero
and decomposable.  If no one of the eight covers has size two, every cover has
all three colours.  Deleting any one member shows that the fixed residual
factor must be coordinate and the other factor family consists of exactly two
nonzero members of each complementary colour.  Applying this on both shores
gives the balanced \(2+2\) core and kills all eight cofactors.

On that core, if the residual colours differ, restricting \(q_1\) by \(y\)
and every port by \(c_u\) kills exactly the other two target colours.  The
sole surviving target colour equals the \(x\)-colour, so the ninth cross
\(P_5\) pullback is nonzero and decomposable.  The only no-pure-pullback core
therefore has \(x,y\) on the same target coordinate.  This split is pointwise;
zero and noncoordinate covectors contribute no colour, and no support sample
or generic chart is used.

### 3. The seventh-response and augmented-face branches retain every leaf

In the same-coordinate balanced core, let \(A,C\subset U\) be the two ports
where the \(a\)- and \(c\)-labels equal colour one.  If \(A\ne C\), at least
one of the six two-subset summands of \(\Phi\) has a unique coordinate word:
for disjoint \(A,C\), the all-one word is unique; for intersection size one,
the word multiplicities include singleton fibres.  Its coefficient is a
product of declared nonzero scalars, so cancellation is impossible and
\(\Phi\ne0\).

Hence \(\Phi=0\) aligns the two labelled partitions.  All six terms then
share one port word and their coefficient is

~~~text
(product_u gamma_u) e_2(alpha_u/gamma_u : u in U).
~~~

Every \(\gamma_u\) is declared nonzero on this stratum, so the ratio form
introduces no exceptional divisor.  The polynomial multiplied form precedes
the division.

On the aligned leaf, write

~~~text
h=xi eta,
p=mu_0/h,
Psi=Pi_Q-p e_0^(tensor 4).
~~~

The exact augmented identity is

~~~text
P_6(Ltilde)
 =-mu_0 e_0^(tensor 6)
   +mu_1 e_1^(tensor 6)
   +mu_2 e_2^(tensor 6)
   -2h e_(q0,0)^* tensor e_(q1,0)^* tensor Psi.
~~~

The sign \(-2\) follows from the bottom-minor ledger above.  In the
\((q_0,q_1)=(0,0)\) slice, the non-\(Q\) terms do not vanish identically;
each lies in the active-line module \(J\).  Quotienting gives
\(h\Psi\in J\).  Only at the all-zero port word do those terms vanish,
separately giving \(h\Pi_Q[0000]=\mu_0\).  Since \(h\ne0\),

~~~text
Psi in J,              Psi[0000]=0.
~~~

All six one-mode flattenings of the augmented tensor have rank three.  The
two residual modes are separated by the complementary residual colour; at a
port, the pure colour-one and colour-two slices have residual words \(11\)
and \(22\), while the colour-zero all-zero coefficient remains nonzero.
Therefore every augmented map is injective and every original incidence map
has rank at least two.  The displayed row-line noncontainment conclusion is
made only when that original rank equals two.

A nonzero \(\Psi\) necessarily creates an off-diagonal coefficient because
its residual word is \(00\) and its all-zero port coefficient vanishes.  Thus
the augmented tensor is weighted diagonal exactly when \(\Psi=0\); no
\(P_6\to\Delta_3\) or GL-equivalence conclusion is made on the
\(\Psi\ne0\) face.

The complementary \(\Phi\ne0\) branch remains equally explicit.  It contains
both misaligned balanced partitions and aligned partitions with
\(e_2\ne0\).  It provides a nonzero physical seventh response, but no legal
response/target selector, nuisance annihilator, or anchor.  The final DAG
routes it back to the response-visible selector obligation rather than
silently deleting it.

### 4. Branch-II singleton has an exact conditional rank gate

On singleton support, \(a_s\) and \(y\) are nonzero coordinate covectors.
Quotient the \(q_1\)-slot by \(K y\) and the \(s\)-slot by \(K a_s\).
Every source term except

~~~text
D_s tensor Pi_(q_1,s)
~~~

vanishes.  If the two quotient-killed colours agreed, the surviving target
would have flattening rank two while the source has rank at most one.
Therefore the colours differ, and the unique third target colour forces both
\(D_s\) and \(\Pi_{q_1s}\) to be nonzero decomposable tensors with scalar
product \(\mu_k\).

This pure \(P_4\) companion alone supplies no local-rank floor.  Maximum-root
maximality excludes rank-zero incidence maps; a rank-one row line must be
coordinate, since a noncoordinate line has a fully supported kernel vector
that would extend the root.  This exact low-rank leaf remains open.  Only if
all four incidence maps have rank at least two does the committed complex
decomposable-\(P_4\) theorem imply that at least two have rank exactly two.
Finitely generated coefficient-field descent preserves the ranks and the
pure identity.  The theorem neither excludes the rank-one leaf nor promotes
the conditional rank-drop conclusion to an exclusion.

### 5. Branch-II two-port signs and transpose are complete

For active ports \(s,t\), the escaping terms are

~~~text
C_s=d tensor a_s+y tensor c_s,
C_t=-d tensor a_t+y tensor c_t.
~~~

Deleting \(t\) and restricting \(q_0,q_1,s\) to
\(K(x),K(y),K(a_s)\) produces the bottom rows \(-d,-c_s,-c_m,-c_n\) and
the exact factorization

~~~text
a_t tensor Theta_t
 =sum_(r notin D_t) mu_r e_(t,r)^* tensor restricted pure-r complement,
D_t={col(x),col(y),col(a_s)}.
~~~

Deleting \(s\) has bottom row \(+d\) and the exchanged cover
\(D_s=\{\operatorname{col}(x),\operatorname{col}(y),
\operatorname{col}(a_t)\}\).  The signs are the literal signs of the two
escaping blocks.  The escaping \(d\otimes a\) terms vanish at the active
\(a\)-coordinate when required, not at the residual \(q_1\)-slot.

The same complementary-tag flattening argument makes both cover sizes at
least two.  A size-two cover gives a nonzero decomposable \(P_5\) cofactor.
If both cofactors were zero, both covers would contain all three colours, so
\(x,y\) would be distinct coordinate colours and \(a_s,a_t\) the same third
colour.  At the all-\(\operatorname{col}(x)\) word, every source edge has a
forced wrong-colour factor while the target coefficient is the declared
nonzero \(\mu_{\operatorname{col}(x)}\), a contradiction.  Thus at least one
cofactor is nonzero and decomposable.  The exact labelled exchange

~~~text
(q_0,x,A,a) <-> (q_1,y,C,c)
~~~

gives the whole transpose branch; no unlabelled symmetry is invoked.

## Controls and dependency boundaries

The formal balanced control declares companion tensors independently.  It
satisfies all \(729\) complete formal target coefficients, all six response
zeros, and the seventh quartic, but is expressly not a common-incidence graph
point.  It proves only that formal companion equations do not kill the
aligned core.

The converse common-incidence control uses six exact rank-three incidence
maps and the aligned edge blocks.  It realizes the three nonzero pure anchors
and the seventh quartic, but the mixed word \(002200\) has a nonzero source
coefficient against zero GHZ target.  It is therefore not a complete-target
point, witness, or counterexample.  It shows why common incidence, local
concision, pure anchors, and the quartic still do not replace the full mixed
equations.

The new proof imports exactly two theorem-sized facts rather than pretending
to replay them computationally:

1. the five kernel-type cases in the committed quartic subrank proof, after
   the new radical gate verifies their hypotheses for arbitrary-rank \(q\);
2. the decomposable-\(P_4\) rank-drop theorem on the explicitly separated
   all-ranks-at-least-two singleton leaf.

The no-import audit states these boundaries and does not count finite fixtures
as proofs of them.

## Nonzero, divisor, and downstream-interface audit

The only target saturation is
\(\mu_0\mu_1\mu_2\ne0\).  Factor lines and scalars are normalized only on
their declared active strata.  Kernel restrictions are exact linear
subspaces.  No kernel coordinate, observable minor, response coordinate,
nuisance determinant, selector coefficient, alignment form, activity factor,
or target-module denominator is inverted.

The balanced ratios use the declared \(\gamma_u\ne0\), and
\(p=\mu_0/(\xi\eta)\) uses the declared \(\xi\eta\ne0\).  The proof retains
the preceding multiplied polynomial identities.  The arbitrary-rank quartic
proof saturates no determinant of \(q\).

The pure \(P_5\) tensors are images on the exact restricted local spaces.
They are not claimed concise, injective, or equivalent to \(\Delta_3\), and
no committed theorem excludes them.  They define a new open pure-compression
interface rather than the existing GLS13 node.  The singleton \(P_4\) output
has only the conditional rank statement described above.  The \(P_6\) output
has injective maps but remains a face-defect tensor unless \(\Psi=0\).

This is a legitimate downstream boundary precisely because each output is an
exact permanent pullback with its full restricted-domain and rank data
displayed.  It is not node closure: none of those permanent interfaces has
been excluded, and none supplies the legal same-\(Q\) response/target package
required by the GLD attachment chain.

## Verification and independence audit

The focused primary uses SymPy exact arithmetic.  It checks the eight
restricted Branch-I cofactors and both cross orientations, every finite colour
cover, the seventh-response word fibres, all bottom minors, all \(729\)
augmented-\(P_6\) coefficients, the face module and six flattening ranks, the
formal and common-incidence controls, every singleton quotient orientation,
both two-port deletions and the labelled transpose, and the quartic radical
fixtures.

The no-import audit does not read or import the primary.  It uses only the
Python standard library, exact `Fraction` arithmetic, direct row-assignment
permanents, explicit kernel bases, finite set partitions, quotient-survival
tables, and its own Gaussian elimination.  This is a genuinely different
derivation and representation, not a second filename around the primary.
It independently checks all newly displayed finite identities and the exact
hypotheses at the two imported dependency boundaries.  It correctly does not
claim to re-prove those dependencies.

The written arbitrary-point argument, the SymPy verifier, the standard-library
no-import replay, and this hostile derivation have distinct evidentiary roles.
Subagent agreement was not treated as independent proof.

## Defects found and repaired before final freeze

Earlier candidate bytes had the following defects.  All were repaired and
rechecked before the six hashes above were frozen.

1. **P1, augmented face-module slice.**  An early proof said the non-\(Q\)
   terms vanished on the residual \((0,0)\) slice.  They generally lie in
   \(J\); only the all-zero port coefficient kills them.  The final proof
   separates the quotient-module argument from the pure anchor.
2. **P1, Branch-II escaping-term annihilation.**  An early two-port sentence
   killed the \(d\otimes a\) pieces at \(q_1\).  They are killed at their
   active \(a\)-coordinate.  The final all-pure-word contradiction names the
   correct slot.
3. **P1, six-response DAG coverage.**  An intermediate topology displayed
   only the aligned balanced core.  The final pointwise split retains the
   complete \(\Phi\ne0\) seventh-nonzero side and the
   \(\Phi=0\) aligned face-defect side.  The former is not promoted to a
   legal selector.
4. **P2, cover-flattening independence.**  The first cover proof did not name
   the independent complementary tags.  The final proof uses the unrestricted
   residual coordinate covectors, so the flattening-rank conclusion is exact.
5. **P2, labelled colour normalization.**  An early display suggested the
   two balanced factor families could be colour-permuted independently.  The
   final proof names their complementary colour pairs separately for display
   and later uses only one simultaneous target-colour relabelling.
6. **P2, augmented rank implication.**  An intermediate statement inferred
   row-line noncontainment without conditioning on the original rank.  The
   final implication is asserted only when that rank equals two.
7. **P2, quartic augmentation sign.**  The final proof distinguishes the
   positive universal tensor
   \(\operatorname{Sym}(e_4^*e_5^*q)\) from its negative pullback under the
   port rows \((-c,a)\).  The sign is exact and immaterial to subrank.
8. **P2, field quantifier.**  The quartic lemma now declares \(X\)
   finite-dimensional, making the finitely generated coefficient-field
   descent literal.
9. **P2, permanent-node scope.**  The first topology risked identifying the
   nonconcise decomposable \(P_5\) outputs with GLS13's injective weighted
   \(P_5\to\Delta_3\) node.  The theorem and frontier now call them a new
   open pure-compression interface.
10. **P2, \(\Phi\ne0\) label.**  An intermediate ledger called the entire
    nonzero-\(\Phi\) side “misaligned,” omitting aligned partitions with
    \(e_2\ne0\).  The final ledger and prose use the exact \(\Phi\ne0\)
    condition.
11. **P2, presentation integrity.**  Lost TeX escapes and one control
    character in early draft bytes were removed; final control-character and
    formatting scans pass.

No defect was waived.

## Exact commands and results

From

~~~text
C:\w\kg-supply-target-node-remaining-20260820
~~~

the independent audit was rerun as

~~~text
python -I claims/arbitrary-order/audit_four_root_determinant_divisor_rank_one_contained_and_one_sided_permanent_reduction.py

GLS14 INDEPENDENT NO-IMPORT AUDIT PASS
quartic radical-gate evaluations: 875
augmented minors/all-word P6 checks: 864
Branch-I P5 direct identities: 1152 deleted-mode + 96 cross
cover/balanced/ninth finite rows: 3414
P6 face/rank checks: 744; quotient-module words: 81
Branch-II P5: 432 direct + 432 labelled-transpose
formal control: 729 target + 486 responses + 81 quartic
common-incidence control: 10 anchors/ranks + 1 mixed term + 729 P6 words
~~~

The focused primary was rerun as

~~~text
python claims/arbitrary-order/verify_four_root_determinant_divisor_rank_one_contained_and_one_sided_permanent_reduction.py

FOUR-ROOT RANK-ONE CONTAINED/ONE-SIDED PRIMARY PASS
Branch I: 8 restricted P5 cofactors, 1152 coefficients
whole-family: 2 cofactors, 192 coefficients
colour cover: 1024 profiles; balanced pairs=324
augmented P6: 15 bottom minors; 729 coefficients
face defect: quotient rank 16; six flattening ranks equal 3
Branch II singleton: 8 quotient identities, 2592 coefficients
Branch II two-port: 24 directed/transpose cofactors, 5184 coefficients
quartic contraction: 81 coefficients
arbitrary-rank gate: 8 rank/radical fixtures
~~~

Static and patch checks were

~~~text
python -m ruff check <primary> <audit>
All checks passed!

python -m ruff format --check <primary> <audit>
2 files already formatted

python -m py_compile <primary> <audit>
PASS with external PYTHONPYCACHEPREFIX

git diff --check
PASS
~~~

Both scripts use exact characteristic-zero arithmetic.  Neither run is
numerical, modular, sampled, or a substitute for the arbitrary-point proof.
Repository-wide candidate-tree QA, commit/push, hosted CI, and merge remain
publication steps outside this review record and must be reported separately.

## Proof-DAG and documentation verdict

The frozen navigation surfaces represent GLS14 correctly:

~~~text
GLS12
  -> GLS13: Branch-III two-port weighted P5 -> Delta3, downstream open;
  -> GLS14: Branch-I/II contained and one-sided pointwise reduction.

GLS14
  -> pure P4/P5 compression interface, OPEN;
  -> Phi!=0 response-visible selector obligation, OPEN;
  -> Phi=0 aligned augmented face defect Psi, OPEN.
~~~

GLS14 is a sibling of GLS13, not its descendant.  The pure decomposable
\(P_5\) interfaces do not point to GLS13's weighted node.  The broader GLS7
one-response-zero branch, GLS8 all-target failure profiles, nonzero-response
absorption, exceptional rank drops, and every legal selector gate retain
their own source edges.  The node-closure checklist marks only this narrow
six-response rank-one routing step; all load-bearing attachment and broader
coverage boxes remain unchecked.

The frontier therefore records a genuine new source-to-interface edge without
claiming that any generic, exceptional, response, selector, permanent, or
strategic-node branch has disappeared.

## Final boundary

What is proved:

- the arbitrary-rank quartic subrank obstruction over every
  characteristic-zero field;
- the exhaustive pointwise Branch-I eight-cover/balanced/ninth-pullback
  classification;
- the exact \(\Phi\ne0\) response-visible and \(\Phi=0\)
  alignment/\(e_2\)/face-defect split;
- the signed augmented-\(P_6\) normal form, \(\Psi\in J\), and its local-rank
  consequences;
- the Branch-II singleton pure \(P_4\) companion with its conditional rank
  gate; and
- one exact pure \(P_5\) deletion on every Branch-II two-port point,
  including the labelled transpose.

What is not proved:

- exclusion of any resulting nonconcise decomposable \(P_4/P_5\) interface;
- \(\Psi=0\), or a downstream theorem accepting \(\Psi\ne0\);
- a legal selector on the \(\Phi\ne0\) response-visible leaf;
- weaker response-zero coverage, nonzero-response absorption, or every
  exceptional selector/rank-drop fibre;
- the same-\(Q\) synchronization, alignment, nuisance, activity, and anchor
  package required by a named GLD attachment theorem;
- closure of the maximum-root surplus-two supply-and-target node;
- permanent nonrestriction, downstream extraction/gluing completion, or the
  global conjecture.

The smallest remaining obligation on the aligned Branch-I leaf is to prove
\(\Psi=0\) from the complete common-incidence mixed equations, or to prove a
downstream theorem that accepts the face-defect normal form.  The
\(\Phi\ne0\) leaf still needs a legal response-visible selector.  The pure
\(P_4/P_5\) leaves still need their own downstream treatment.  Beyond this
six-response divisor, the smallest strategic obligation is unchanged: force
one complete legal same-\(Q\) response/target package, or exclude every
pointwise failure branch on the same physical graph.

The supply-and-target strategic node remains **OPEN**.  The global
Krenn--Gu conjecture remains **UNRESOLVED**.
